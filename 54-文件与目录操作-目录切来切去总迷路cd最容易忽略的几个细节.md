# 54｜文件与目录操作｜目录切来切去总迷路？cd 最容易忽略的几个细节

`cd` 之所以容易被低估，是因为大家都觉得它“只是切目录”。

但实际在服务器上，路径迷路、相对路径走偏、脚本里目录不对、软链接目录判断错误，这些问题最后都可能落在 `cd` 的使用细节上。

如果只想先记 3 条：

- 不确定时，绝对路径比相对路径更稳
- 两个目录来回切，`cd -` 很值钱
- 切完目录后，顺手 `pwd`

## 一、什么情况下先用 `cd`

`cd` 最适合这些场景：

- 进入日志目录、配置目录、项目目录
- 在两个目录之间来回切换
- 脚本里显式进入某个工作目录

最常见的几条写法是：

```bash
cd /var/log
cd ..
cd -
cd ~
```

## 二、先记住这几条命令

```bash
cd /etc/nginx
cd logs
cd ..
cd -
cd ~
pwd
```

它们分别适合：

- 用绝对路径进入指定目录
- 进入当前目录下子目录
- 返回上一级
- 回到上一个目录
- 回家目录
- 切换后确认位置

## 三、最常见的几个高频细节

### 1. 绝对路径和相对路径别混着想

```bash
cd /data/app
cd logs
```

第一条不依赖当前所在位置，第二条强依赖当前目录。

如果你已经不确定自己在哪了，优先回到绝对路径，通常更稳。

### 2. 两个目录来回切，优先用 `cd -`

```bash
cd /var/log/nginx
cd /etc/nginx/conf.d
cd -
```

这在日志目录和配置目录之间来回切时非常顺手。

### 3. 切完后别靠感觉，直接确认

```bash
cd /data/app
pwd
```

确认成本很低，但能挡掉很多路径误判。

## 四、脚本里用 `cd` 要注意什么

### 1. 进入目录失败时，别继续往下跑

```bash
cd /data/app || exit 1
```

### 2. 不要默认相对路径一定成立

脚本被不同用户、不同目录调用时，相对路径最容易飘。

### 3. 必要时显式保存和恢复目录

```bash
old_dir=$(pwd)
cd /data/app || exit 1
# 做事
cd "$old_dir"
```

## 五、最常见的误区

### 1. 只顾着切，不确认结果

切目录本身不难，难的是后续操作是否真的发生在你以为的地方。

### 2. 在复杂脚本里大量依赖相对路径

脚本跑在不同入口下时，这很容易出问题。

### 3. 不会用 `cd -`

很多目录来回切换的效率问题，其实一条 `cd -` 就能解决。

## 六、结论

`cd` 真正值得掌握的，不是更多参数，而是路径感。

绝对路径更稳，`cd -` 更省心，切完配 `pwd` 更安全，这三条已经足够实用。
