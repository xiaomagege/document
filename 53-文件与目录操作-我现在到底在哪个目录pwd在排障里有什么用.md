# 53｜文件与目录操作｜我现在到底在哪个目录？pwd 在排障里有什么用

`pwd` 看起来像最简单的命令之一，但很多误删、误改、找错日志的事故，都是从“没先确认当前目录”开始的。

它的价值不在复杂，而在于它经常决定你后面的动作是不是在正确位置上执行。对高风险操作来说，这一步远比看起来重要。

如果只想先记 3 条：

- 执行删除、移动、批量修改前，先 `pwd`
- 和 `cd` 连着用，能显著减少路径误判
- 遇到软链接目录时，记得区分 `pwd` 和 `pwd -P`

## 一、什么情况下先用 `pwd`

`pwd` 最适合这些场景：

- 刚切完目录，想确认自己到底在哪
- 准备执行 `rm`、`mv`、`cp`、`find` 这类有范围影响的命令
- 进入了软链接目录，想确认真实路径

最常见的起手式就是：

```bash
pwd
cd /data/app/logs
pwd
```

## 二、先记住这几条命令

```bash
pwd
pwd -P
cd /var/log/nginx
pwd
old_dir=$(pwd)
```

它们分别适合：

- 看当前目录
- 看解析软链接后的真实目录
- 切目录后做确认
- 在脚本里保存当前位置

## 三、排障和高风险操作前，为什么它很值钱

### 1. 它能挡住很多“人在错目录里操作”的问题

```bash
pwd
ls -lh
```

这两步放在 `rm -rf`、批量移动、批量替换前，非常值得养成习惯。

### 2. 它和 `cd` 配合最有价值

```bash
cd /data/app
pwd
```

不要默认自己已经切到预期目录，尤其是脚本里、跳转多次时，确认一下成本极低，收益却很高。

### 3. 它能帮你识别软链接路径

```bash
pwd
pwd -P
```

如果你在发布目录、版本目录、软链接切换目录里工作，这两条的区别很重要。

## 四、脚本里最常见的用法

### 1. 记录当前目录

```bash
old_dir=$(pwd)
```

### 2. 进入目标目录执行操作

```bash
cd /data/app || exit 1
```

### 3. 执行完后再回去

```bash
cd "$old_dir"
```

这比在脚本里一路相对路径乱飘稳得多。

## 五、最常见的误区

### 1. 觉得它太简单，不值得写

恰恰是这种“太简单”的命令，最容易因为省略而出问题。

### 2. 只凭提示符判断目录

很多 shell 提示符会简写路径，或者你已经经过了多次跳转，真正稳的还是直接 `pwd`。

### 3. 软链接环境下只看逻辑路径

逻辑路径和真实物理路径不一致时，排障很容易绕。

## 六、结论

`pwd` 不负责解决复杂问题，但它负责保证你在正确的位置上解决问题。

高风险操作前先看一眼路径，这个习惯很小，但非常值钱。
