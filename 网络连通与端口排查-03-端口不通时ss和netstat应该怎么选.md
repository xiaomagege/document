# 网络连通与端口排查03：端口不通时，ss 和 netstat 应该怎么选？

端口不通时，最怕的是问题还没分层，就开始到处猜。

服务到底有没有监听，监听的是哪个地址，端口有没有被抢，连接状态是不是异常，这些问题其实都应该先在本机 socket 层看清楚。

如果只想先记 3 条：

- 新环境优先 `ss`
- 老环境没有 `ss` 时，`netstat` 依然能完成第一轮判断
- 不要一上来就猜防火墙，先确认本机到底有没有监听、谁在监听、监听在哪个地址

## 一、什么时候先用 `ss`，什么时候还会用 `netstat`

这两个命令本质上都在回答同一类问题：

**这台机器上的端口和连接，现在是什么状态？**

更推荐的选择是：

- 新环境、常规排查：优先 `ss`
- 老环境、精简环境、历史脚本：仍然会遇到 `netstat`

换句话说，不是 `netstat` 完全没用，而是现在大多数第一轮判断，`ss` 更值得先看。

## 二、先记住这几条高频命令

```bash
ss -lnt
sudo ss -lntp
ss -ant
ss -s
sudo netstat -lntp
netstat -ant
netstat -rn
```

它们分别适合：

- `ss -lnt`：看 TCP 监听端口
- `ss -lntp`：看监听端口对应进程
- `ss -ant`：看所有 TCP 连接
- `ss -s`：看连接摘要
- `netstat -lntp`：老环境里看监听和进程
- `netstat -ant`：老环境里看连接状态
- `netstat -rn`：老环境里顺手看路由表

## 三、端口不通时，建议先这样排

### 1. 先看端口有没有监听

```bash
ss -lnt
```

如果环境里没有 `ss`，再退到：

```bash
sudo netstat -lntp
```

这一层最重要，因为如果端口根本没监听，问题就不是“网络不通”，而是服务端自己没准备好。

### 2. 再看是谁在监听

```bash
sudo ss -lntp | grep ':8080'
```

或者：

```bash
sudo netstat -lntp | grep ':8080'
```

这一步能回答：

- 目标进程是不是已经起来了
- 端口是不是被别的进程占了
- 监听端口是不是和你以为的一致

### 3. 别只看端口号，还要看监听地址

这一点经常是根因：

- `0.0.0.0:8080`：通常表示对外可达
- `127.0.0.1:8080`：通常只本机可达

很多“服务明明启动了但外面访问不了”的问题，根因不是防火墙，而是服务只绑定在回环地址。

### 4. 监听没问题，再看连接状态

```bash
ss -ant
ss -s
```

老环境里可以看：

```bash
netstat -ant
```

这一步适合判断：

- 有没有连接进来
- 是否堆了大量 `TIME_WAIT`、`CLOSE_WAIT`
- 当前连接数是不是异常高

## 四、`ss` 和 `netstat` 到底怎么分工

你可以直接这样理解：

- `ss`：现在更推荐的默认选择
- `netstat`：老环境补位工具

最常见的替换关系：

```bash
netstat -lntp  ->  ss -lntp
netstat -ant   ->  ss -ant
netstat -s     ->  ss -s
```

但 `netstat` 仍然有两个现实价值：

- 老系统上它更常见
- `netstat -rn` 看路由时依然顺手

## 五、最常见的误区

### 1. 端口不通就先查防火墙

如果服务端自己都没监听，防火墙根本不是第一层问题。

### 2. 只看端口，不看监听地址

端口存在不代表外部一定能访问，地址绑定错了，一样会表现成“服务不通”。

### 3. 看到 `TIME_WAIT` 多就马上下结论

它可能只是短连接多，不一定就是故障本身。

### 4. 没有进程信息就以为端口没人占

很多时候只是权限不够，需要：

```bash
sudo ss -lntp
```

或：

```bash
sudo netstat -lntp
```

## 六、结论

端口问题的第一轮排查，核心不是“先猜哪层坏了”，而是先把本机监听和连接状态看清楚。

新环境优先 `ss`，老环境再补 `netstat`。先确认有没有监听、谁在监听、监听在哪，再决定要不要继续查防火墙、链路和应用层，这样排查会快很多。

