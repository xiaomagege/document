# 21｜文本处理与批量处理｜排序结果不对？sort 最容易忽略的几个选项

`sort` 本身不难，难的是很多人默认以为它“会按自己想的方式排”。

结果就是数字排错、容量排错、字段排错，最后怀疑数据有问题，其实只是排序方式没选对。`sort` 真正值得掌握的，不是参数越多越好，而是先分清你到底在排字符串、数字、容量，还是某一列。

如果只想先记 3 条：

- 排数字时先想 `-n`
- 排带 `K/M/G` 的容量时先想 `-h`
- 做统计排行时，`sort` 往往要和 `uniq`、`awk` 一起用

## 一、什么情况下先用 `sort`

`sort` 最适合这些场景：

- 想给一批文本排序
- 想做日志统计排行
- 想在去重前把相同内容排到一起

最常见的起手式是：

```bash
sort file.txt
sort -n nums.txt
sort -u file.txt
```

## 二、先记住这几条命令

```bash
sort file.txt
sort -n nums.txt
sort -nr nums.txt
sort -h sizes.txt
sort -t ':' -k 3 -n /etc/passwd
```

它们分别适合：

- 默认按整行文本排序
- 按数字升序
- 按数字降序
- 按容量单位排序
- 按指定字段排序

## 三、最常见的几种排序场景

### 1. 纯文本排序

```bash
sort names.txt
```

这适合用户名、域名、普通字符串列表。

### 2. 数字排序

```bash
sort -n nums.txt
sort -nr nums.txt
```

如果你不加 `-n`，`10` 和 `2` 的顺序就很容易让人看得发懵。

### 3. 容量排序

```bash
du -h --max-depth=1 /var | sort -hr
```

排磁盘占用时，`-h` 很关键。否则带单位的结果很容易排错。

### 4. 按指定字段排序

```bash
sort -t ':' -k 3 -n /etc/passwd
```

字段排序在配置文件、统计表和文本报表里很常见。真要按列排时，先想清楚分隔符和第几列。

## 四、`sort` 为什么经常和 `uniq` 连用

因为 `uniq` 只能处理相邻重复行。

所以很多统计链路都会先：

```bash
sort file.txt | uniq -c
```

如果再想看出现次数最多的内容，就继续：

```bash
sort file.txt | uniq -c | sort -nr
```

这套组合在日志分析里非常高频。

## 五、最常见的误区

### 1. 排数字却忘了 `-n`

这是最常见也最隐蔽的坑。

### 2. 排容量却忘了 `-h`

带 `K/M/G` 的结果如果按普通字符串排，基本一定会怪。

### 3. 重定向回原文件

像下面这种写法：

```bash
sort names.txt > names.txt
```

有数据风险。要回写时更稳的是：

```bash
sort names.txt -o names.txt
```

## 六、结论

`sort` 真正要先分清的，不是语法，而是“你在排什么类型的数据”。

字符串、数字、容量、字段，这四种想清楚了，`sort` 就会顺很多。
