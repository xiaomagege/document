# 04｜慢系统与性能排查｜CPU高、内存高、僵尸进程，ps 应该怎么看？

`top` 适合第一眼看全局，`ps` 更适合把问题钉到具体进程上。

当你已经知道“机器可能有问题”，但还想进一步回答下面这些问题时，`ps` 就会比 `top` 更好用：

- 到底是谁最吃 CPU
- 到底是谁最占内存
- 某个服务到底有没有起来
- 进程是不是卡在 `D` 状态，或者已经成了僵尸进程

如果只想先记 3 条：

- `ps aux --sort=-%cpu | head -10` 用来看 CPU 热点
- `ps aux --sort=-%mem | head -10` 用来看内存热点
- `ps -eo pid,ppid,user,%cpu,%mem,stat,cmd --sort=-%cpu | head` 适合做精确留证

## 一、什么情况下从 `top` 切到 `ps`

`ps` 最适合做第二步，而不是第一步。

一般是先用 `top` 判断方向，再用 `ps` 做精确定位。它尤其适合这些场景：

- 想把高 CPU 进程按顺序列出来
- 想确认哪个进程最占内存
- 想查某个服务是否真的在运行
- 想看父子进程关系，避免误杀
- 想确认进程状态里是否有 `D`、`Z` 这类异常值

要记住一个差别：

- `top` 是连续刷新
- `ps` 是执行瞬间的快照

所以 `ps` 更适合做“固定证据”和“脚本化筛选”。

## 二、先记住这几个高频命令

这几条已经能覆盖大多数线上排查场景：

```bash
ps -ef
ps aux --sort=-%cpu | head -10
ps aux --sort=-%mem | head -10
ps -eo pid,ppid,user,%cpu,%mem,stat,cmd --sort=-%cpu | head
ps -fp 1234
ps -ef | grep [n]ginx
```

分别适合：

- `ps -ef`：看全量进程和父子关系
- `ps aux --sort=-%cpu | head -10`：找 CPU 热点
- `ps aux --sort=-%mem | head -10`：找内存热点
- `ps -eo ...`：自定义字段，排查和留证都很方便
- `ps -fp 1234`：单看某个 PID 的完整信息
- `ps -ef | grep [n]ginx`：确认某个服务是否存在，同时避开 `grep` 自己

## 三、排查时最常看的输出和字段

### 1. `ps -ef` 更适合看结构

```bash
ps -ef
```

这类输出更适合看：

- 谁是父进程
- 服务是谁拉起来的
- 命令完整路径和启动参数

最常看的字段有：

- `UID`：所属用户
- `PID`：进程 ID
- `PPID`：父进程 ID
- `STIME`：启动时间
- `TIME`：累计 CPU 时间
- `CMD`：启动命令

如果你担心误杀进程，`PPID` 和 `CMD` 往往比 `%CPU` 更重要。

### 2. `ps aux` 更适合看资源占用

```bash
ps aux
```

这类输出更适合看：

- 谁占 CPU 多
- 谁占内存多
- 哪些进程状态异常

最常看的字段有：

- `%CPU`：CPU 使用率
- `%MEM`：内存使用率
- `VSZ`：虚拟内存
- `RSS`：常驻物理内存
- `STAT`：进程状态
- `COMMAND`：命令

在性能排查里，`RSS` 通常比 `VSZ` 更接近你真正想知道的“它到底吃了多少物理内存”。

### 3. `STAT` 是判断方向的关键字段

`STAT` 经常比很多人想象得更重要。

最常见的值有：

- `R`：运行中
- `S`：休眠
- `D`：不可中断等待，常见于 IO 卡顿
- `T`：停止或被跟踪
- `Z`：僵尸进程

附加标志也值得认识：

- `<`：高优先级
- `N`：低优先级
- `s`：会话首进程
- `l`：多线程进程
- `+`：前台进程组

如果 `D` 状态反复出现，就不要再只盯 CPU，而应该往 IO 或存储侧排查。

## 四、最常见的 5 个实战场景

### 1. 查某个服务到底有没有起来

```bash
ps -ef | grep [n]ginx
```

这条命令最适合快速确认：

- 服务进程在不在
- 启动参数是什么
- 是不是拉起了多个 worker

### 2. 按 CPU 从高到低找热点进程

```bash
ps aux --sort=-%cpu | head -10
```

当你已经在 `top` 里看到 CPU 有压力，这条命令可以更稳定地把热点进程列出来，方便截图、留证和后续比对。

### 3. 按内存从高到低找大户

```bash
ps aux --sort=-%mem | head -10
```

如果你怀疑的是内存压力、swap 活跃或者疑似泄漏，这条命令通常是第二步。

### 4. 看指定 PID 的详细信息

```bash
ps -fp 1234
```

适合在准备 `kill` 之前再确认一遍：

- 这是不是你要找的那个进程
- 它是谁拉起来的
- 命令和参数是不是对得上

### 5. 自定义字段，把留证做干净

```bash
ps -eo pid,ppid,user,%cpu,%mem,stat,cmd --sort=-%cpu | head
```

这条命令在排障里特别实用，因为它把最需要的字段一次性列出来了。

用它能同时回答：

- 热点进程是谁
- 它是谁的子进程
- 它现在是运行、休眠还是在等 IO

## 五、`top` 和 `ps` 分工怎么记

你可以直接这样记：

- `top`：先看全局，判断机器是在忙还是在等
- `ps`：再做精确定位，把问题落到具体进程

一个常见排查顺序是：

1. `top -c`
2. `ps aux --sort=-%cpu | head -10`
3. `ps aux --sort=-%mem | head -10`
4. `ps -eo pid,ppid,user,%cpu,%mem,stat,cmd --sort=-%cpu | head`

如果在 `top` 里已经看到 `wa` 高、`D` 状态进程多，那么 `ps` 的作用就不是继续找 CPU 热点，而是确认哪些进程正在等待。

## 六、最常见的误区

### 1. `grep` 把自己也匹配出来

像这样写：

```bash
ps -ef | grep nginx
```

经常会把 `grep nginx` 自己也匹配出来。更稳一点的写法是：

```bash
ps -ef | grep [n]ginx
```

### 2. 把 `ps` 当成实时监控工具

`ps` 是快照，不会自己刷新。

如果你想连续观察变化，用 `top` 更合适；如果你想留下一次性证据，`ps` 更合适。

### 3. 还没确认清楚就直接 `kill`

在生产环境里，先看清 `PPID`、`CMD` 和进程状态，再决定是否处理，会比看到高 CPU 就直接杀稳得多。

## 七、结论

`ps` 不是用来替代 `top` 的，它更像是 `top` 之后的第二把刀。

当你需要把“系统慢”收敛到“到底是哪一个进程、什么状态、什么父子关系”时，`ps` 往往就是最稳的一步。
