# 文本处理与批量处理06：快速切字段、数行数时，cut 和 wc 怎么配合？

很多文本处理需求并不复杂。

你可能只是想把第 1 列切出来，或者想知道某个关键字到底命中了多少行。这类场景如果一上来就写 `awk`，未必错，但也未必是最省事的起点。

如果只想先记 3 条：

- 分隔符稳定、只切字段时，`cut` 很轻
- 想统计行数、命中数、最长行时，`wc` 很顺手
- 先切、再筛、再数，是这两条命令最常见的配合方式

## 一、什么情况下先想到 `cut` 和 `wc`

这两个命令的价值都在“轻量”：

- `cut`：只做字段截取，不做复杂判断
- `wc`：只做数量统计，不做复杂提取

它们最适合这些场景：

- 从 `/etc/passwd`、TSV、简单 CSV 里快速切列
- 统计日志总行数、错误行数、匹配数量
- 先看数据大概规模，再决定要不要继续上 `awk`、`sed`

## 二、先记住这几条高频用法

```bash
cut -d ':' -f 1,7 /etc/passwd
cut -d ',' -f 1,3 data.csv
cut -f 1,2 data.tsv
cut -c 1-19 app.log
wc -l app.log
wc -l < app.log
grep 'ERROR' app.log | wc -l
wc -L access.log
```

它们分别适合：

- `cut -d ':' -f 1,7`：按固定分隔符取多列
- `cut -d ',' -f 1,3`：取 CSV 指定列
- `cut -f 1,2`：按 Tab 取列
- `cut -c 1-19`：按字符位置截固定前缀
- `wc -l file`：统计行数
- `wc -l < file`：只拿数字，不带文件名
- `grep ... | wc -l`：统计命中数
- `wc -L`：看最长一行长度

## 三、这两个命令在排查里最常见的配合方式

### 1. 先把关心的字段切出来

```bash
cut -d ':' -f 1,7 /etc/passwd
cut -d ',' -f 1,3 data.csv
```

适合：

- 文件格式稳定
- 只关心几列内容
- 还不需要条件判断和计算

如果你只是想确认“字段长什么样”，`cut` 往往比 `awk` 更快进入状态。

### 2. 先统计规模和命中数量

```bash
wc -l access.log
grep 'ERROR' app.log | wc -l
find /var/log -type f | wc -l
```

适合：

- 先判断文件或结果规模
- 粗看错误数量、输出条数
- 给后续排查先建立量感

### 3. 先切，再数

这两条命令最常见的组合，不是彼此替代，而是前后衔接：

```bash
cut -d ',' -f 3 data.csv | sort | uniq -c
grep 'ERROR' app.log | wc -l
```

前者是先抽字段再做统计，后者是先过滤命中再做计数。

## 四、什么时候该换成 `awk`

`cut` 和 `wc` 都很省事，但边界也很清楚。

这几种情况就别勉强它们了：

- 分隔符不稳定，字段会漂
- 需要按条件判断后再输出
- 需要边提取边计算
- CSV 里本身带引号和逗号

这时候更适合：

```bash
awk '{print $1}'
awk -F ':' '$3 > 1000 {print $1}'
```

可以把思路记成一句话：

**只切字段，用 `cut`；只做计数，用 `wc`；开始有逻辑判断，就换 `awk`。**

## 五、最常见的误区

### 1. 连续空格输出还硬用 `cut`

字段位置很容易漂，这时 `awk` 通常更稳。

### 2. 把复杂 CSV 交给 `cut`

一旦字段里自己带逗号，`cut` 的边界就到了。

### 3. 只想拿数字，还在用带文件名的 `wc -l file`

如果结果要进脚本判断，通常更适合：

```bash
wc -l < file
```

### 4. 把 `wc -l` 当成绝对语义上的“行数”

它本质上数的是换行符，最后一行没有换行时，结果可能和肉眼理解有差异。

## 六、结论

`cut` 和 `wc` 的价值，不在“强大”，而在“足够快、足够轻”。

格式稳定时先用 `cut`，需要量感时先用 `wc`，先切再数是它们最常见的配合方式。等逻辑开始变复杂，再把主力交给 `awk` 也不迟。

