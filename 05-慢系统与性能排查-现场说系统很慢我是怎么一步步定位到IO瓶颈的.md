# 05｜慢系统与性能排查｜现场说系统很慢，我是怎么一步步定位到 IO 瓶颈的

线上有人反馈“系统整体变慢”，最容易犯的错就是直接认定为 CPU 不够。

这次现场排查里，机器还能登录，业务也没有大面积报错，但接口明显变慢、命令执行也发卡。最终查下来，问题并不在 CPU，而是在磁盘 IO。

## 一、问题现象

现场最先看到的是这些信号：

- 页面加载明显变慢
- 接口超时开始增多
- SSH 还能登录，但命令回显变慢
- 机器没有直接打满 CPU 的迹象

这类现象通常意味着，问题不一定是“机器算不过来”，也可能是“机器在等资源”。

## 二、第一判断：先确认机器是在忙，还是在等

第一步没有急着找“罪魁祸首进程”，而是先执行：

```bash
top -c
```

这一步的目标不是立刻拿到根因，而是先判断方向。

现场最值得关注的不是单个热点进程，而是：

- `id` 还有空闲，说明 CPU 没有真正打满
- `wa` 偏高，说明 CPU 有明显时间在等 IO
- 进程列表里出现 `D` 状态，说明有线程卡在不可中断等待

到这里，排查方向已经从“CPU 算力不足”转向了“IO 等待”。

## 三、现场排查过程

### 1. 先用 `top -c` 做第一轮分类

第一轮主要看 3 件事：

1. `load average` 有没有持续抬升
2. CPU 行里的 `wa` 是否偏高
3. 进程状态里有没有持续出现 `D`

这一步给出的结论是：

- 机器慢，不是因为 CPU 真正打满
- 机器更像是在等磁盘或存储

### 2. 再用 `ps` 排除“明显的 CPU 热点”

接着执行：

```bash
ps aux --sort=-%cpu | head -10
ps -eo pid,ppid,user,%cpu,%mem,stat,cmd --sort=-%cpu | head
```

这一步想确认两件事：

- 有没有异常进程把 CPU 长时间吃满
- 有没有多个进程处在 `D` 状态

现场没有看到特别夸张的 CPU 热点，但能看到等待状态的进程，这进一步说明瓶颈不在“算力”，而在“等待链路”。

### 3. 用 `iostat -y -x 1 3` 做设备级确认

接着执行：

```bash
iostat -y -x 1 3
```

现场最关键的几个指标是：

```text
avg-cpu:  %user  %nice %system %iowait  %steal %idle
           6.12    0.00    2.31    18.45    0.00   73.12

Device            r/s   w/s   rkB/s   wkB/s  await  r_await  w_await  %util
sda              120.3  85.6   5200    3400   45.6     38.2     56.4   98.7
```

这组数字已经足够说明问题：

- `%iowait=18.45%`：CPU 大量时间在等 IO
- `%util=98.7%`：磁盘设备几乎打满
- `await=45.6ms`：单次 IO 等待明显偏高
- `w_await > r_await`：写路径比读路径更慢

这一步把“怀疑 IO”升级成了“有明确证据表明 IO 是主因”。

### 4. 再用 `iotop` 收敛到具体写入进程

为了继续落到业务动作上，再执行：

```bash
iotop -o -d 1
```

这一步最关心的是：

- 哪个进程在持续写盘
- 哪个进程 IO% 明显偏高
- 读写压力里，哪一侧更重

最终定位到高 IO 的业务进程，且写入占比明显偏高。结合现场行为，可以把问题收敛到高频日志刷盘叠加突发写入。

## 四、为什么最终判断根因在 IO

这次不是靠某一个指标单独定性的，而是靠一条完整证据链：

1. `top -c` 看到 CPU 不高，但 `wa` 偏高
2. 进程列表出现持续的 `D` 状态
3. `ps` 没有发现足以解释整机变慢的 CPU 热点
4. `iostat` 明确看到 `%iowait`、`%util`、`await` 全部异常
5. `iotop` 继续把问题收敛到具体写入进程

当这些证据指向同一个方向时，就不需要继续在 CPU 路线上打转了。

## 五、处理动作

### 1. 先止损

先做的不是深挖架构，而是先把系统拉回可用：

- 降低非关键日志级别
- 减少同步刷盘频率
- 暂停高峰期的重 IO 后台任务
- 对明显突刺的写入流量做限流

### 2. 再优化

系统稳定后，再处理长期问题：

- 日志与数据盘分离，减少争抢
- 对高频写路径做批量化和异步化
- 建立 `%iowait`、`%util`、`await`、`D` 状态进程数的基线告警

## 六、这次排查留下的经验

这次现场排查最重要的收获只有一句话：

**系统慢，不一定是 CPU 高；CPU 不高，也不代表机器没问题。**

当你看到“命令执行慢、接口变慢、CPU 却不高”这种组合时，优先检查 IO 等待，通常会比继续死盯 CPU 更快命中根因。
