# 07｜磁盘与空间排查｜`df` 明明有空间，为什么还是报 `No space left on device`？

磁盘空间问题最容易迷惑人的地方在于：你以为自己在看“磁盘还有多少空间”，但 Linux 实际上要看的是“当前写入路径所在的文件系统还能不能继续写”。

`df` 的核心价值不是列出一堆容量数字，而是先回答这几个问题：

- 哪个挂载点快满了
- 当前目录到底落在哪个文件系统上
- 空间还有但仍然写不进去时，是不是 inode 用完了

如果只想先记 3 条：

- 先用 `df -h /path` 看当前路径所在文件系统，不要只看整机总容量
- 报 `No space left on device` 时，立刻补一眼 `df -ih`
- `df` 看文件系统是否还能写，`du` 才看目录里谁占空间

## 一、什么时候先看 `df`

遇到下面这些问题时，第一步通常先看 `df`：

- 应用写日志失败
- 数据库无法落盘
- 构建产物生成失败
- 容器镜像拉取失败
- 系统提示 `No space left on device`
- 根分区、日志分区或数据分区疑似快满

先执行：

```bash
df -h
```

如果你已经知道出问题的目录，优先直接看路径：

```bash
df -h /var/log
df -h /data
df -h /
```

这比只看整机列表更准确，因为问题通常发生在某一个挂载点上。

## 二、排查时最常用的几条命令

```bash
df -h
df -Th
df -ih
df -h /var/log
df -h /data
df -h -x tmpfs -x devtmpfs
df -P /
```

它们分别适合：

- `df -h`：看各挂载点容量
- `df -Th`：同时看文件系统类型
- `df -ih`：看 inode 使用率
- `df -h /path`：看某个路径所在分区
- `df -h -x tmpfs -x devtmpfs`：过滤临时文件系统
- `df -P /`：脚本里使用稳定输出

## 三、输出里先看哪几列

`df -h` 输出里，先盯这几列就够了：

- `Filesystem`：文件系统或设备
- `Size`：总容量
- `Used`：已用容量
- `Avail`：可用容量
- `Use%`：使用率
- `Mounted on`：挂载点

最重要的是 `Use%` 和 `Mounted on`。

比如 `/var/log` 写不进去，就不要只看 `/` 有没有空间，而要看：

```bash
df -h /var/log
```

因为 `/var/log` 可能单独挂载，也可能属于 `/`，这会直接影响后续清理方向。

## 四、磁盘有空间却仍然报错，优先查这 3 类原因

### 1. inode 用完了

容量还有，但 inode 用完时，一样无法创建新文件。

检查命令：

```bash
df -ih
```

如果 `IUse%` 接近 100%，问题通常不是大文件，而是大量小文件。

常见来源包括：

- 缓存目录堆积
- session 文件堆积
- 邮件队列堆积
- 小日志文件切分过多
- 临时文件没有清理

### 2. 写入路径所在分区满了

很多人只看整机里还有某个大分区有空间，却忽略了当前写入路径并不在那个分区。

比如 `/data` 还有很多空间，不代表 `/var/log` 也有空间。

确认方式：

```bash
df -h /var/log
df -h /tmp
df -h /data/app
```

### 3. 文件删了，但空间没释放

如果删除大文件后 `df` 仍然没有下降，可能是文件还被进程打开。

可以查：

```bash
lsof | grep deleted
```

这种情况下，目录里看不到文件，但文件句柄还在，空间不会立刻归还给文件系统。

## 五、`df` 和 `du` 为什么经常对不上

一句话区别：

- `df` 看文件系统整体空间
- `du` 看目录中文件占用

两者不一致常见原因有：

- 文件已删除但仍被进程占用
- 挂载点覆盖了原目录
- 权限不足导致 `du` 统计不完整
- 文件系统预留空间和元数据占用
- 容器 overlay 文件系统带来额外层级

排查顺序建议是：

```bash
df -h /path
df -ih /path
du -h --max-depth=1 /path | sort -hr
lsof | grep deleted
```

## 六、生产环境里的使用建议

重点关注这些位置：

- `/`
- `/var`
- `/var/log`
- `/tmp`
- `/data`
- 数据库数据目录
- 容器运行目录

日常巡检可以先看：

```bash
df -Th
df -ih
```

如果要写脚本，优先用：

```bash
df -P /
```

`-P` 的输出格式更稳定，比直接解析默认输出更适合自动化。

## 七、结论

`df` 最适合回答“这个路径所在的文件系统还能不能继续写”。

遇到空间不足时，先用 `df -h /path` 确认挂载点，再用 `df -ih` 排除 inode 问题，最后再用 `du` 去找具体目录。这个顺序比直接全盘乱删稳得多。
