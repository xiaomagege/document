# 26｜文本处理与批量处理｜文件到底有多少行？wc 不只是数行这么简单

提到 `wc`，很多人脑子里只有一条：`wc -l`。

这当然没错，但它在运维里其实还有不少顺手的场景。统计行数、算命令输出条数、看最长行、区分字符数和字节数，这些都很实用。

如果只想先记 3 条：

- 统计行数最常用的是 `wc -l`
- 想只拿数字，不要文件名时，用重定向 `<`
- 中文文本里，字节数和字符数不是一回事

## 一、什么情况下先用 `wc`

`wc` 最适合这些场景：

- 想知道文件大概有多少行
- 想统计某条命令到底输出了多少条记录
- 想快速确认文件是不是空的

最常见的起手式就是：

```bash
wc -l access.log
grep 'ERROR' app.log | wc -l
wc -l < app.log
```

## 二、先记住这几条命令

```bash
wc -l file.txt
wc -l < file.txt
grep 'ERROR' app.log | wc -l
wc -c app.log
wc -m file.txt
wc -L access.log
```

它们分别适合：

- 统计文件行数
- 只拿数字结果
- 统计命中行数
- 看字节数
- 看字符数
- 看最长行长度

## 三、它在排查里最常见的几种用法

### 1. 统计日志规模

```bash
wc -l access.log
```

这适合快速感知一个日志文件到底是几百行、几万行，还是已经大到不适合直接翻。

### 2. 统计关键字命中数量

```bash
grep 'ERROR' app.log | wc -l
```

这在日志排查里非常顺手。

### 3. 统计命令输出条数

```bash
find /var/log -type f | wc -l
```

文件数量、结果数量、唯一值数量，这类问题经常都能落到 `wc -l`。

## 四、为什么 `< 文件` 这个写法很值钱

```bash
wc -l < app.log
```

和：

```bash
wc -l app.log
```

相比，它只输出数字，不带文件名。这对脚本判断和命令替换都更方便。

## 五、`-c`、`-m`、`-L` 分别适合什么

### 1. `-c`

```bash
wc -c app.log
```

看字节数。

### 2. `-m`

```bash
wc -m file.txt
```

看字符数。包含中文时，它和 `-c` 可能明显不同。

### 3. `-L`

```bash
wc -L access.log
```

看最长一行长度。排查异常超长行时很好用。

## 六、最常见的误区

### 1. 把 `wc -l` 当成“精确语义上的行数”

它本质上数的是换行符数量，最后一行没有换行符时，结果和肉眼理解可能会有差别。

### 2. 中文场景里混淆字节数和字符数

`-c` 和 `-m` 不要混着理解。

### 3. 直接拿带文件名的输出做脚本判断

如果你只想拿数字，`wc -l < file` 通常更干净。

## 七、结论

`wc` 很简单，但并不单薄。

行数统计、结果计数、字符和字节区分，这几件事掌握了，它在日志和脚本里就已经很实用了。
