# 权限身份与提权01：sudo 为什么会失败？排查时先看哪几项

`sudo` 失败时，很多人的第一反应是“这条命令是不是不能用”。可更常见的情况是，问题根本不在命令本身，而在：

- 你是谁
- 你有没有被授权
- 你现在拿到的是哪个环境
- 你到底是在提权，还是在切用户

如果只想先记 3 条：

- `sudo` 失败先看身份和授权，不要先怪命令
- 想知道自己能执行什么，先 `sudo -l`
- 修改授权时只用 `visudo`

## 一、`sudo` 失败时，通常先排哪几层

常见失败大致落在这几类：

- 当前用户根本没 sudo 权限
- 密码认证没过
- 只被允许执行部分命令
- 命令路径和授权规则不匹配
- 你以为自己拿到了目标环境，实际并没有

所以第一步不是反复重试，而是先确认：

```bash
whoami
id
sudo -l
```

## 二、先记住这几条命令

```bash
sudo command
sudo -l
sudo -u deploy whoami
sudo -i
sudo visudo
```

它们分别适合：

- `sudo command`：临时提权执行一条命令
- `sudo -l`：列出当前用户可执行的 sudo 命令
- `sudo -u user`：切到指定用户执行命令
- `sudo -i`：进入 root 登录式 shell
- `visudo`：安全编辑 sudoers

## 三、推荐的排查顺序

### 1. 先确认自己是谁

```bash
whoami
id
```

很多 sudo 问题不是命令错，而是你对当前用户身份判断错了。

### 2. 再看你到底被授权了什么

```bash
sudo -l
```

这一步通常能很快分清：

- 是完全没授权
- 还是只允许执行部分命令
- 还是需要密码但缓存已经失效

### 3. 再看你是想“提权”还是“切用户”

这两件事不是同一个场景：

- 临时执行管理员命令：`sudo command`
- 以指定用户身份测试环境：`sudo -u appuser command`
- 进入 root 登录式 shell：`sudo -i`

### 4. 如果要改授权，只用 `visudo`

```bash
sudo visudo
```

不要直接编辑 `/etc/sudoers`，因为语法错误可能直接把 sudo 整个搞坏。

## 四、最常见的 4 个问题

### 1. `user is not in the sudoers file`

这通常就是没授权。

需要由管理员把用户加入可用组，或写入明确的 sudoers 规则。

### 2. 密码一直不过

这更像认证问题，而不是授权问题。

### 3. 明明能 sudo，但某条命令就是不行

这通常说明：

- 只授权了部分命令
- 规则里写的是完整命令路径
- 你执行的命令路径和授权路径不一致

这时先查真实路径：

```bash
which systemctl
```

### 4. 进了 shell，但环境和预期不一样

像 `sudo -i`、`sudo -u`、`su -`，环境行为并不完全一样。

如果你是在排查“为什么这个用户跑起来不一样”，要特别留意这一点。

## 五、`sudo` 和 `su` 怎么分工

- 临时提权执行一条命令：`sudo`
- 需要长时间切到另一个用户环境：`su - user` 或 `sudo -iu user`
- 生产环境做最小授权和审计：优先 `sudo`

## 六、最常见的误区

### 1. 所有人都给 `sudo ALL`

这看似省事，后面通常会变成审计和权限边界的大坑。

### 2. 直接改 sudoers 文件

正确姿势是：

```bash
sudo visudo
```

### 3. 只看命令名，不看真实路径

授权规则常常依赖完整路径，这一点在 `systemctl`、脚本命令上尤其容易踩坑。

## 七、结论

`sudo` 失败时，先分清“我是谁、我有没有权限、我到底想做哪种提权动作”。

先 `whoami`、`id`、`sudo -l`，再去改规则或重试，通常会快得多。

