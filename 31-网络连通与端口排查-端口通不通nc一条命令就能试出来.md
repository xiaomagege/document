# 31｜网络连通与端口排查｜端口通不通，nc 一条命令就能试出来

很多“服务不通”的问题，最先需要确认的不是应用逻辑，而是最基础的一层：

**从这台机器到那个端口，到底能不能连上。**

`nc` 在这个环节特别好用，因为它足够直接，执行一条命令就能先把端口可达性试出来。

如果只想先记 3 条：

- 测 TCP 端口，先用 `nc -vz host port`
- 脚本和排查里记得加 `-w` 超时
- `nc` 说明端口层能不能到，HTTP 层细节还要交给 `curl`

## 一、什么时候先用 `nc`

这些场景都适合先用 `nc`：

- 怀疑某个端口不通
- 想确认本机能不能连到目标服务
- `ping` 是通的，但应用还是连不上
- 想快速试探 TCP 或 UDP 端口
- 想临时监听一个端口做简单验证

`nc` 最擅长回答的是：

**端口层面到底通不通？**

## 二、先记住这几条命令

```bash
nc -vz example.com 80
nc -vz -w 3 example.com 443
nc -vz 127.0.0.1 8080
nc -vzu 8.8.8.8 53
nc -l 8080
```

它们分别适合：

- `nc -vz host port`：测 TCP 端口
- `-w 3`：设置超时，避免卡住
- `127.0.0.1`：测本机服务
- `-u`：测 UDP 端口
- `-l`：临时监听端口

## 三、端口排查时最常见的 4 个用法

### 1. 测目标 TCP 端口

```bash
nc -vz example.com 80
```

如果成功，说明从当前机器到目标主机的这个 TCP 端口是可达的。

这一步最适合先排除：

- 路由不通
- 防火墙或安全组阻断
- 端口根本没开放

### 2. 测本机端口是否能连

```bash
nc -vz 127.0.0.1 8080
```

这适合确认本机服务端口是否真的可连。

再配合：

```bash
ss -lntp | grep ':8080'
```

基本就能把“有没有监听”和“能不能连到”这两件事一起确认掉。

### 3. 给探测加超时

```bash
nc -vz -w 3 example.com 443
```

线上排查和脚本里都建议带超时，不然网络异常时容易等很久。

### 4. 临时监听一个端口

```bash
nc -l 8080
```

这很适合做临时验证，比如确认另一台机器能不能主动连进来。

## 四、`ping` 通但 `nc` 不通，通常说明什么

这是非常常见的一类问题。

它通常说明：

- 主机层面能到
- 但具体端口层面不通

常见原因包括：

- 服务没监听
- 服务只监听本地地址
- 防火墙或安全组拦截
- 中间网络策略限制 TCP

这种情况下，排查顺序很清楚：

1. 服务端 `ss -lntp`
2. 客户端 `nc -vz host port`
3. 如果是 HTTP，再继续 `curl -v`

## 五、UDP 为什么要更谨慎

```bash
nc -vzu 8.8.8.8 53
```

UDP 没有像 TCP 那样明确的连接建立过程，所以结果经常没有 TCP 那么“硬”。

这意味着：

- `nc -u` 可以作为参考
- 但很多时候还得结合具体协议工具进一步验证

所以 UDP 场景里，`nc` 更像“初筛工具”，不是最终定论。

## 六、最常见的误区

### 1. `nc` 成功就以为应用一定正常

`nc` 成功只说明端口层能连上，不代表：

- HTTP 返回对
- TLS 正常
- 应用逻辑正常
- 鉴权没有问题

### 2. 端口探测不设超时

不加 `-w`，某些异常场景下会拖很久。

### 3. 拿 `nc` 去做长期文件传输方案

它可以临时传点东西，但正式场景还是更适合 `scp`、`rsync` 之类的工具。

## 七、结论

`nc` 是确认“端口到底通不通”的快速工具。

先用 `nc -vz` 验证端口层，再用 `ss` 看服务端监听，最后有需要再切到 `curl` 看应用层，整条排查路径会很顺。
