# 13｜日志查看与文本定位｜日志里找报错，grep 怎么查得又快又准

真正的线上排查，很少有人是在日志里“慢慢看”；更多时候是在一大堆输出里快速缩小范围。

`grep` 的价值就在这里。它不是单纯搜索文本，而是帮你把海量内容压缩成可判断的几行。

如果只想先记 3 条：

- 先用 `grep -n` 带出行号
- 看报错上下文，优先用 `grep -C 3`
- 实时跟日志时，`tail -f` 配 `grep --line-buffered` 更稳

## 一、什么时候先用 `grep`

这些场景都适合先用 `grep`：

- 日志里找报错
- 配置目录里找某个参数
- 想确认某个关键字是否出现过
- 想从杂乱输出里只保留少量关键行
- 想快速排除空行、注释行、无关噪音

`grep` 最擅长回答的是：

**我真正关心的内容在哪里？**

## 二、先记住这几条命令

```bash
grep -n ERROR app.log
grep -i timeout app.log
grep -n -C 3 ERROR app.log
grep -r \"server.port\" ./config
grep -Ev \"^#|^$\" app.conf
tail -f app.log | grep --line-buffered ERROR
```

它们分别适合：

- `-n`：显示行号
- `-i`：忽略大小写
- `-C 3`：看上下文
- `-r`：递归搜目录
- `-E`：扩展正则
- `-v`：反向过滤

## 三、排查里最常见的 5 种用法

### 1. 查日志里的报错

```bash
grep -n ERROR app.log
```

这条命令最适合先确认：

- 有没有明显报错
- 报错出现了几次
- 报错大概在什么位置

### 2. 看报错上下文

```bash
grep -n -C 3 ERROR app.log
```

比起只看一行命中结果，排查里更常需要知道：

- 报错前发生了什么
- 报错后还有没有连锁输出

`-C 3` 表示同时看前后各 3 行。也可以用：

- `-A 3`：只看后 3 行
- `-B 3`：只看前 3 行

### 3. 忽略大小写搜索

```bash
grep -i timeout app.log
```

适合关键字大小写不固定的场景，比如：

- `ERROR`
- `Error`
- `error`

### 4. 在整个目录里找配置项

```bash
grep -r \"server.port\" ./config
```

这类用法适合：

- 找配置项定义
- 确认某个开关到底在哪个文件里
- 比较多个环境目录的差异

### 5. 过滤掉注释和空行

```bash
grep -Ev \"^#|^$\" app.conf
```

这在看配置文件时特别顺手，因为很多时候你真正关心的是“生效配置”，不是注释说明。

## 四、实时日志里怎么配合 `tail`

实时排查时，经常会这样用：

```bash
tail -f app.log | grep --line-buffered ERROR
```

`tail` 负责持续输出新增日志，`grep` 负责只留下你关心的关键字。

如果不加 `--line-buffered`，有时会因为缓冲看起来“不够实时”。

## 五、`grep`、`find`、`awk` 怎么分工

- `grep`：找“有没有、在哪几行”
- `find`：找“哪些文件”
- `awk`：做更细的列处理、统计、格式化

一个常见顺序是：

1. 先 `find` 缩小文件范围
2. 再 `grep` 找关键字
3. 最后有需要再用 `awk` 提取字段

比如：

```bash
find /var/log -name \"*.log\" -exec grep -n Hikari {} +
```

## 六、最常见的误区

### 1. 搜到太多结果却不带行号

不带 `-n`，回头再定位会很累。排查时默认带上行号通常更省事。

### 2. 递归搜索时把二进制文件也扫进去

在目录很大时，直接 `grep -r` 可能会把无关文件也扫进去。更稳一点的是先缩小目录，或者配合文件后缀范围。

### 3. 用 `grep` 查进程名时把自己也匹配进去

像这样：

```bash
ps -ef | grep nginx
```

会把 `grep nginx` 自己也搜出来。更稳的是：

```bash
ps -ef | grep [n]ginx
```

## 七、结论

`grep` 最适合做“快速缩小范围”。

日志排查时，先 `grep -n` 定位，再 `grep -C` 看上下文；实时观察时，再和 `tail -f` 配合。这套组合已经足够应付大多数日志和配置排查场景。
