# 权限身份与提权02：执行命令前，whoami 和 id 应该怎么配合？

很多权限问题一开始就查偏了，不是因为命令不会用，而是因为连“现在到底是谁在执行命令”都没先确认。

`whoami` 和 `id` 看起来都很小，但它们真正值钱的地方是分工很清楚：

- `whoami` 回答“现在是谁在执行”
- `id` 回答“这个身份后面还带着哪些 UID、GID 和组关系”

如果只想先记 3 条：

- 想先确认当前执行身份，先看 `whoami`
- 想继续判断权限为什么不够，再看 `id`
- 权限问题别只盯用户名，还要看所属组和当前会话是否生效

## 一、什么时候先看 `whoami`，什么时候接着看 `id`

最常见的起手顺序其实很简单：

```bash
whoami
id
```

这两条放在一起，最适合这些场景：

- `sudo` 前后确认是不是已经变成 root
- `su` 切用户之后确认身份和环境
- 文件提示 `Permission denied`，想判断到底是身份不对，还是组不对
- 手工执行和自动执行结果不一样，怀疑运行用户不同

## 二、先记住这几条高频命令

```bash
whoami
sudo whoami
id
id -u
id -un
id -gn
id -Gn
```

它们分别适合：

- `whoami`：只看当前有效用户
- `sudo whoami`：确认 sudo 后是不是 root
- `id`：看完整身份背景
- `id -u`：只看 UID，脚本里判断 root 很常用
- `id -un`：只看用户名
- `id -gn`：看主组名称
- `id -Gn`：看所有组名称

## 三、权限排查时，这两个命令最常见的配合方式

### 1. 先确认当前到底是谁

```bash
whoami
```

这一步很适合快速排除误判：

- 你以为自己已经切到了 `deploy`，实际还在 `root`
- 你以为脚本是 root 执行，实际是普通用户在跑

### 2. 再确认这个身份背后的组关系

```bash
id
id -Gn
```

这一步能帮你判断：

- 当前用户属于哪些组
- 为什么用户名看着没问题，还是访问不了目录
- 当前会话里新加的组有没有真正生效

### 3. `sudo`、`su` 场景下连着看

```bash
whoami
sudo whoami
su - deploy
whoami
id
```

这组命令的价值在于，把“身份切换成功没有”和“切过去后组关系对不对”一次看清。

### 4. 配合文件权限一起判断

```bash
id
ls -l app.log
ls -ld /data/app
```

真正的权限判断，很少只看单条命令。更常见的是把这 3 组信息串起来：

- 当前是谁
- 当前属于哪些组
- 文件和目录的属主、属组、权限位是什么

## 四、这两个命令最适合帮你避免什么误判

### 1. 只看 `whoami`，却不知道自己根本不在目标组里

用户名对了，不代表权限就一定对。共享目录、部署目录、日志目录的问题，很多都卡在组关系上。

### 2. 组已经加了，但当前 shell 还没生效

改完用户组后，如果这次会话里 `id -Gn` 还看不到新组，往往需要重新登录再确认。

### 3. 看到 `Permission denied` 就直接 `chmod 777`

很多时候更合理的动作，是先判断该不该改组、改属主，还是只是当前执行身份不对。

## 五、最常见的误区

### 1. 把 `whoami` 当成完整权限判断

它只能回答“现在是谁”，不能回答“为什么这个人还没权限”。

### 2. 只看名字，不看 UID/GID

在跨主机同步、挂载、容器和 NFS 场景里，数字 ID 本身就可能是线索。

### 3. 切用户后不复核

`su`、`sudo`、定时任务、CI 任务这些场景，最怕默认自己“应该就是那个用户”。

## 六、结论

`whoami` 和 `id` 不应该互相替代，而应该连着用。

先用 `whoami` 确认“谁在执行”，再用 `id` 看清“这个身份后面有哪些组和权限背景”，很多原本会绕远路的权限问题，会快很多。

