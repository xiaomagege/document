# 24｜文本处理与批量处理｜只想快速切字段时，cut 比 awk 更省事吗

`cut` 最大的优点，就是够轻。

当你的需求只是“把这几列切出来”，而输入格式又很稳定时，它通常比 `awk` 更省事。但只要分隔符开始变得不稳定，或者你想加条件判断，它很快就会到边界。

如果只想先记 3 条：

- 分隔符稳定、只切字段时，`cut` 很顺手
- 连续空格分隔的输出，通常更适合 `awk`
- 复杂 CSV 不是 `cut` 的主场

## 一、什么情况下先用 `cut`

`cut` 最适合这些场景：

- `/etc/passwd` 这类固定分隔符文件
- 简单 CSV、TSV
- 想按字符位置截一段前缀

最常见的起手式是：

```bash
cut -d ':' -f 1 /etc/passwd
cut -d ',' -f 1,3 data.csv
cut -c 1-10 file.txt
```

## 二、先记住这几条命令

```bash
cut -d ':' -f 1 /etc/passwd
cut -d ':' -f 1,7 /etc/passwd
cut -d ',' -f 1,3 data.csv
cut -f 1,2 data.tsv
cut -c 1-19 app.log
```

它们分别适合：

- 提用户名
- 提多列
- 提 CSV 指定列
- 提 TSV 指定列
- 按字符位置切固定前缀

## 三、它和 `awk` 的边界在哪

### 1. `cut`

更适合：

- 固定分隔符
- 固定列位
- 简单截取

### 2. `awk`

更适合：

- 连续空白分隔
- 条件过滤
- 统计和计算

如果你脑子里只是“把第 1 列和第 7 列切出来”，优先想到 `cut` 没问题；如果你脑子里已经开始出现“第 3 列大于 80 才输出”，那往往就该切到 `awk` 了。

## 四、最常见的几种用法

### 1. 固定分隔符取列

```bash
cut -d ':' -f 1,7 /etc/passwd
```

### 2. 默认 Tab 分隔

```bash
cut -f 1,2 data.tsv
```

### 3. 按字符位置截取

```bash
cut -c 1-19 app.log
```

如果日志前 19 个字符固定是时间戳，这种写法很省事。

## 五、最常见的误区

### 1. 连续空格输出还想硬用 `cut`

这时字段位置很容易漂，`awk` 通常更稳。

### 2. 把复杂 CSV 交给 `cut`

一旦字段里本身有逗号、引号，`cut` 的边界就到了。

### 3. 该做条件判断时还在坚持 `cut`

它擅长截取，不擅长判断。

## 六、结论

`cut` 不是比 `awk` 强，而是更轻、更快进入状态。

当输入格式稳定、需求只是切列时，用它很舒服；一旦格式和逻辑开始复杂，就别勉强它了。
