# Linux磁盘与空间排查命令：fdisk命令详解

在 Linux 中新增数据盘、查看分区表或对磁盘进行分区时，经常会用到 `fdisk`。它是一个传统但仍然非常常用的磁盘分区工具。

需要特别注意的是，`fdisk` 不是普通查看命令。它可以修改磁盘分区表，错误操作可能导致数据丢失。生产环境中使用 `fdisk` 前，一定要确认目标磁盘、备份重要数据，并理解每一步操作的含义。

---

## 一、`fdisk` 是什么？

`fdisk` 是 Linux 下用于查看和管理磁盘分区表的命令行工具。

它主要用于：

- 查看磁盘和分区信息
- 给新磁盘创建分区
- 删除分区
- 修改分区类型
- 查看分区表结构
- 辅助完成数据盘初始化

常见设备名包括：

- `/dev/sda`、`/dev/sdb`
- `/dev/vda`、`/dev/vdb`
- `/dev/xvda`、`/dev/xvdb`
- `/dev/nvme0n1`

说明：

- `/dev/sdb` 表示整块磁盘
- `/dev/sdb1` 表示该磁盘上的第一个分区
- 不要把整盘设备和分区设备混淆

---

## 二、常见使用语法与重点参数

```bash
fdisk [选项] 磁盘设备
```

常用参数说明：

- `-l`：列出磁盘和分区信息
- `-u`：显示扇区单位
- `-s 分区`：显示分区大小，部分系统中可能不推荐使用
- `-b 大小`：指定扇区大小

交互模式中的常用命令：

- `m`：查看帮助
- `p`：打印分区表
- `n`：新建分区
- `d`：删除分区
- `t`：修改分区类型
- `w`：保存并写入分区表
- `q`：退出，不保存修改

常见示例：

```bash
fdisk -l
fdisk -l /dev/sdb
fdisk /dev/sdb
```

---

## 三、常用命令与使用场景

### 1. 查看所有磁盘和分区

```bash
fdisk -l
```

会列出系统识别到的磁盘、分区表类型、磁盘大小和分区信息。

排查新挂载磁盘时，可以配合：

```bash
lsblk
fdisk -l
```

一起确认设备名称和分区情况。

### 2. 查看指定磁盘分区表

```bash
fdisk -l /dev/sdb
```

只查看 `/dev/sdb` 这块磁盘的信息。

在生产环境中，建议先用这种方式确认目标磁盘，再进入交互模式。

### 3. 进入磁盘分区交互模式

```bash
fdisk /dev/sdb
```

进入后可以输入 `m` 查看帮助。

注意：进入交互模式本身不会立即修改磁盘，只有执行 `w` 写入时才会保存修改。

### 4. 打印当前分区表

在交互模式中输入：

```bash
p
```

可以查看当前磁盘已有分区。

在创建或删除分区前，建议先执行 `p`，确认当前状态。

### 5. 新建分区

典型流程如下：

```bash
fdisk /dev/sdb
```

进入后依次输入：

```bash
n
p
1


w
```

含义说明：

- `n`：创建新分区
- `p`：创建主分区
- `1`：分区编号为 1
- 两次回车：使用默认起始和结束扇区，即使用整块磁盘空间
- `w`：写入分区表并退出

完成后通常会得到 `/dev/sdb1` 这样的分区。

### 6. 删除分区

交互模式中输入：

```bash
d
```

然后选择要删除的分区编号。

删除分区是高风险操作，执行 `w` 之前要反复确认。如果误操作，可以输入：

```bash
q
```

退出且不保存修改。

### 7. 修改分区类型

交互模式中输入：

```bash
t
```

然后选择分区类型代码。

可以输入：

```bash
L
```

查看可用分区类型代码。

普通 Linux 文件系统分区通常使用默认类型即可。只有在创建 swap、LVM 等特殊分区时才需要修改类型。

### 8. 让系统重新读取分区表

分区写入后，有时系统不会立即识别新分区，可以执行：

```bash
partprobe /dev/sdb
```

或重新查看：

```bash
lsblk
```

如果仍未识别，可能需要确认磁盘是否正在被使用，或在合适窗口重启系统。

### 9. 新分区后的常见后续步骤

`fdisk` 只负责分区，不负责格式化和挂载。

新建分区后通常还需要：

```bash
mkfs.xfs /dev/sdb1
mkdir -p /data
mount /dev/sdb1 /data
df -Th
```

如果需要开机自动挂载，还要配置 `/etc/fstab`。

---

## 四、常见问题与建议

### 1. `fdisk` 会不会立刻修改磁盘？

进入 `fdisk /dev/sdb` 后，普通交互操作不会立即写入磁盘。

只有输入：

```bash
w
```

才会保存修改。

如果只是查看或发现操作不对，可以输入：

```bash
q
```

不保存退出。

### 2. 应该对 `/dev/sdb` 操作，还是对 `/dev/sdb1` 操作？

分区操作针对整块磁盘，例如：

```bash
fdisk /dev/sdb
```

格式化和挂载通常针对分区，例如：

```bash
mkfs.xfs /dev/sdb1
mount /dev/sdb1 /data
```

不要把这两个层级混淆。

### 3. `fdisk` 适合所有磁盘吗？

传统 `fdisk` 常用于 MBR 或 GPT 分区操作。对于超大磁盘、复杂 GPT 管理或更明确的分区需求，也可以考虑 `parted`。

日常云服务器新增普通数据盘时，`fdisk` 仍然非常常见。

### 4. 分区后为什么看不到 `/dev/sdb1`？

可以尝试：

```bash
partprobe /dev/sdb
lsblk
```

如果磁盘正在被使用，内核可能无法重新读取分区表，需要谨慎安排重启窗口。

### 5. 生产环境使用 `fdisk` 前要做什么？

建议至少确认以下信息：

```bash
lsblk -f
fdisk -l
df -Th
blkid
```

确认目标磁盘不是系统盘、不是已有数据盘、没有挂载到关键业务目录，再继续分区操作。

---

## 五、常用命令速查

```bash
fdisk -l
fdisk -l /dev/sdb
fdisk /dev/sdb
# 交互模式：
m
p
n
d
t
w
q
partprobe /dev/sdb
lsblk
mkfs.xfs /dev/sdb1
mkdir -p /data
mount /dev/sdb1 /data
df -Th
```

---

## 六、总结

`fdisk` 是 Linux 中常用的磁盘分区工具，适合查看分区表和为新磁盘创建分区。它的核心使用流程通常是：

```bash
lsblk
fdisk -l /dev/sdb
fdisk /dev/sdb
partprobe /dev/sdb
lsblk
```

必须记住：`fdisk` 可以修改分区表，属于高风险命令。生产环境中使用前一定先确认设备名和现有数据，操作中如果不确定就用 `q` 退出，不要随意执行 `w` 写入。