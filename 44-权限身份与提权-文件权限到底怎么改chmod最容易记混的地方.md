# 44｜权限、身份与提权｜文件权限到底怎么改？chmod 最容易记混的地方

Linux 权限问题里，`chmod` 是最常被用到、也最容易被用错的一条命令。

很多人一遇到 `Permission denied` 就想直接上 `777`，但这通常只是把问题暂时压过去，不是真正判断清楚“到底是谁需要什么权限”。

如果只想先记 3 条：

- 改权限前先看 `ls -l`
- 文件和目录的权限含义不完全一样，尤其是目录的 `x`
- 别把 `chmod 777` 当通用修复方案

## 一、什么情况下先用 `chmod`

`chmod` 最适合这些场景：

- 脚本不能执行，需要补执行权限
- 目录能看到却进不去，需要判断目录权限
- 配置文件、日志文件权限太宽或太窄，需要修正
- 部署目录权限看着不合理，需要最小化调整

第一步通常不是直接改，而是先看：

```bash
ls -l file
ls -ld dir
```

先看清当前权限，再决定怎么改。

## 二、先记住这几条命令

```bash
chmod +x deploy.sh
chmod 644 app.conf
chmod 755 /data/app
chmod o-r secret.txt
chmod -R 755 /data/app
```

它们分别适合：

- `+x`：给脚本补执行权限
- `644`：普通文件常用权限
- `755`：目录常用权限
- `o-r`：去掉其他用户读权限
- `-R`：递归修改目录

## 三、权限判断时，先分清文件和目录

### 1. 文件权限

文件上的 `r`、`w`、`x` 通常更直观：

- `r`：能读
- `w`：能改
- `x`：能执行

比如：

```bash
chmod +x deploy.sh
```

通常就是为了让脚本能执行。

### 2. 目录权限

目录上的含义不完全一样，尤其是 `x`：

- `r`：能列出目录内容
- `w`：能在目录里创建、删除、改名
- `x`：能进入目录

所以目录没有 `x` 时，哪怕你看得到名字，也可能进不去、也访问不到里面的文件。

这也是为什么很多“文件权限看着没问题却还是打不开”的问题，最后根因其实在父目录。

## 四、排查时最常用的几种改法

### 1. 给脚本加执行权限

```bash
chmod +x deploy.sh
```

如果你只想给所有者加执行权限，也可以：

```bash
chmod u+x deploy.sh
```

### 2. 普通文件通常用 `644`

```bash
chmod 644 app.conf
```

这适合大多数配置文件、文本文件。它表达的是：所有者可写，其他人只读。

### 3. 普通目录通常用 `755`

```bash
chmod 755 /data/app
```

这适合大多数需要“别人能进但不能写”的目录。

### 4. 敏感文件权限收紧

比如 SSH 私钥：

```bash
chmod 600 ~/.ssh/id_rsa
```

权限太宽时，SSH 本身就可能拒绝使用这个私钥。

### 5. 递归修改要格外慎重

```bash
chmod -R 755 /data/app
```

递归很方便，但也是最容易放大误操作的一种写法。很多时候更稳的做法是把目录和文件分开处理：

```bash
find /data/app -type d -exec chmod 755 {} +
find /data/app -type f -exec chmod 644 {} +
```

## 五、数字权限和符号权限怎么选

### 1. 数字权限

适合快速设置常见权限：

- `7 = rwx`
- `6 = rw-`
- `5 = r-x`
- `4 = r--`

所以：

- `644`：文件常见
- `755`：目录常见
- `600`：敏感文件常见

### 2. 符号权限

适合做局部调整：

```bash
chmod g+w shared.txt
chmod o-r secret.txt
chmod u+x run.sh
```

如果你只是想微调一位权限，符号方式通常更直观。

## 六、最常见的误区

### 1. 一上来就 `chmod 777`

这往往说明问题还没看清。真正应该先问的是：

- 当前是谁在访问
- 它属于哪些组
- 文件属主和属组是谁
- 目录权限是不是卡住了

### 2. 只看文件，不看父目录

目录缺少 `x` 时，文件本身权限再正常也没用。

### 3. 对整棵目录直接递归同一种权限

目录和文件的合理权限往往不同，递归一刀切很容易把可执行脚本、敏感配置或上传目录一起改坏。

## 七、结论

`chmod` 不是“权限不够时随手改一下”的命令，而是用来做最小化、针对性修正的。

先看 `ls -l`，分清文件和目录，再决定是改 `chmod`、改组，还是改属主，这样才不容易越改越乱。
