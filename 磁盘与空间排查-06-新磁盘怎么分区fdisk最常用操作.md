# 磁盘与空间排查06：新磁盘怎么分区？fdisk 最常用的操作有哪些

`fdisk` 不是普通查看命令，它能修改磁盘分区表。也正因为它有杀伤力，写这类文章时不能只罗列参数，必须把“什么时候能动、什么时候不能动”讲清楚。

如果只想先记 3 条：

- 操作前先用 `lsblk -f`、`df -Th`、`fdisk -l` 确认目标盘
- 进入 `fdisk` 不会立刻写盘，只有输入 `w` 才会保存修改
- 不确定时用 `q` 退出，不要硬着头皮执行 `w`

## 一、什么时候会用到 `fdisk`

`fdisk` 主要用于查看和修改磁盘分区表。

常见场景包括：

- 新增数据盘后创建分区
- 查看某块磁盘的分区表
- 删除测试盘上的旧分区
- 修改分区类型
- 辅助完成数据盘初始化

它不负责格式化，也不负责挂载。

一块新盘从可见到可用，通常要经历：

```text
识别磁盘 -> 分区 -> 格式化 -> 创建挂载点 -> 挂载 -> 配置开机自动挂载
```

`fdisk` 只负责其中的“分区”。

## 二、操作前先确认目标设备

先看设备关系：

```bash
lsblk -f
df -Th
fdisk -l
```

重点确认：

- 哪个是系统盘
- 哪个是新增数据盘
- 目标盘是否已经有分区
- 目标盘是否已经挂载
- 目标盘大小是否符合预期

常见设备名包括：

- `/dev/sda`、`/dev/sdb`
- `/dev/vda`、`/dev/vdb`
- `/dev/xvda`、`/dev/xvdb`
- `/dev/nvme0n1`

要分清：

- `/dev/sdb`：整块磁盘
- `/dev/sdb1`：这块磁盘上的第一个分区

## 三、最常见的新盘分区流程

假设新盘是 `/dev/sdb`。

进入交互模式：

```bash
fdisk /dev/sdb
```

先打印当前分区表：

```text
p
```

创建新分区：

```text
n
p
1


```

这里两次回车表示使用默认起始和结束扇区，也就是通常使用整块磁盘空间。

最后写入：

```text
w
```

如果任何一步不确定，退出且不保存：

```text
q
```

## 四、写入后还要做什么

分区完成后，通常要让系统重新读取分区表：

```bash
partprobe /dev/sdb
lsblk -f
```

然后才是格式化和挂载：

```bash
mkfs.xfs /dev/sdb1
mkdir -p /data
mount /dev/sdb1 /data
df -Th
```

如果需要开机自动挂载，再配置 `/etc/fstab`。这一步建议用 UUID，而不是直接写 `/dev/sdb1`。

## 五、交互模式里常用按键

```text
m  查看帮助
p  打印分区表
n  新建分区
d  删除分区
t  修改分区类型
L  查看分区类型代码
w  写入分区表并退出
q  不保存退出
```

真正危险的是 `w`。

在 `w` 之前，很多交互动作还只是待写入状态；执行 `w` 后，分区表才会被修改。

## 六、最常见的误区

### 1. 对分区设备执行分区操作

分区操作通常针对整块磁盘：

```bash
fdisk /dev/sdb
```

格式化和挂载通常针对分区：

```bash
mkfs.xfs /dev/sdb1
mount /dev/sdb1 /data
```

### 2. 没确认就删除分区

`d` 删除分区后，如果再执行 `w`，风险就很高。

生产环境里删除分区前，必须确认：

- 是否有业务数据
- 是否已备份
- 是否仍在挂载
- 是否有进程正在使用

### 3. 把 `fdisk` 当成扩容万能工具

复杂 GPT、超大磁盘、LVM、云盘在线扩容等场景，可能需要 `parted`、`growpart`、`xfs_growfs`、`resize2fs` 等工具配合。

`fdisk` 是分区工具，不是完整扩容方案。

## 七、结论

`fdisk` 适合给新磁盘创建分区，也适合查看分区表，但它属于高风险命令。

生产环境里记住一个顺序：先 `lsblk -f` 看清设备，再 `fdisk -l` 确认目标盘，最后才进入 `fdisk /dev/xxx`。不确定时用 `q` 退出，这比事后恢复数据轻松得多。

