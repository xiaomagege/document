# 13｜日志查看与文本定位｜日志排查时，tail -f 怎么看得更快

日志排查里最常见的动作不是“把整份日志看完”，而是尽快盯住最新变化。

`tail` 最适合做这件事，尤其是在你已经知道日志文件位置，但只想看最后几行、实时跟踪新输出、或者快速锁定最新报错的时候。

如果只想先记 3 条：

- 实时盯日志，先用 `tail -f`
- 日志会轮转时，优先用 `tail -F`
- 只想看最新错误附近内容时，先 `tail` 再配 `grep`

## 一、什么时候先用 `tail`

这些场景都应该先想到 `tail`：

- 服务刚启动，想盯最新日志
- 接口刚报错，想看最后几十行发生了什么
- 排查线上问题，只想看“最新变化”
- 日志很大，不适合从头打开
- 日志轮转后，还想继续跟踪新文件

`tail` 最擅长回答的是：

**最新发生了什么？**

## 二、先记住这几条命令

```bash
tail -n 50 app.log
tail -f app.log
tail -F app.log
tail -n 200 app.log | grep ERROR
tail -f app.log | grep --line-buffered ERROR
```

它们分别适合：

- `tail -n 50`：看最后 50 行
- `tail -f`：持续跟踪文件追加内容
- `tail -F`：适合会轮转的日志
- `tail ... | grep`：只看最后一段里的关键信息
- `grep --line-buffered`：边跟边筛，不会卡住输出

## 三、排查里最常见的 4 种用法

### 1. 先看最后几十行

```bash
tail -n 50 app.log
```

这一步最适合快速判断：

- 最近有没有异常栈
- 报错是不是刚发生
- 启动、重启、回滚后最新状态是什么

### 2. 实时跟踪新日志

```bash
tail -f app.log
```

适合：

- 盯服务启动过程
- 观察接口请求后的即时日志
- 和业务同事同步复现问题时边看边判断

### 3. 日志轮转场景用 `-F`

```bash
tail -F app.log
```

`-f` 是跟当前文件描述符，`-F` 更接近“跟这个名字代表的文件”。

如果日志会被切割、重建、轮转，`-F` 通常更稳。像 `logrotate` 场景里，用 `-f` 容易在旧文件上傻等。

### 4. 边跟边筛选错误

```bash
tail -f app.log | grep --line-buffered ERROR
```

适合：

- 只想看报错
- 不想被正常访问日志淹没
- 只想观察某个关键字是否反复出现

如果要看上下文，可以先不实时跟，改成：

```bash
tail -n 200 app.log | grep -n -C 3 ERROR
```

## 四、`tail -f` 和 `tail -F` 到底怎么选

你可以直接这样记：

- 文件稳定存在、不会轮转：`tail -f`
- 日志会被切割、重建、轮转：`tail -F`

线上应用日志大多数情况下都更适合 `-F`，因为真正排障时，最烦的不是命令不会，而是你以为自己在看新日志，实际盯的还是旧文件。

## 五、`tail` 和 `head`、`less` 怎么分工

- `tail`：看最后一段，盯最新变化
- `head`：看开头几行
- `less`：需要来回翻、搜索、跳转时更合适

一个很实用的顺序是：

1. `tail -n 50 app.log`
2. 如果还要持续观察，用 `tail -F app.log`
3. 如果需要往前翻很多行或反复搜索，切到 `less`

## 六、最常见的误区

### 1. 大日志一上来就 `cat`

大日志更适合 `tail` 或 `less`。直接 `cat` 不仅慢，还会把终端刷满。

### 2. 实时筛选时没加 `--line-buffered`

像这样：

```bash
tail -f app.log | grep ERROR
```

有时会因为缓冲导致输出不够及时。实时排查时更稳的是：

```bash
tail -f app.log | grep --line-buffered ERROR
```

### 3. 日志轮转后还在用 `-f`

日志文件被切走以后，`-f` 可能还盯着旧文件描述符。遇到轮转场景，优先改用 `-F`。

## 七、结论

`tail` 解决的不是“怎么看文件”，而是“怎么最快看到最新变化”。

日志排查时，先 `tail -n` 看最后一段，再按需要切到 `tail -f` 或 `tail -F`，已经能覆盖绝大多数线上观察场景。
