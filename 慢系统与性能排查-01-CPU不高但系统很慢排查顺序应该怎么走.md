# 慢系统与性能排查01：CPU不高但系统很慢，排查顺序应该怎么走？

系统“变慢”是 Linux 运维里最常见、也最容易误判的问题之一。

很多时候第一反应是“CPU 不够了”，但真实情况往往不是这样。线上更常见的是：

- CPU 不高，但系统响应很迟钝
- 接口偶发超时，业务日志没有明显报错
- 机器还能登录，但命令执行明显变慢
- 负载不低，可一时又看不出是哪个进程有问题

这类问题最怕的不是命令不会，而是排查顺序错了。比起一上来就猜根因，更有效的做法是先判断机器到底是在忙，还是在等。

如果只想先记 3 条：

- 先用 `top -c` 做分类判断，不要一上来就盯某个进程
- `top` 负责看全局，`ps` 负责精确定位，`iostat` 负责给出 IO 证据
- 线上先止损，再谈根因优化

## 一、先用一张判断表把方向分出来

排查时先回答一个问题：

**机器到底是在忙，还是在等？**

你可以先用下面这张速查表建立方向感：

| 现象 | 第一判断方向 | 优先命令 |
| --- | --- | --- |
| `id` 很低，热点进程 CPU 很高 | CPU 紧张 | `top -c`、`ps aux --sort=-%cpu` |
| CPU 不高，但 `wa` 很高 | IO 等待 | `top -c`、`iostat -y -x 1 3` |
| `Swap` 持续增长，内存回收明显 | 内存压力 | `top -c`、`ps aux --sort=-%mem` |
| `D` 状态进程持续出现 | 存储或 IO 卡顿 | `top -c`、`ps`、`iostat` |
| 负载高，但 CPU 并不高 | 可能是 IO、锁等待或进程堆积 | `top -c`、`ps`、`iostat` |

这一步的目标不是立刻定位根因，而是先把范围缩小。

## 二、推荐排查顺序

### 1. 先用 `top -c` 看全局

第一步建议直接执行：

```bash
top -c
```

`-c` 的价值在于能直接看到完整命令行，排查时比只看进程名有效得多。

重点先看 5 个地方：

- `load average`：负载是在上升、回落，还是长期过高
- CPU 行里的 `id`、`wa`、`sy`：机器是在忙着算，还是在等
- `Tasks` 和进程状态：有没有持续的 `D` 或大量异常状态
- 内存和 `Swap`：是不是已经开始拿性能换空间
- 进程列表：有没有明显的 CPU 或内存热点

这一步的目标是把问题先分到 CPU、内存、IO 这几个主方向里。

### 2. 再用 `ps` 做精确定位

`top` 适合总览，`ps` 适合精准筛选。

最常用的几条命令如下：

```bash
ps aux --sort=-%cpu | head -10
ps aux --sort=-%mem | head -10
ps -eo pid,ppid,user,%cpu,%mem,stat,cmd --sort=-%cpu | head
```

这一步主要回答 3 个问题：

1. 是谁最吃 CPU
2. 是谁最占内存
3. 有没有异常状态进程，比如 `D` 或 `Z`

如果 `top` 和 `ps` 里都反复出现 `D` 状态进程，就不要再停留在“哪个进程 CPU 高”这个层面了，应该直接切到 IO 侧。

### 3. 如果怀疑 IO，立即看 `iostat -y -x 1 3`

当你看到下面任意一种情况时，就该切到 `iostat`：

- `wa` 明显偏高
- CPU 不高，但系统仍然很慢
- 存在持续的 `D` 状态进程
- 负载高，但热点 CPU 进程并不明显

建议直接执行：

```bash
iostat -y -x 1 3
```

这一步优先看 4 个指标：

- `%iowait`
- `%util`
- `await`
- `r_await` / `w_await`

经验上可以这样记：

- `%iowait`：CPU 有多少时间在等 IO
- `%util`：磁盘设备忙不忙
- `await`：一次 IO 平均要等多久
- `r_await` / `w_await`：到底是读慢还是写慢

### 4. 如果已经确认是 IO 问题，再去找具体进程

这一步可以继续用：

```bash
iotop -o -d 1
```

重点看：

- 哪个进程正在持续读写
- IO% 是否明显偏高
- 是读压力更大，还是写压力更大

如果你看到写路径明显更重，常见方向包括：

- 日志刷盘过于频繁
- 批量落盘或导出任务叠加
- 数据库 checkpoint、flush 或 fsync 压力

### 5. 先止损，再谈根因优化

线上排障里，先恢复可用通常比原地深挖更重要。

只要证据链已经能说明“问题大概率在 IO、CPU 或内存某一侧”，就可以先做低风险止损动作，比如：

- 降低非关键日志级别
- 暂停高峰期的大批量后台任务
- 限流明显的写入流量
- 暂时下线异常实例

## 三、三种最常见的慢系统场景

### 1. CPU 真正打满了

常见特征：

- `id` 很低
- 热点进程 `%CPU` 很高
- `wa` 不高
- `ps aux --sort=-%cpu` 很快能看出热点进程

这类场景更像“机器真的在忙着算”。

### 2. CPU 不高，但系统依然很慢

这是最容易误判的一类。

常见特征：

- `id` 还有空闲
- `wa` 偏高
- 进程里出现 `D` 状态
- `iostat` 里 `%util` 和 `await` 异常

这类场景通常不是 CPU 问题，而是：

- 磁盘打满
- 存储响应变慢
- 写入放大
- 读写排队

### 3. 内存压力导致系统变慢

常见特征：

- `Swap` 使用持续增长
- 大内存进程明显上升
- 系统开始频繁回收内存
- 响应变慢，但热点 CPU 不一定明显

这类场景下，先看：

```bash
ps aux --sort=-%mem | head -10
```

然后结合 `top` 里的 `RES`、`%MEM` 和 `Swap` 一起判断。

## 四、一个可复用的现场排查 checklist

以后你再遇到“系统变慢”，可以直接按这个顺序走：

1. `top -c`
2. 看 `load average`
3. 看 `id`、`wa`、`sy`、`Swap`
4. 看是否存在持续的 `D` 状态进程
5. `ps aux --sort=-%cpu | head -10`
6. `ps aux --sort=-%mem | head -10`
7. 如果怀疑 IO，立刻执行 `iostat -y -x 1 3`
8. 如果已经确认 IO，再用 `iotop -o -d 1` 找具体进程
9. 先止损，再做根因复盘

如果你只想记一条最重要的经验，那就是：

**系统慢，不要先问“哪个进程 CPU 高”，先问“机器是在忙，还是在等”。**

## 五、排查时最常见的误区

### 1. 只看 CPU，不看 `wa`

CPU 不高不代表系统没问题，它也可能是在等磁盘、等存储、等锁。

### 2. 看到 `free` 很低就断定内存不够

Linux 会把空闲内存拿去做缓存，所以 `free` 低本身不能直接说明异常。

更该看的是 `Swap` 是否活跃，以及大内存进程是否明显增长。

### 3. 只看单次输出，不看连续变化

无论是 `top` 还是 `iostat`，都不要只看一眼就下结论。

至少连续观察几轮，才能区分是瞬时抖动还是持续瓶颈。

### 4. 一上来就深挖根因，不先止损

线上排障里，“恢复可用”通常优先级更高。

如果已经确认瓶颈方向，先做低风险止损动作，往往比继续原地分析更有价值。

## 六、结论

系统变慢时，最怕的不是命令不会，而是排查顺序错了。

先用 `top -c` 做分类判断，再用 `ps` 精确定位，必要时用 `iostat` 深入到设备层，这条路径已经能覆盖大多数常见的慢系统场景。

