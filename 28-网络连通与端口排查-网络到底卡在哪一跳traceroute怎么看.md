# 28｜网络连通与端口排查｜网络到底卡在哪一跳？traceroute 怎么看

当你已经知道“访问有问题”，但还不知道问题出在本机、出口、运营商链路，还是目标侧时，`traceroute` 的价值就出来了。

它不是告诉你“服务有没有好”，而是帮你看“路径大概走到了哪儿，在哪一段开始变慢、丢包或者没回应”。

如果只想先记 3 条：

- `traceroute -n` 是最常用起手式
- 某一跳出现 `* * *` 不一定就是故障点
- 真正有价值的是看异常是否从某一跳开始持续影响后续所有跳

## 一、什么情况下先用 `traceroute`

`traceroute` 最适合这些场景：

- `ping` 结果看着怪，但你还不知道问题卡在哪一段
- 跨机房、跨运营商、跨地域访问突然变慢
- 域名能解析，端口也偶尔通，但链路表现不稳定

最常用的第一条通常就是：

```bash
traceroute -n 目标
```

这里的 `-n` 很值得养成习惯，因为它可以少做反向解析，输出更快。

## 二、先记住这几条命令

```bash
traceroute -n example.com
traceroute -I example.com
traceroute -T -p 443 example.com
traceroute -n -m 20 example.com
traceroute -n -q 1 example.com
```

它们分别适合：

- `-n`：排查时最常用
- `-I`：用 ICMP 探测，适合和 `ping` 对照
- `-T -p 443`：更贴近真实 TCP 业务流量
- `-m 20`：限制最大跳数
- `-q 1`：只发 1 个探测包，结果更快

## 三、怎么看结果才不容易误判

### 1. 先看路径有没有走出去

如果前几跳就开始异常，问题更可能在：

- 本机网络
- 本地出口
- 本机到网关这段

### 2. 再看异常是不是持续传导

真正更值得重视的是这种情况：

- 从某一跳开始延迟突然明显升高
- 后续所有跳都一起变高
- 最终目标也延迟高或直接不可达

这通常比“单独某一跳偶尔高一下”更有意义。

### 3. `* * *` 不一定等于故障

很多中间设备会：

- 不回应探测包
- 限速探测响应
- 只转发真实业务流量，不理会 traceroute

所以如果中间某一跳是 `* * *`，但后面还能继续走、目标也正常，那它通常只是“不回复”，不一定是“断了”。

## 四、什么时候该换探测方式

### 1. 默认结果不完整时，试 `-I`

```bash
traceroute -I 目标
```

这适合和 `ping` 的 ICMP 结果做对照。

### 2. 排查 HTTPS 或特定业务端口时，试 `-T`

```bash
traceroute -T -p 443 目标
```

这往往比默认方式更贴近真实业务链路，尤其是你要排查的本来就是 TCP 端口访问问题。

## 五、推荐的联动排查顺序

`traceroute` 很少单独用，通常是夹在中间的：

```bash
ping -c 4 目标
traceroute -n 目标
nc -vz 目标 端口
curl -v URL
```

它负责回答的是：

- 路是不是大体通
- 路走到哪儿开始不对

端口和应用层还是要交给 `nc`、`curl` 继续判断。

## 六、最常见的误区

### 1. 盯着单个中间节点的高延迟不放

很多路由器只是对探测包回复得慢，但真实转发并不慢。要看的是异常有没有继续传到后面。

### 2. 看到 `* * *` 就判定链路断了

如果后续还能继续显示，说明这跳不回你，不代表它不转发业务流量。

### 3. 只用默认模式，不换探测方式

不同网络对 ICMP、UDP、TCP 探测的处理并不一样。结果怪异时，换 `-I` 或 `-T` 往往很有帮助。

## 七、结论

`traceroute` 最有价值的地方，不是给你一张“完整地图”，而是帮你判断异常大概从哪里开始放大。

看它时不要盯单点，要看趋势；不要只看某一跳，要看它后面是不是整段一起异常。
