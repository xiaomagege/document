# 日志查看与文本定位04：线上临时改配置和查日志，vim 最常用的操作有哪些

很多人一提 `vim` 就想到“很难学”，但真正线上会用到的，往往只是少量高频动作。

你不需要把 `vim` 学成一门语言，先把“打开、查找、改一处、保存退出”这条链路练熟，已经够解决大多数临时场景。

如果只想先记 3 条：

- 先记模式切换：`i` 编辑，`Esc` 退出编辑
- 先记保存退出：`:wq`、`:q!`
- 查日志和改配置时，最常用的是 `/关键字` 和 `:%s/旧/新/g`

## 一、什么时候必须会一点 `vim`

这些场景里，`vim` 基本躲不开：

- SSH 上去临时改配置
- 快速打开日志搜索错误
- 修改一两个参数后立刻保存
- 没有图形界面，也不方便安装其他编辑器

这篇不讲完整教程，只保留线上最常用的一小套动作。

## 二、先记住这几个核心操作

模式切换：

- `i`：进入插入模式
- `Esc`：回到普通模式

保存退出：

- `:w`：保存
- `:q`：退出
- `:wq`：保存并退出
- `:q!`：不保存强制退出

查找：

- `/ERROR`：向下搜索
- `n`：继续下一个
- `N`：返回上一个

替换：

```text
:%s/old/new/g
```

## 三、线上最常见的 4 个场景

### 1. 快速改一处配置再退出

打开配置：

```bash
vim nginx.conf
```

常见流程：

1. `/worker_processes` 找到目标行
2. `i` 进入编辑
3. 改完按 `Esc`
4. `:wq` 保存退出

这已经覆盖了大量临时改配置场景。

### 2. 查日志里某个关键字

```bash
vim app.log
```

进入后：

```text
/ERROR
```

然后用 `n`、`N` 在命中之间切换。

如果日志很大，纯浏览时 `less` 更舒服；但如果你想顺手做点标记、删除或替换，`vim` 会更方便。

### 3. 批量替换某个配置项

```text
:%s/INFO/WARN/g
```

这适合：

- 批量替换日志级别
- 改多个相同域名、路径、参数值

操作前先确认替换范围，别在大文件里直接盲改。

### 4. 打开大日志后快速跳位置

常见高频动作：

- `gg`：跳到开头
- `G`：跳到结尾
- `:100`：跳到第 100 行

这些动作在处理长日志或大配置时会非常省时间。

## 四、只保留最值得记的按键

如果你不想记一长串，先记这组：

- `i`
- `Esc`
- `:wq`
- `:q!`
- `/关键字`
- `n`
- `N`
- `gg`
- `G`
- `:%s/old/new/g`

把这组用熟，线上临时改配置和查日志基本就够用了。

## 五、最常见的误区

### 1. 还在插入模式就想输入命令

很多“怎么退不出去”的问题，本质上都是还没按 `Esc`。

### 2. 大日志里把 `vim` 当纯阅读器

如果只是看、不改，`less` 通常更轻松。`vim` 更适合“看完还要顺手改”。

### 3. 批量替换前不确认范围

`:%s/.../.../g` 很强，但也很容易改多。生产环境里，先定位、先确认，再替换。

## 六、结论

线上用 `vim`，重点不是全面，而是熟练。

先把“打开、搜索、改一处、保存退出”这条最短路径练熟，再慢慢补其他动作，已经足够让你在临时配置和日志排查里不慌。

