# 20｜文本处理与批量处理｜批量替换文本时，sed 最常用的操作有哪些

`sed` 在运维里最常见的使用场景，不是“学一门语言”，而是“我想把这段文本批量改掉”。

它很强，但也很容易在 `-i` 上翻车。因为一旦原地修改写错，配置和数据就已经被改掉了。所以 `sed` 最该建立的是修改边界感，而不是参数收集癖。

如果只想先记 3 条：

- 先不加 `-i` 预览结果，再决定是否落盘
- 批量替换最常用的是 `s/旧/新/g`
- 替换路径时，分隔符不一定非要用 `/`

## 一、什么情况下先用 `sed`

`sed` 最适合这些场景：

- 批量替换配置项
- 删除空行或调试行
- 打印指定范围的文本
- 对一批文件做同样的文本修改

最常见的起手式就是：

```bash
sed 's/foo/bar/g' file.txt
sed -n '5,10p' file.txt
sed '/DEBUG/d' app.log
```

## 二、先记住这几条命令

```bash
sed 's/foo/bar/g' file.txt
sed -i 's/foo/bar/g' file.txt
sed -n '5,10p' file.txt
sed '/^$/d' file.txt
sed 's#/data/old#/data/new#g' app.conf
```

它们分别适合：

- 预览替换结果
- 原地修改
- 打印指定行范围
- 删除空行
- 处理带 `/` 的路径替换

## 三、批量改文本时，推荐先怎么做

### 1. 先预览，不直接落盘

```bash
sed 's/old/new/g' app.conf
```

先看输出是不是你想要的，再决定要不要加 `-i`。

### 2. 真要原地改时，先备份

```bash
cp -a app.conf app.conf.bak.$(date +%F)
sed -i 's/old/new/g' app.conf
```

这对配置文件尤其重要。

### 3. 改路径时换个分隔符

```bash
sed 's#/data/old#/data/new#g' app.conf
```

如果替换文本里本来就很多 `/`，继续硬用 `/` 只会让表达式越来越难读。

## 四、最常用的几类操作

### 1. 替换

```bash
sed 's/foo/bar/g' file.txt
```

这是最常用的一类。

### 2. 打印指定范围

```bash
sed -n '5p' file.txt
sed -n '5,10p' file.txt
```

适合快速看固定行段。

### 3. 删除

```bash
sed '/^$/d' file.txt
sed '/DEBUG/d' app.log
```

删空行、删调试行都很常见。

### 4. 替换整行

```bash
sed '/^port=/c\port=8080' app.conf
```

当你不想只替换局部，而是整行直接换掉时，这种写法更清楚。

## 五、`sed`、`grep`、`awk` 怎么分工

### 1. `sed`

更适合：

- 按规则替换
- 删除或打印某些行
- 做流式文本编辑

### 2. `grep`

更适合：

- 快速筛关键字

### 3. `awk`

更适合：

- 按列提取
- 条件过滤
- 统计和计算

如果你脑子里想的是“改文本”，优先想到 `sed`；想的是“切列和算数”，优先想到 `awk`。

## 六、最常见的误区

### 1. 上来就 `sed -i`

这是最容易出问题的一步。先预览，再落盘，通常更稳。

### 2. 用很宽泛的替换条件

全局替换写得太宽，最容易把不该改的地方也带进去。

### 3. 用 `sed` 做复杂结构化处理

能做，但如果逻辑已经明显偏向“字段和条件”，往往该考虑 `awk` 了。

## 七、结论

`sed` 最值钱的地方，是它能把批量文本修改做得很快。

但它真正的使用门槛不在语法，而在风险控制。先预览、再备份、最后再 `-i`，这套顺序比背更多例子更重要。
