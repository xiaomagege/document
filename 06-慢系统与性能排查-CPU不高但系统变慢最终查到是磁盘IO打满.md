# 06｜慢系统与性能排查｜CPU不高但系统变慢，最终查到是磁盘 IO 打满

这次故障最容易让人误判的地方在于：系统已经明显变慢，但 CPU 并没有表现出“打满”的样子。

如果只看 CPU 百分比，很容易得到一个错误结论：机器还有空闲，问题应该不大。真正把问题找出来，靠的是把“系统慢”一步步拆成可验证的证据链。

## 一、背景

某次现场反馈“系统整体变慢、接口响应明显延迟”，但业务进程没有大面积报错，机器也还能够登录。

这类问题如果一上来就猜业务代码，排查很容易绕远路。更稳妥的办法，是先从系统层把瓶颈方向定出来。

## 二、故障现象

现场最明显的表现有这些：

- 页面加载慢，接口超时增多
- 命令执行有明显停顿感
- CPU 使用率看起来不夸张
- 整机响应却明显下降

这类现象通常不是“单纯 CPU 不够”，更像是某种等待在拖慢整机。

## 三、排查过程

### 1. 先看 `top -c`：CPU 不高，但 `wa` 偏高

第一步执行：

```bash
top -c
```

重点不是看某个进程有多高，而是看全局信号：

- `us` 不高：不是纯计算型瓶颈
- `id` 仍然有空闲：CPU 没有真正打满
- `wa` 明显抬高：存在 IO 等待信号

这一步给出的第一个结论是：

**系统慢，不是因为“机器算不过来”，而是因为“CPU 在等 IO”。**

### 2. 再看进程状态：出现 `D` 态进程

接着在 `top -c` 和 `ps` 里关注进程状态：

```bash
ps -eo pid,ppid,user,%cpu,%mem,stat,cmd --sort=-%cpu | head
```

现场可以看到进程列表里有 `D` 状态。

`D` 代表不可中断等待，常见于磁盘或存储等待。这一步的意义不是定位具体业务，而是进一步坐实“问题方向在 IO”。

### 3. 用 `iostat -y -x 1 3` 做磁盘级确认

执行命令：

```bash
iostat -y -x 1 3
```

现场关键指标如下：

```text
avg-cpu:  %user  %nice %system %iowait  %steal %idle
           6.12    0.00    2.31    18.45    0.00   73.12

Device            r/s   w/s   rkB/s   wkB/s  rrqm/s wrqm/s await  r_await  w_await  svctm  %util
sda              120.3  85.6   5200    3400     2.1   1.3  45.6     38.2     56.4     3.2   98.7
```

解读这组数据时，最关键的是：

- `%iowait=18.45%`：CPU 大量时间在等待 IO
- `%util=98.7%`：磁盘繁忙度接近极限
- `await=45.6ms`：单次 IO 等待显著偏高
- `w_await > r_await`：写路径比读路径更慢

到这里，证据已经很完整了：系统慢的主因是磁盘 IO 瓶颈。

### 4. 用 `iotop -o -d 1` 找“罪魁祸首进程”

继续执行：

```bash
iotop -o -d 1
```

这一步只观察正在发生 IO 的进程，最终定位到高 IO% 的业务进程，且写入占比明显偏高。

结合业务行为，可以进一步锁定为高频日志刷盘叠加突发写入导致。

## 四、为什么最终判断为写入瓶颈

真正让结论站得住的，不是某一个命令，而是下面这组组合证据：

1. `top` 里 `wa` 偏高，CPU 并不忙
2. 进程状态里出现 `D` 态
3. `iostat` 里 `%util` 接近 100%，`await` 明显偏高
4. `w_await` 高于 `r_await`
5. `iotop` 看到写入型业务进程持续活跃

这组信息叠在一起时，问题已经不只是“磁盘忙”，而是“写入路径正在把磁盘拖到排队”。

## 五、处理动作

### 1. 紧急止损

- 降低非关键日志级别，减少同步刷盘频率
- 将部分批量写改为异步或批处理
- 避开高峰时段执行重 IO 后台任务

### 2. 长期优化

- 日志与数据盘分离，避免业务读写和日志争抢同一块盘
- 为 `%iowait`、`%util`、`await`、`D` 态进程数建立告警基线
- 对核心写路径做限流和削峰，避免瞬时突刺把磁盘打满

## 六、结果回看

优化后现场观察到：

- `%iowait` 明显回落
- `await` 降低，`%util` 不再长期高位
- 接口响应恢复，超时率下降

这说明前面的判断方向是对的，止损动作也确实命中了主瓶颈。

## 七、复盘沉淀

这次故障最后沉淀成了一条很实用的排查顺序：

1. `top -c`：先判断是 CPU 忙还是 IO 等待
2. 看进程状态：是否存在持续的 `D` 态
3. `iostat -y -x 1 3`：用 `%iowait`、`%util`、`await` 定位设备层瓶颈
4. `iotop -o -d 1`：落到具体进程，关联业务动作
5. 先止损，再做根因优化

这次故障再次说明，系统“慢”不等于 CPU“高”。当 CPU 不高但服务变慢时，优先检查 IO 等待，往往能更快命中根因。
