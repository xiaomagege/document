# linux常用命令：pwd命令详解

在 Linux 日常使用和服务器运维中，`pwd` 是最基础的路径确认命令之一。它的作用很简单：显示当前所在目录。但在真实工作中，很多误删、误移动、找错日志、改错配置的问题，往往都和“没有确认当前目录”有关。

因此，`pwd` 虽然简单，但非常重要。尤其是在执行删除、移动、批量修改、部署脚本等操作前，先看一眼当前路径，是一个很值得养成的习惯。

---

## 一、`pwd` 是什么？

`pwd` 是 print working directory 的缩写，用于显示当前 shell 会话所在的工作目录。

它主要用于：

- 确认当前所在路径
- 判断当前目录是否符合预期
- 配合 `cd` 检查目录切换结果
- 在脚本中获取当前工作目录
- 排查软链接目录下的真实物理路径

说明：

- `pwd` 输出的是当前工作目录路径
- 日常操作中经常和 `cd`、`ls`、`find`、`rm`、`cp`、`mv` 配合使用
- 在高风险操作前，先执行 `pwd` 可以减少路径误判

---

## 二、常见使用语法与重点参数

```bash
pwd [选项]
```

常见参数说明：

- `-L`：显示逻辑路径，保留软链接路径，通常是默认行为
- `-P`：显示物理路径，解析软链接后的真实目录

常见示例：

```bash
pwd
pwd -L
pwd -P
```

大多数情况下，直接使用 `pwd` 就足够了。只有在目录涉及软链接时，才需要重点关注 `pwd -L` 和 `pwd -P` 的区别。

---

## 三、常用命令与使用场景

### 1. 查看当前所在目录

```bash
pwd
```

示例输出：

```bash
/home/deploy
```

这是最基础的用法。登录服务器后，如果不确定当前在哪个目录，先执行 `pwd`。

### 2. 切换目录后确认位置

```bash
cd /var/log/nginx
pwd
```

输出：

```bash
/var/log/nginx
```

这个组合很常用。`cd` 负责切换目录，`pwd` 负责确认切换后的结果。

### 3. 执行高风险操作前确认目录

```bash
pwd
ls -lh
```

例如准备清理日志前，先确认当前目录：

```bash
cd /data/app/logs
pwd
ls -lh
```

确认无误后再执行删除或移动操作。不要在不确认当前路径的情况下直接执行：

```bash
rm -rf *
```

这类命令一旦目录判断错误，风险很高。

### 4. 在脚本中获取当前目录

```bash
current_dir=$(pwd)
echo "$current_dir"
```

适合脚本中记录当前执行位置，或者执行完某些操作后再回到原目录。

例如：

```bash
old_dir=$(pwd)
cd /data/app
# 执行一些操作
cd "$old_dir"
```

这样可以避免脚本执行后留在不可预期的目录。

### 5. 查看软链接目录的逻辑路径

假设存在软链接：

```bash
/data/current -> /data/releases/2026-05-06
```

执行：

```bash
cd /data/current
pwd
```

可能输出：

```bash
/data/current
```

这表示当前 shell 保留的是逻辑路径，也就是你进入时使用的软链接路径。

### 6. 查看软链接目录的真实物理路径

```bash
pwd -P
```

可能输出：

```bash
/data/releases/2026-05-06
```

这个命令会解析软链接，显示真实目录。

在排查发布目录、版本目录、软链接切换、线上回滚问题时，`pwd -P` 很有用。

### 7. 对比 `pwd -L` 和 `pwd -P`

```bash
pwd -L
pwd -P
```

如果当前目录没有经过软链接，两者输出通常一样。

如果当前目录经过软链接，可能出现差异：

```bash
pwd -L
/data/current

pwd -P
/data/releases/2026-05-06
```

这个差异说明：你看到的目录名和真实目录并不是同一个路径层级。

### 8. 配合 `basename` 获取当前目录名

```bash
basename "$(pwd)"
```

如果当前目录是：

```bash
/data/app/logs
```

输出就是：

```bash
logs
```

适合脚本里只需要当前目录名称，而不是完整路径的场景。

### 9. 配合 `dirname` 获取上级目录

```bash
dirname "$(pwd)"
```

如果当前目录是：

```bash
/data/app/logs
```

输出就是：

```bash
/data/app
```

这种写法在脚本路径处理里比较常见。

---

## 四、常见问题与建议

### 1. `pwd` 和 `echo $PWD` 有什么区别？

两者大多数情况下输出一样：

```bash
pwd
echo $PWD
```

区别在于：

- `pwd` 是命令，用于打印当前工作目录
- `$PWD` 是 shell 维护的环境变量

在涉及软链接、子 shell、脚本执行环境时，两者可能表现出细微差异。日常使用中，优先用 `pwd` 确认当前路径即可。

### 2. 为什么 `pwd` 和 `pwd -P` 输出不一样？

通常是因为当前目录经过了软链接。

例如：

```bash
cd /data/current
pwd
pwd -P
```

一个显示软链接路径，一个显示真实物理路径。这在发布目录中很常见。

### 3. 为什么执行命令前建议先看 `pwd`？

因为很多命令会依赖当前目录，例如：

```bash
rm -rf *
cp *.log /tmp/
mv old new
```

如果当前目录判断错了，结果就可能完全不同。尤其是删除和移动操作，执行前先确认当前路径非常必要。

### 4. 在脚本里什么时候需要用 `pwd`？

常见场景包括：

- 保存脚本执行前所在目录
- 输出日志记录当前目录
- 拼接相对路径为绝对路径
- 执行完成后回到原目录

例如：

```bash
start_dir=$(pwd)
echo "start from: $start_dir"
```

### 5. 生产环境中使用 `pwd` 有什么建议？

建议在以下操作前先执行 `pwd`：

- 删除文件前
- 移动文件前
- 批量修改文件前
- 执行部署脚本前
- 进入软链接发布目录后

特别是在多窗口、多服务器、多用户切换的情况下，先确认路径能避免很多误操作。

---

## 五、常用命令速查

```bash
pwd
pwd -L
pwd -P
cd /var/log/nginx && pwd
current_dir=$(pwd)
echo "$PWD"
basename "$(pwd)"
dirname "$(pwd)"
```

---

## 六、总结

`pwd` 是 Linux 下最基础的路径确认命令。建议优先熟练掌握以下几条：

```bash
pwd
pwd -P
cd /path/to/dir && pwd
current_dir=$(pwd)
```

`pwd` 本身不复杂，但它能帮助你确认当前操作位置。实际工作中，很多问题不是命令不会用，而是路径判断错了。养成在关键操作前先执行 `pwd` 的习惯，可以有效降低误删、误改、误移动文件的风险。
