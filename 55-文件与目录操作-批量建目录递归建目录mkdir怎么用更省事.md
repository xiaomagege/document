# 55｜文件与目录操作｜批量建目录、递归建目录，mkdir 怎么用更省事

`mkdir` 看起来只是“建个目录”，但只要一进脚本和部署场景，它很快就会遇到两个现实问题：父目录不存在怎么办，目录已经存在怎么办。

这也是为什么 `mkdir` 最值得掌握的不是花哨写法，而是 `-p` 和目录权限这些真正影响稳定性的细节。

如果只想先记 3 条：

- 多级目录优先 `mkdir -p`
- 脚本里建目录，默认先考虑“重复执行会不会报错”
- 创建后需要权限可控时，再看 `-m`

## 一、什么情况下先用 `mkdir`

`mkdir` 最适合这些场景：

- 初始化项目目录、日志目录、备份目录
- 一次创建多级路径
- 脚本里准备运行环境

最常见的起手式通常是：

```bash
mkdir logs
mkdir -p /data/app/logs
```

## 二、先记住这几条命令

```bash
mkdir logs
mkdir -p /data/app/logs
mkdir -m 755 scripts
mkdir -pv /data/app/{logs,tmp,backup}
mkdir -p /data/backup/$(date +%F)
```

它们分别适合：

- 创建单个目录
- 递归创建多级目录
- 创建时指定权限
- 一次创建多组目录并显示过程
- 创建日期目录

## 三、为什么 `mkdir -p` 这么常用

### 1. 父目录不存在也能建

```bash
mkdir -p /data/app/logs
```

### 2. 目录已存在也不报错

这在脚本里特别重要，因为脚本往往需要可重复执行。

如果你用的是普通 `mkdir`，第二次运行就可能因为“目录已存在”直接中断。

## 四、创建目录时，哪些细节最常用

### 1. 批量建同级目录

```bash
mkdir -p /data/app/{logs,tmp,backup}
```

### 2. 创建后马上看权限

```bash
mkdir -m 755 scripts
ls -ld scripts
```

### 3. 脚本里给路径加引号

```bash
backup_dir=\"/data/backup/$(date +%F)\"
mkdir -p \"$backup_dir\"
```

这是个很小的习惯，但能减少变量路径带空格或特殊字符时的隐患。

## 五、最常见的误区

### 1. 脚本里还在用普通 `mkdir`

如果脚本需要稳定重复执行，通常应该先问自己：这个目录如果已经存在，会不会出问题。

### 2. 创建完目录就默认权限一定对

目录最终权限还可能受 `umask` 影响，所以对权限敏感的目录，最好再确认一遍。

### 3. 只建目录，不确认路径是否真是目标路径

尤其是带变量时，先 `echo` 或 `pwd` 看清楚路径，会更稳。

## 六、结论

`mkdir` 最实用的核心其实很简单：多级目录和脚本场景里，优先想到 `-p`。

再往下，只要把权限和路径确认好，它就已经足够稳定好用。
