# Linux博客命名与标题改造方案

这份文档保留为命名改造存档。

当前状态：

- 主线文章标题改造已完成
- 主线文章文件名改造已完成
- 现在使用这份文档时，主要把它当作“旧标题到新标题的对照表”

## 一、目标

这次改造的目标不是把标题改得“夸张”，而是从“资料库命名”改成“更像真实问题入口的命名”。

要解决的核心问题有 3 个：

1. 总名称要让人记得住
2. 单篇标题要让人更愿意点进去
3. 读者一眼就知道这套内容偏“实战排障”，不是单纯命令手册

---

## 二、总名称怎么选

### 1. 三种可选方向

| 方向 | 名称建议 | 特点 |
| --- | --- | --- |
| 稳妥专业型 | `Linux运维排障手记` | 专业、清晰、偏技术博客 |
| 更有记忆点 | `运维现场笔记` | 更像品牌名，容易记住 |
| 更偏事故实战 | `Linux故障排查实录` | 很强调现场感和问题感 |

### 2. 最推荐的组合

如果你想同时兼顾专业感和吸引力，我最推荐这一组：

- 主名称：`运维现场笔记`
- 副标题：`Linux排障与运维实战`

这个组合的好处是：

- 比“Linux命令详解”更像一个会持续更新的专栏
- 比纯技术名更有记忆点
- 还能保留 Linux 和排障的明确定位

### 3. 第二推荐组合

如果你更想走稳妥路线，推荐：

- 主名称：`Linux运维排障手记`

这个名字的优势是清晰、稳、专业，但记忆点会比“运维现场笔记”弱一点。

---

## 三、标题改造规则

以后标题尽量不再使用下面这种格式：

```text
Linuxxxx：xxx命令详解
```

建议改成更接近读者搜索方式的标题。

### 1. 问题型标题

公式：

```text
现象 + 命令/方向 + 怎么查/为什么
```

例子：

- `df` 明明有空间，为什么还是报 `No space left on device`？
- SSH 登录失败时，第一步应该查什么？

### 2. 场景型标题

公式：

```text
场景 + 命令 + 结果
```

例子：

- 日志排查时，`tail -f` 怎么看得更快
- 端口不通时，`ss` 为什么更值得先看

### 3. 对比型标题

公式：

```text
命令 A、命令 B + 应该怎么选
```

例子：

- `df` 和 `du` 为什么看到的结果不一样
- `scp` 和 `rsync` 传文件时怎么选

### 4. 实战型标题

公式：

```text
现场问题 + 排查动作 + 根因
```

例子：

- CPU 不高但系统变慢：一次定位到磁盘 IO 瓶颈的现场排查
- 服务起不来时，我会按什么顺序用 systemctl 往下查

---

## 四、首页与导航建议命名

### 1. 首页名称

建议首页或总导航页使用：

- `运维现场笔记：Linux排障总导航`

### 2. 专题页名称

专题页标题尽量以“问题/场景”为主，不以“命令”开头。

例如：

- Linux 系统变慢时的排查顺序
- Linux 磁盘空间排查命令大全
- Linux 日志查看命令怎么选
- Linux 网络连通与端口排查命令清单

---

## 五、现有文章标题改造建议

下面这份清单优先用于：

- 首页展示标题
- 专栏目录标题
- 文章对外发布标题

这部分清单当前主要用于回查“旧标题 -> 新标题”的映射关系。

### 1. 慢系统与性能排查

| 当前标题 | 建议新标题 |
| --- | --- |
| `Linux系统变慢时的排查顺序` | `CPU不高但系统很慢，排查顺序应该怎么走？` |
| `Linux进程与性能：top命令详解` | `Linux系统变慢时，为什么第一步先看 top？` |
| `Linux进程与性能：ps命令详解` | `CPU高、内存高、僵尸进程，ps 应该怎么看？` |
| `Linux进程与性能：iostat命令详解` | `IO高、磁盘慢时，iostat 重点看哪几个指标？` |
| `Linux排障实战：现场反馈产品系统慢的排查步骤` + `Linux排障实战：一次IO瓶颈导致系统缓慢排查记录` | `CPU不高但系统变慢，怎么定位磁盘 IO 瓶颈？` |

### 2. 磁盘与空间排查

| 当前标题 | 建议新标题 |
| --- | --- |
| `Linux磁盘与空间排查命令：df命令详解` | `df` 明明有空间，为什么还是报 `No space left on device`？ |
| `Linux磁盘与空间排查命令：du命令详解` | `哪个目录最占空间？du 应该怎么快速看` |
| `Linux磁盘与空间排查命令：lsblk命令详解` | `Linux 磁盘、分区、挂载点关系，lsblk 一次看清` |
| `Linux磁盘与空间排查命令：fdisk命令详解` | `新磁盘怎么分区？fdisk 最常用的操作有哪些` |
| `Linux磁盘与空间排查命令：mount命令详解` | `磁盘挂载为什么不生效？mount 排查时看什么` |
| `Linux文件与目录：如何查看文件目录大小？` | `Linux 查看目录大小，最快的几种方法` |

### 3. 日志查看与文本定位

| 当前标题 | 建议新标题 |
| --- | --- |
| `Linux文件与文本查看：cat / head / more 命令详解（已合并）` | `小文件、开头预览、简单分页时，cat、head、more 怎么选？` |
| `Linux文件与文本查看：tail命令详解` | `日志排查时，tail -f 怎么看得更快` |
| `Linux文件与文本查看：less命令详解` | `大日志文件打不开时，为什么更推荐 less` |
| `Linux文件与文本查看：grep命令详解` | `日志里找报错，grep 怎么查得又快又准` |
| `Linux文件与文本查看：vim命令详解` | `线上临时改配置和查日志，vim 最常用的操作有哪些` |

### 4. 文件与目录操作

| 当前标题 | 建议新标题 |
| --- | --- |
| `Linux文件与目录：你了解ls命令吗？` | `Linux 查看目录和权限信息，ls 够用到什么程度` |
| `Linux文件与目录基础命令：pwd / cd 命令详解（已合并）` | `切目录总迷路？pwd 和 cd 怎么配合更稳` |
| `Linux文件与目录基础命令：mkdir / touch 命令详解（已合并）` | `初始化目录和占位文件时，mkdir 和 touch 怎么配合？` |
| `Linux文件与目录基础命令：cp命令详解` | `复制文件和目录时，cp 最容易踩的坑有哪些` |
| `Linux文件与目录基础命令：mv命令详解` | `移动、重命名文件时，mv 怎么避免误操作` |
| `Linux文件与目录基础命令：rm命令详解` | `rm 用得快不如用得稳：删除文件前先看这几件事` |
| `Linux文件与文本查看：find命令详解` | `文件太多找不到？find 才是服务器上最强搜索工具` |

### 5. 网络连通与端口排查

| 当前标题 | 建议新标题 |
| --- | --- |
| `Linux网络与远程运维命令：ping命令详解` | `服务器连不通时，ping 第一步能帮你判断什么` |
| `Linux网络与远程运维命令：traceroute命令详解` | `网络到底卡在哪一跳？traceroute 怎么看` |
| `Linux网络与远程运维命令：ss / netstat 命令详解（已合并）` | `端口不通时，ss 和 netstat 应该怎么选？` |
| `Linux网络与远程运维命令：nc命令详解` | `端口通不通，nc 一条命令就能试出来` |
| `Linux网络与远程运维命令：curl命令详解` | `接口到底通没通？curl 是排查 HTTP 问题的第一把刀` |
| `Linux网络与远程运维命令：wget命令详解` | `只想验证下载链路，wget 比 curl 什么时候更顺手` |

### 6. SSH 与远程运维

| 当前标题 | 建议新标题 |
| --- | --- |
| `Linux网络与远程运维命令：ssh命令详解` | `SSH 登录失败时，第一步应该查什么？` |
| `Linux网络与远程运维命令：scp命令详解` | `Linux 远程拷贝文件，scp 最常用的几种写法` |
| `Linux网络与远程运维命令：rsync命令详解` | `大文件同步为什么更推荐 rsync` |

### 7. 权限、身份与提权

| 当前标题 | 建议新标题 |
| --- | --- |
| `Linux权限与用户管理命令：whoami / id 命令详解（已合并）` | `执行命令前，whoami 和 id 应该怎么配合？` |
| `Linux权限与用户管理命令：su命令详解` | `切换用户后环境不对？su 最容易踩的坑有哪些` |
| `Linux权限与用户管理命令：sudo命令详解` | `sudo 为什么会失败？排查时先看哪几项` |
| `Linux权限与用户管理命令：chmod命令详解` | `文件权限到底怎么改？chmod 最容易记混的地方` |
| `Linux权限与用户管理命令：chown命令详解` | `文件属主不对怎么办？chown 应该怎么改` |
| `Linux权限与用户管理命令：chgrp命令详解` | `文件属组不对时，chgrp 应该怎么用` |

### 8. 文本处理与批量处理

| 当前标题 | 建议新标题 |
| --- | --- |
| `Linux文本处理与筛选命令：awk命令详解` | `按列提取、过滤日志，awk 最值钱的用法有哪些` |
| `Linux文本处理与筛选命令：cut / wc 命令详解（已合并）` | `快速切字段、数行数时，cut 和 wc 怎么配合？` |
| `Linux文本处理与筛选命令：sed命令详解` | `批量替换文本时，sed 最常用的操作有哪些` |
| `Linux文本处理与筛选命令：sort命令详解` | `排序结果不对？sort 最容易忽略的几个选项` |
| `Linux文本处理与筛选命令：uniq命令详解` | `日志去重统计时，uniq 应该怎么配合 sort` |
| `Linux文本处理与筛选命令：xargs命令详解` | `批量执行命令时，xargs 怎么用才安全` |

### 9. 服务、软件与防火墙

| 当前标题 | 建议新标题 |
| --- | --- |
| `Linux系统与软件管理：systemctl命令详解` | `服务起不来时，systemctl 应该先看什么` |
| `Linux系统与软件管理：rpm命令详解` | `软件包装没装、版本对不对，rpm 怎么快速确认` |
| `Linux网络与防火墙：firewalld命令详解` | `端口开了还是不通？firewalld 应该怎么查` |
| `Linux网络与防火墙：iptables命令详解` | `Linux 防火墙规则太多时，iptables 该从哪看起` |

---

## 六、执行顺序说明

这部分原本用于指导命名改造落地顺序，当前已经基本执行完成。

原执行顺序是：

1. 先确定总名称
2. 先改首页、导航页、专题页标题
3. 再改高流量潜力文章标题
4. 最后再决定是否批量改文件名

当前实际状态是：

- 总名称已确定
- 导航页与主线标题已改
- 主线文件名已批量重命名

如果只做最小改动但想立刻看到效果，优先改这 10 个标题最划算：

1. `Linux系统变慢时的排查顺序`
2. `Linux进程与性能：top命令详解`
3. `Linux进程与性能：iostat命令详解`
4. `Linux磁盘与空间排查命令：df命令详解`
5. `Linux磁盘与空间排查命令：du命令详解`
6. `Linux文件与文本查看：tail命令详解`
7. `Linux文件与文本查看：grep命令详解`
8. `Linux网络与远程运维命令：ssh命令详解`
9. `Linux权限与用户管理命令：sudo命令详解`
10. `Linux系统与软件管理：systemctl命令详解`

---

## 七、最终建议

如果你希望这套博客更像“一个会被记住的专栏”，而不是“零散命令文章集合”，推荐这样落地：

- 专栏名称：`运维现场笔记`
- 副标题：`Linux排障与运维实战`
- 导航页标题：`运维现场笔记：Linux排障总导航`

这组命名既保留了技术定位，也明显比“Linux 命令详解”更有温度、更容易记住。
