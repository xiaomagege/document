# 08｜磁盘与空间排查｜哪个目录最占空间？du 应该怎么快速看

`df` 能告诉你哪个挂载点快满了，但它不会告诉你到底是谁把空间吃掉了。

真正要定位“大目录、大文件、日志堆积、缓存膨胀”，通常要靠 `du` 一层一层缩小范围。

如果只想先记 3 条：

- 找目录占用，先用 `du -h --max-depth=1 /path | sort -hr`
- 根目录排查时加 `-x`，避免跨到其他挂载点
- 大目录不要一上来深度递归，先按层级缩小范围

## 一、什么时候优先用 `du`

下面这些场景都适合用 `du`：

- `df -h` 已经确认某个分区快满
- 想知道 `/var`、`/data`、`/home` 下谁最大
- 日志、缓存、备份文件疑似膨胀
- 清理前想评估删除收益
- `df` 显示空间高，但你需要定位具体目录

`du` 关注的是目录和文件占用，它比 `df` 更适合回答：

**到底是谁占了空间？**

## 二、先记住这几条命令

```bash
du -sh /var/log
du -h --max-depth=1 /var | sort -hr
du -xh --max-depth=1 /
du -ah /var/log | sort -hr | head -20
du -h --max-depth=1 . --exclude='node_modules'
find /var/log -type f -size +100M -exec du -h {} + | sort -hr
```

它们分别适合：

- `du -sh /path`：看单个目录总大小
- `du -h --max-depth=1 /path | sort -hr`：看一级目录谁最大
- `du -xh --max-depth=1 /`：排查根分区时不跨文件系统
- `du -ah /path | sort -hr | head`：找大文件和大目录
- `--exclude`：排除无关目录
- `find ... -size`：按文件大小进一步筛选

## 三、推荐的空间排查路径

### 1. 先用 `df` 找到问题分区

```bash
df -h
```

比如发现 `/var` 所在分区快满，再进入 `/var` 排查。

### 2. 再用 `du` 看一级目录

```bash
du -h --max-depth=1 /var | sort -hr
```

这一步不要贪多，只看一级目录。先判断是 `/var/log`、`/var/lib`、`/var/cache`，还是其他目录占用异常。

### 3. 进入最大目录继续缩小

比如发现 `/var/log` 最大：

```bash
du -h --max-depth=1 /var/log | sort -hr
```

继续逐层往下，直到定位到具体服务、具体文件类型或具体日期目录。

### 4. 必要时再找大文件

```bash
find /var/log -type f -size +100M -exec du -h {} + | sort -hr
```

这一步适合在已经缩小到某个目录后使用，不建议一上来全盘执行。

## 四、为什么 `du` 有时候很慢

`du` 需要遍历目录树。目录越大、文件越多、存储越慢，它就越慢。

常见慢的原因：

- 大量小文件
- 深层目录太多
- 网络挂载或云盘延迟高
- 根目录下跨到了其他挂载点
- 权限不足导致反复报错

更稳的做法是：

```bash
du -xh --max-depth=1 /
```

`-x` 可以限制只统计当前文件系统，避免从 `/` 一路扫到 `/data`、`/mnt` 这类单独挂载目录。

## 五、`du` 和 `df` 结果不一致怎么办

常见原因有：

- 文件已删除但进程仍占用
- 当前目录下面还有挂载点
- 权限不足，导致 `du` 没统计完整
- 文件系统自身元数据或预留空间

可以补查：

```bash
lsof | grep deleted
mount | grep /path
df -h /path
df -ih /path
```

如果 `du` 看起来不大，但 `df` 仍然很满，优先怀疑“删除未释放”或“挂载点覆盖”。

## 六、清理前一定要确认的事

生产环境不要看到大文件就直接删。

清理前至少确认：

- 这个目录属于哪个业务
- 文件是否仍在被进程写入
- 是否有备份、上传、数据库、容器运行数据
- 删除后是否需要重启服务才能释放空间

更稳的动作通常是：

- 日志先截断或按规则清理
- 缓存先确认可再生
- 备份先确认保留周期
- 容器目录先用容器工具清理

## 七、结论

`du` 最适合回答“到底是谁占了空间”。

排查磁盘满时，推荐顺序是：先用 `df -h /path` 找挂载点，再用 `du -h --max-depth=1 /path | sort -hr` 逐层缩小范围，最后才考虑清理或迁移。
