# 26｜网络连通与端口排查｜服务器连不通时，ping 第一步能帮你判断什么

`ping` 很常用，但它最容易被高估。

很多人一看到服务访问失败，就先 `ping` 一下，然后不是因为它通了就误以为服务没问题，就是因为它不通就直接判定机器挂了。更稳的做法，是把 `ping` 放回它最适合的位置：先帮你判断“网络这一层大概有没有问题”，但不要让它替你回答所有问题。

如果只想先记 3 条：

- `ping` 适合先看主机或链路在 ICMP 层面通不通
- `ping` 通了，不代表端口和服务一定通
- `ping` 不通，也不一定代表服务器真的挂了

## 一、什么情况下先用 `ping`

`ping` 最适合这些场景：

- 先判断目标主机是不是大体可达
- 先看 DNS 解析有没有明显问题
- 先看延迟和丢包是不是已经很异常

第一条最常用的就是：

```bash
ping -c 4 目标IP
ping -c 4 域名
```

它能帮你快速回答：

- 能不能收到回应
- 大概延迟高不高
- 有没有明显丢包

## 二、先记住这几条命令

```bash
ping -c 4 8.8.8.8
ping -c 4 example.com
ping -q -c 10 8.8.8.8
ping -4 -c 4 example.com
ping -6 -c 4 example.com
```

它们分别适合：

- `-c 4`：快速测几次就停，排查最常用
- `ping 域名`：顺手验证解析和连通性
- `-q`：只看统计结果，适合看丢包和平均延迟
- `-4` / `-6`：分开验证 IPv4 和 IPv6

## 三、`ping` 能帮你先判断什么

### 1. 主机大体可不可达

```bash
ping -c 4 10.0.0.8
```

如果能稳定收到回应，至少说明从你当前这台机器到目标主机的 ICMP 路径大体可达。

### 2. 域名解析是不是已经出问题

```bash
ping -c 4 example.com
```

如果连 IP 都没解析出来，那问题就可能还没到业务层，先去看 DNS。

### 3. 延迟和丢包是不是已经异常

```bash
ping -q -c 10 example.com
```

这一步很适合快速看：

- 丢包率是不是已经明显偏高
- 平均延迟是不是突然升高
- 抖动是不是很大

## 四、`ping` 不能替你判断什么

### 1. 不能证明服务端口一定可用

`ping` 用的是 ICMP，不是你的业务端口。

所以即使 `ping` 通了，服务也可能：

- 端口没监听
- 防火墙没放行
- 应用本身已经挂了

这时要继续看：

```bash
nc -vz host 端口
curl -v URL
ss -lntp
```

### 2. 不能证明服务器一定挂了

`ping` 不通时，也可能只是：

- 目标禁了 ICMP
- 安全组不让 ping
- 防火墙丢了 ICMP
- 中间设备限速或丢弃

所以它不通时，更准确的理解是：ICMP 这条路没给你答案，而不是服务器一定已经死了。

## 五、排查时怎么把它放在正确位置

推荐顺序通常是：

1. 先 `ping` 看基础连通性和延迟
2. 再 `nc` 看端口
3. 再 `curl` 看应用层响应
4. 再到服务端看 `ss`、防火墙和服务状态

如果你怀疑路径问题，再补：

```bash
traceroute -n 目标
```

## 六、最常见的误区

### 1. `ping` 通了就判断“服务没问题”

这往往会漏掉端口、防火墙、应用层错误。

### 2. `ping` 不通就判断“机器挂了”

很多机器压根就不会回应 ICMP，这在云主机和受控环境里很常见。

### 3. 一直不带 `-c`

排查时更推荐：

```bash
ping -c 4 目标
```

这样输出更干净，也更适合脚本和记录结果。

## 七、结论

`ping` 很适合做第一步，但只适合做第一步。

它最擅长回答“网络这一层有没有明显异常”，真正要判断端口、服务和应用是否正常，还得继续往下查。
