# 47｜文件与目录操作｜Linux 查看目录和权限信息，ls 够用到什么程度

`ls` 很基础，但在服务器上它远不只是“看看当前目录里有什么文件”。

很多排障动作的第一眼，其实都是从 `ls -l` 开始的：权限对不对、文件是谁的、最近谁改过、目录本身能不能进，这些都能先从 `ls` 里拿到线索。

如果只想先记 3 条：

- 看权限和属主时，优先 `ls -l`
- 看隐藏文件时，别忘了 `-a`
- 看目录本身属性时，用 `ls -ld`，别只会 `ls -l 目录名`

## 一、什么情况下先用 `ls`

`ls` 最适合这些场景：

- 先看目录里大概有什么
- 先看文件权限、属主、时间和大小
- 先确认目录本身的权限是不是有问题

最常用的起手式通常是：

```bash
ls -l
ls -la
ls -ld 目录
```

## 二、先记住这几条命令

```bash
ls
ls -l
ls -la
ls -lh
ls -lt
ls -ld /data/app
```

它们分别适合：

- `ls`：快速看名字
- `ls -l`：看权限、属主、时间
- `ls -la`：连隐藏文件一起看
- `ls -lh`：大小更直观
- `ls -lt`：按时间看最新变动
- `ls -ld`：看目录本身属性

## 三、排查时，最有价值的几种看法

### 1. 看权限和属主

```bash
ls -l app.log
```

这一步很适合先判断：

- 当前文件是谁的
- 权限位看起来是不是合理
- 最近有没有明显被改动

### 2. 看隐藏文件

```bash
ls -la
```

很多配置问题都藏在点文件里，比如 `.env`、`.bashrc`、`.ssh`。不带 `-a` 很容易漏掉关键线索。

### 3. 看最近谁动过

```bash
ls -lt
```

排查“刚才谁改了配置”“哪个文件刚生成”时，这条很顺手。

### 4. 看目录本身，而不是目录内容

```bash
ls -ld /data/app
```

这是最容易被忽略的一点。很多权限问题根因在目录本身，但你如果只执行 `ls -l /data/app`，看到的其实是目录里面的内容。

## 四、`ls` 最适合做到哪一步

`ls` 很适合做第一眼判断，但它通常不是终点。

比如：

- 想继续查目录大小，要交给 `du`
- 想继续找文件，要交给 `find`
- 想继续判断权限背景，要交给 `id`、`chmod`、`chown`

所以可以把它理解成：最好的入口之一，但不是所有问题的终局工具。

## 五、最常见的误区

### 1. 把 `ll` 当标准命令

很多系统里 `ll` 只是 alias，脚本和通用文档里更稳的是直接写 `ls -l`。

### 2. 只看文件，不看目录本身

目录没有执行权限时，文件本身权限正常也可能照样进不去。

### 3. 看到大小就以为是磁盘占用

`ls` 看到的是文件大小，磁盘实际占用还要继续看 `du`。

## 六、结论

`ls` 不是一个“太基础所以不用认真写”的命令，它在排障里其实非常高频。

先用它看权限、属主、时间和隐藏文件，再决定后面要不要进 `find`、`du` 或权限链路，会很顺。
