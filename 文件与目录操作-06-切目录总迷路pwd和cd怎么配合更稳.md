# 文件与目录操作06：切目录总迷路？pwd 和 cd 怎么配合更稳

很多误删、误改、找错日志的事故，不是因为命令不会用，而是因为路径感丢了。

`pwd` 和 `cd` 单独看都很基础，但真到服务器上做排查和高风险操作时，这两个命令应该被当成一组来看：

- `cd` 负责切到目标位置
- `pwd` 负责确认你真的到了对的位置

如果只想先记 3 条：

- 高风险操作前先 `pwd`
- 不确定路径时，绝对路径通常比相对路径稳
- 两个目录来回切时，用 `cd -`，切完再 `pwd`

## 一、什么时候要把 `pwd` 和 `cd` 连着用

最值得连着用的场景有 3 类：

- 刚跳进日志目录、配置目录、部署目录时
- 准备执行 `rm`、`mv`、`cp`、批量替换这类高风险动作前
- 脚本里需要临时切目录、做完再切回去时

最常见的起手式其实很简单：

```bash
cd /data/app/logs
pwd
```

## 二、先记住这几条高频用法

```bash
pwd
pwd -P
cd /var/log/nginx
cd ..
cd -
old_dir=$(pwd)
```

它们分别适合：

- `pwd`：看当前逻辑路径
- `pwd -P`：看解析软链接后的真实路径
- `cd /path`：用绝对路径进入目标目录
- `cd ..`：回上一级
- `cd -`：回到上一个目录
- `old_dir=$(pwd)`：在脚本里先保存当前位置

## 三、这两个命令最常见的配合场景

### 1. 切到目标目录后立刻确认

```bash
cd /etc/nginx
pwd
ls
```

这一步看起来简单，但它能挡住很多“以为自己已经进对目录”的误判。

### 2. 两个目录来回切，优先用 `cd -`

```bash
cd /var/log/nginx
cd /etc/nginx/conf.d
cd -
pwd
```

这在“查日志目录”和“改配置目录”之间来回跳时特别省心。

### 3. 软链接目录里同时看逻辑路径和真实路径

```bash
pwd
pwd -P
```

如果你在发布目录、版本目录、软链接切换目录里工作，这两条的区别很重要。

## 四、脚本里怎么把它们用稳

### 1. 切目录失败时别继续跑

```bash
cd /data/app || exit 1
```

### 2. 先保存原目录，再恢复

```bash
old_dir=$(pwd)
cd /data/app || exit 1
# 做事
cd "$old_dir"
```

### 3. 不要默认相对路径一定成立

脚本被不同用户、不同入口调用时，相对路径最容易飘。只要一不确定，就优先绝对路径。

## 五、最常见的误区

### 1. 只凭提示符判断目录

很多 shell 提示符会简写路径，真正稳的还是直接 `pwd`。

### 2. 只顾着切目录，不确认切到了哪里

`cd` 本身不难，难的是后续操作是不是发生在你以为的地方。

### 3. 在复杂脚本里大量依赖相对路径

这类问题平时不一定暴露，一旦换入口、换用户、换执行环境，就很容易出错。

## 六、结论

`pwd` 和 `cd` 的价值，不在功能复杂，而在于它们能帮你把路径感找回来。

先切，再确认；不确定就用绝对路径；两个目录来回切就用 `cd -`。把这几条习惯养稳，很多低级但代价高的误操作都能提前挡住。

