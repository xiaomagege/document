# Linux文件与目录基础命令：cd命令知多少？

在 Linux 日常使用、服务器运维和问题排查中，`cd` 是最基础、使用频率最高的目录切换命令之一。无论是进入项目目录、切换到日志目录、返回上一级目录、回到用户家目录，还是在多个目录之间来回切换，`cd` 都是绕不开的基础命令。

很多人觉得 `cd` 很简单，只是“进入某个目录”，但实际工作中经常会遇到相对路径、绝对路径、软链接目录、权限不足、脚本里切目录不生效等问题。把这些细节理解清楚，能减少很多路径相关的低级错误。

---

## 一、`cd` 是什么？

`cd` 是 change directory 的缩写，用于切换当前 shell 会话所在的工作目录。

它主要用于：

- 进入指定目录
- 返回上一级目录
- 回到当前用户家目录
- 在最近两个目录之间来回切换
- 配合相对路径和绝对路径快速定位目录

说明：

- `cd` 通常是 shell 内置命令，不是普通的外部可执行程序
- `cd` 改变的是当前 shell 的工作目录
- 如果目录不存在或没有进入权限，`cd` 会执行失败

查看 `cd` 是否为内置命令：

```bash
type cd
```

常见输出类似：

```bash
cd is a shell builtin
```

这说明 `cd` 是由当前 shell 直接处理的命令。

---

## 二、常见使用语法与重点参数

```bash
cd [目录]
```

常见写法说明：

- `cd /path/to/dir`：切换到指定绝对路径目录
- `cd dir`：切换到当前目录下的子目录
- `cd ..`：返回上一级目录
- `cd ../..`：返回上两级目录
- `cd`：回到当前用户家目录
- `cd ~`：回到当前用户家目录
- `cd -`：回到上一次所在目录
- `cd .`：停留在当前目录，通常用于理解路径，不常单独使用

部分 shell 中常见参数：

- `-L`：逻辑路径模式，默认行为，保留软链接路径显示
- `-P`：物理路径模式，解析软链接后进入真实物理路径

示例：

```bash
cd -P /data/current
```

如果 `/data/current` 是软链接，`cd -P` 会进入它指向的真实目录。

---

## 三、常用命令与使用场景

### 1. 进入指定目录

```bash
cd /var/log
```

这是最常见的用法，适合直接进入某个明确的系统目录、日志目录或应用目录。

进入后可以用 `pwd` 查看当前所在目录：

```bash
pwd
```

### 2. 进入当前目录下的子目录

```bash
cd logs
```

如果当前目录下存在 `logs` 子目录，就会进入该目录。这个写法依赖当前所在位置，属于相对路径。

例如当前在 `/app/nginx`：

```bash
cd logs
```

实际进入的是：

```bash
/app/nginx/logs
```

### 3. 返回上一级目录

```bash
cd ..
```

适合从子目录回到父目录。例如当前在：

```bash
/app/nginx/logs
```

执行后会回到：

```bash
/app/nginx
```

### 4. 连续返回多级目录

```bash
cd ../..
```

适合目录层级较深时快速往上退两级。

例如当前在：

```bash
/app/nginx/logs/access
```

执行后会回到：

```bash
/app/nginx
```

如果要返回三级，可以写：

```bash
cd ../../..
```

### 5. 回到当前用户家目录

```bash
cd
```

或者：

```bash
cd ~
```

普通用户执行后通常回到：

```bash
/home/用户名
```

`root` 用户执行后通常回到：

```bash
/root
```

这是登录服务器后最常用的“回到起点”方式。

### 6. 进入指定用户的家目录

```bash
cd ~root
```

表示进入 `root` 用户的家目录。

也可以进入其他用户家目录：

```bash
cd ~deploy
```

前提是用户存在，并且当前用户有权限访问对应目录。

### 7. 回到上一次所在目录

```bash
cd -
```

这个命令非常适合在两个目录之间来回切换。

例如：

```bash
cd /var/log/nginx
cd /app/nginx/conf
cd -
```

最后一次 `cd -` 会回到：

```bash
/var/log/nginx
```

再次执行：

```bash
cd -
```

又会回到：

```bash
/app/nginx/conf
```

实际排查问题时，经常需要在配置目录和日志目录之间来回切换，`cd -` 很实用。

### 8. 使用绝对路径切换目录

```bash
cd /etc/nginx/conf.d
```

绝对路径从 `/` 根目录开始，不依赖当前所在目录。

适合：

- 路径明确
- 脚本中切换目录
- 避免相对路径带来的误操作

在生产环境中，如果是关键操作，优先使用绝对路径会更稳妥。

### 9. 使用相对路径切换目录

```bash
cd ../logs
```

相对路径依赖当前目录。

例如当前在：

```bash
/app/nginx/conf
```

执行：

```bash
cd ../logs
```

会进入：

```bash
/app/nginx/logs
```

相对路径写起来短，但前提是你清楚当前所在位置。执行关键命令前建议先用 `pwd` 确认路径。

### 10. 进入带空格的目录

如果目录名中有空格，需要加引号或转义空格。

```bash
cd "my project"
```

或者：

```bash
cd my\ project
```

生产环境目录一般不建议使用空格命名，脚本处理起来容易出错。

### 11. 进入软链接目录

假设存在软链接：

```bash
/data/current -> /data/releases/2026-05-06
```

执行：

```bash
cd /data/current
pwd
```

很多 shell 默认会显示逻辑路径：

```bash
/data/current
```

如果想查看真实物理路径，可以使用：

```bash
pwd -P
```

或者进入时直接使用：

```bash
cd -P /data/current
```

在排查发布目录、软链接切换、版本回滚问题时，这个细节很有用。

### 12. 切换目录失败时查看原因

```bash
cd /data/app
```

如果失败，常见原因有三类：

- 目录不存在
- 当前用户没有进入权限
- 路径写错了

可以依次检查：

```bash
ls -ld /data/app
pwd
whoami
```

如果提示 `No such file or directory`，通常是路径不存在或写错。

如果提示 `Permission denied`，通常是当前用户没有目录的执行权限。

---

## 四、常见问题与建议

### 1. 为什么 `cd` 后提示 No such file or directory？

说明目标目录不存在，或者路径写错了。

可以先确认父目录是否存在：

```bash
ls -ld /data
ls -ld /data/app
```

如果是相对路径，先确认当前所在目录：

```bash
pwd
```

很多路径错误都是因为当前目录和自己以为的不一致。

### 2. 为什么 `cd` 后提示 Permission denied？

说明当前用户没有进入该目录的权限。

目录想要被进入，必须有执行权限 `x`。

查看目录权限：

```bash
ls -ld /data/app
```

如果当前用户不是目录属主，也不在对应用户组里，就可能无法进入。

### 3. `cd` 和 `pwd` 经常一起用吗？

是的。`cd` 负责切换目录，`pwd` 负责确认当前目录。

尤其在执行删除、移动、批量修改这类高风险操作前，建议先执行：

```bash
pwd
```

确认位置后再继续操作。

### 4. `cd -` 适合什么场景？

适合在两个目录之间来回切换，例如：

- 日志目录和配置目录
- 当前版本目录和历史版本目录
- 项目目录和临时目录

常见组合：

```bash
cd /var/log/nginx
cd /etc/nginx
cd -
```

比重复输入长路径更高效。

### 5. 为什么脚本里执行 `cd`，退出脚本后当前目录没变？

因为脚本通常在子 shell 中执行，子 shell 里的目录变化不会影响父 shell。

例如脚本内容：

```bash
cd /data/app
pwd
```

执行脚本时，脚本内部确实进入了 `/data/app`，但脚本结束后，当前交互式 shell 的目录不会改变。

如果确实想让脚本影响当前 shell，需要用 `source` 执行：

```bash
source ./change-dir.sh
```

或者：

```bash
. ./change-dir.sh
```

这个问题在写部署脚本、环境初始化脚本时很常见。

### 6. 为什么进入软链接目录后，`pwd` 显示的不是实际目录？

因为默认情况下很多 shell 会保留逻辑路径。

例如你进入的是软链接：

```bash
cd /data/current
```

`pwd` 可能显示：

```bash
/data/current
```

但真实路径可能是：

```bash
/data/releases/2026-05-06
```

想看真实路径，用：

```bash
pwd -P
```

或者：

```bash
cd -P /data/current
```

### 7. 生产环境中使用 `cd` 有什么建议？

建议养成三个习惯：

- 关键操作前先 `pwd`
- 重要路径尽量使用绝对路径
- 删除、移动文件前先 `ls` 确认目标

例如：

```bash
cd /data/app/logs
pwd
ls -lh
```

确认无误后再执行后续操作。

---

## 五、常用命令速查

```bash
cd /var/log
cd logs
cd ..
cd ../..
cd
cd ~
cd ~root
cd -
cd /etc/nginx/conf.d
cd ../logs
cd "my project"
cd my\ project
cd -P /data/current
pwd
pwd -P
```

---

## 六、总结

`cd` 是 Linux 下最基础的目录切换命令，也是所有文件操作、日志排查、系统运维工作的起点。建议优先熟练掌握以下几条：

```bash
cd /path/to/dir
cd ..
cd ../..
cd
cd ~
cd -
pwd
```

掌握这些用法后，大多数“进入目录、返回上级、回到家目录、在两个目录之间切换、确认当前路径”的场景都能快速处理。

实际工作中，`cd` 本身不复杂，真正容易出问题的是路径判断和权限判断。执行关键操作前先用 `pwd` 确认当前位置，遇到失败时用 `ls -ld` 检查目录是否存在和权限是否正确，这样能避免很多目录误操作和排查方向错误的问题。
