# 18｜日志查看与文本定位｜只想看前几行内容，head 有哪些高频用法

`head` 很简单，但它解决的问题非常明确：

**我只想先看开头一点点，不想把整个文件都打开。**

这在看配置、预览数据文件、确认日志开头、截取命令输出时都很顺手。

如果只想先记 3 条：

- 看文件开头，直接用 `head`
- 指定行数时，最常用的是 `head -n 20 file`
- 想看命令输出前几行时，直接 `命令 | head`

## 一、什么时候优先用 `head`

这些场景很适合先用 `head`：

- 快速看配置文件开头
- 预览 CSV、日志、文本文件格式
- 确认文件头部注释或表头
- 输出太长，只想先看前几行
- 想确认命令结果是不是大致对路

它最擅长回答的是：

**文件或输出开头长什么样？**

## 二、最常用的几条命令

```bash
head app.log
head -n 20 app.log
head -c 100 file.txt
head file1.txt file2.txt
ps -ef | head
```

它们分别适合：

- `head file`：默认看前 10 行
- `head -n 20 file`：看更多行
- `head -c 100 file`：看开头几个字节
- `head file1 file2`：同时预览多个文件
- `命令 | head`：只取输出前几行

## 三、最常见的 4 个场景

### 1. 快速看配置文件开头

```bash
head -n 20 /etc/nginx/nginx.conf
```

适合确认：

- 文件是不是你要找的那个
- 开头注释和全局配置长什么样
- 编码、换行、表头有没有明显问题

### 2. 预览数据文件格式

```bash
head -n 5 data.csv
```

很适合先看：

- 表头字段顺序
- 分隔符是不是预期的
- 开头几行数据有没有错位

### 3. 只取很长输出的前几行

```bash
ps -ef | head
```

或者：

```bash
find /var/log -type f | head
```

这在确认输出结构时很有用，不必一上来就看全量结果。

### 4. 看文件开头一小段字节

```bash
head -c 100 file.txt
```

这适合：

- 只想确认开头内容
- 想快速看文本头部特征
- 处理某些格式文件时做轻量预览

## 四、`head` 和 `tail`、`less` 怎么分工

- `head`：看开头
- `tail`：看结尾和最新变化
- `less`：需要翻页、搜索、来回跳时更合适

你可以直接这样选：

- 只看前几行：`head`
- 只看最后几行：`tail`
- 需要搜索和回看：`less`

## 五、最常见的误区

### 1. 想看文件结尾却还在用 `head`

这时候应该切到：

```bash
tail file
```

### 2. 大文件里反复定位内容还坚持用 `head`

`head` 适合看开头，不适合搜索和来回翻。需要深入时，直接改用 `less`。

### 3. 管道里习惯性先 `cat` 再 `head`

很多时候像这样：

```bash
cat app.log | head
```

并不比直接：

```bash
head app.log
```

更有优势。

## 六、结论

`head` 不是复杂工具，但在“快速预览开头”这件事上非常高效。

看配置、看表头、看输出结构时，先用 `head`，往往比整份打开更轻快。
