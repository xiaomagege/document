# 文件与目录操作05：rm 用得快不如用得稳：删除文件前先看这几件事

`rm` 在 Linux 里真正让人害怕的，不是它难，而是它太直接。

很多误删并不是因为参数不会写，而是因为删之前没有先确认“当前在哪、删的是谁、范围会不会被通配符放大”。所以 `rm` 这篇最该讲的不是参数大全，而是删除前的判断顺序。

如果只想先记 3 条：

- 删除前先看 `pwd`
- 递归删除前先看 `ls -ld` 和匹配结果
- 不确定时，先 `mv` 到临时备份目录，比直接 `rm` 更稳

## 一、什么情况下先用 `rm`

`rm` 最适合这些场景：

- 删除明确无用的单个文件
- 清理一批临时文件
- 删除确认可清理的旧目录

最常见的写法是：

```bash
rm old.log
rm -i app.conf
rm -r old_dir
```

但越是常见的写法，越容易因为“太顺手”而少做确认。

## 二、先记住这几条命令

```bash
rm old.log
rm -i app.conf
rm -v *.tmp
rm -r old_dir
rm -- -test
```

它们分别适合：

- 删除单个文件
- 删除前确认
- 看清批量删除过程
- 递归删目录
- 删除以 `-` 开头的特殊文件名

## 三、删除前，最该先确认什么

### 1. 当前目录

```bash
pwd
```

很多事故都不是因为 `rm` 本身，而是因为人在错误目录里执行了正确命令。

### 2. 目标到底是谁

```bash
ls -l old.log
ls -ld old_dir
```

如果你都还没确认目标本身，就已经开始删，风险其实已经上来了。

### 3. 通配符到底会匹配到什么

```bash
ls -lh *.tmp
```

先看一遍匹配结果，再执行删除，永远比直接一把下去更稳。

## 四、什么时候该直接删，什么时候该先挪走

### 1. 明确无用、可重建的临时文件

可以直接删：

```bash
rm -v *.tmp
```

### 2. 还不完全确定的文件

更稳的是先挪到临时目录：

```bash
mkdir -p /tmp/delete-backup
mv old.log /tmp/delete-backup/
```

这在日志、配置和脚本场景里尤其值得养成习惯。

## 五、递归删除为什么风险最高

### 1. 影响范围会瞬间放大

```bash
rm -r old_dir
```

如果目标目录判断错了，结果不会只错一个文件，而是整棵一起错。

### 2. `-f` 会把提醒一起关掉

像下面这种写法：

```bash
rm -rf tmp_dir
```

不是不能用，而是必须建立在你已经确认过路径和范围的前提上。

## 六、最常见的误区

### 1. 把 `rm -rf` 当成常规清理命令

它很高效，但也最容易把误差直接放大。

### 2. 用 `rm` 清空正在写入的日志

很多时候你真正想要的是清空内容，而不是删除文件本身。这时更适合：

```bash
> app.log
truncate -s 0 app.log
```

### 3. 不会处理特殊文件名

如果文件名以 `-` 开头，直接删可能会被当成参数：

```bash
rm -- -test
```

## 七、结论

`rm` 最重要的不是“删得快”，而是“删之前有没有把路径和范围看清”。

先 `pwd`，再确认目标，再确认匹配结果。不确定时先挪走，这套顺序比记更多参数更重要。

