# Linux博客结构重组路线图

这份文档保留为早期重组思路存档，当前执行请优先参考：

- [Linux文章系统性重构规划](./Linux文章系统性重构规划.md)
- [Linux文章分类与发布路径](./Linux文章分类与发布路径.md)

## 一、为什么要重组

当前仓库已经积累了不少内容，但整体结构仍然偏“命令词条库”。

详细执行规划见：

- [Linux文章系统性重构规划](./Linux文章系统性重构规划.md)

- Linux 命令型文章约 52 篇
- 排障/问题型文章约 2 篇
- 其它整理型或跨主题文章若干

现有结构的主要问题不是“内容少”，而是“切分太细”：

- 以单个命令为最小发布单元，很多文章天然篇幅有限，信息密度偏低
- 标题虽然统一，但大多偏资料库命名，不够贴近读者搜索问题的方式
- 文章之间缺少专题页、问题页和相关阅读，流量很难在站内流动
- 读者能学到命令，但不容易形成“以后继续关注这个作者”的理由

从现在开始，博客主结构不再以“一个命令一篇”为核心，而改成：

```text
问题驱动 + 专题聚合 + 排障实战 + 命令速查
```

当前执行方式也同步调整为：

- 直接改造现有文章正文
- 不再单独保留一套“旧文章供新专题引用”的平行结构
- 现有文章会逐步被重写成问题导向、场景导向的版本

## 二、新的内容架构

### 1. 导航页

导航页不追求流量，追求“第一次进入就看懂你写什么”。

建议保留 1 个总入口页，负责承接全部内容：

- Linux 运维排障总导航
- 按问题进入，而不是按命令进入
- 每个专题页都能继续跳转到实战文和速查文

### 2. 专题页

专题页是以后最重要的主力内容层。

它的特点是：

- 一篇文章解决一类问题
- 一篇文章串起多个高频命令
- 读者搜问题时更容易命中
- 文章内部天然适合做命令对比、使用顺序和排障路径

以后优先写的，不是“`tail` 命令详解”，而是：

- Linux 日志查看与定位命令实战
- Linux 磁盘空间排查命令大全
- Linux 系统变慢时的排查顺序

### 3. 问题页

问题页是搜索流量入口，标题要直接对应搜索意图。

这类文章的目标是：

- 回答一个具体问题
- 给出明确判断路径
- 把读者带到相关专题页和实战页

典型题目应该像这样：

- `df` 明明有空间，为什么还报 `No space left on device`？
- CPU 不高，但 Linux 系统很慢，该从哪里开始排查？
- SSH 免密登录失败，一般先查哪几个地方？

### 4. 排障实战页

排障实战页负责建立信任感，是最容易带来关注和收藏的内容层。

这类文章要强调：

- 真实问题现象
- 判断过程
- 证据链
- 根因与处理动作
- 复盘后的可复用方法

排障实战页不是配角，而是博客最能体现经验差异的内容。

### 5. 命令速查页

命令速查页仍然保留，但角色要下沉。

以后单个命令文章的定位是：

- 给专题页做补充
- 给问题页做延伸阅读
- 承担速查和参数说明
- 不是博客首页的主打内容

换句话说，命令速查页可以存在，但不再承担“主流量入口”的职责。

## 三、重组后的核心栏目

### 1. 慢系统与性能排查

解决的问题：

- 系统整体变慢
- CPU、内存、IO 到底谁是瓶颈
- 先看什么，后看什么

现有素材：

- `ps`
- `top`
- `iostat`
- 两篇 IO 排障实战文章

主专题建议：

- Linux 系统变慢时的排查顺序
- CPU 不高但系统很慢怎么办
- Linux 进程、负载和 IO 指标怎么一起看

### 2. 日志查看与文本定位

解决的问题：

- 日志太大怎么看
- 只想看最新错误日志怎么办
- 关键词定位、翻页、实时跟踪怎么组合使用

现有素材：

- `cat`
- `head`
- `tail`
- `less`
- `more`
- `grep`
- `vim`

主专题建议：

- Linux 日志查看命令怎么选：`cat`、`less`、`tail`、`grep`
- 排查报错时，日志定位的最快命令组合
- 大文件、大日志场景下的查看策略

### 3. 文件与目录操作

解决的问题：

- 在服务器上高效找文件、切目录、批量处理文件
- 复制、移动、删除时如何减少误操作

现有素材：

- `pwd`
- `cd`
- `ls`
- `find`
- `mkdir`
- `touch`
- `cp`
- `mv`
- `rm`

主专题建议：

- Linux 文件与目录操作高频命令实战
- `find`、`ls`、`cd` 怎么配合提高排查效率
- 生产环境删除、移动、复制文件的安全习惯

### 4. 磁盘与空间排查

解决的问题：

- 磁盘为什么满了
- 分区、挂载、目录占用到底怎么看
- `df`、`du`、`lsblk` 的使用边界是什么

现有素材：

- `df`
- `du`
- `lsblk`
- `fdisk`
- `mount`
- 查看文件目录大小

主专题建议：

- Linux 磁盘空间排查命令大全
- `df` 和 `du` 为什么看到的结果不一样
- 分区、挂载点和目录占用的完整判断顺序

### 5. 网络连通与端口排查

解决的问题：

- 服务为什么连不上
- 端口是否监听
- 网络是主机问题、链路问题还是服务问题

现有素材：

- `ping`
- `traceroute`
- `ss`
- `netstat`
- `nc`
- `curl`
- `wget`

主专题建议：

- Linux 网络连通问题排查命令清单
- 端口不通时先看 `ss` 还是先看 `ping`
- 本机监听、远端连通、应用返回怎么一步步确认

### 6. SSH 与远程运维

解决的问题：

- 远程登录失败
- 传文件慢或失败
- 免密登录、端口转发、远程同步怎么做

现有素材：

- `ssh`
- `scp`
- `rsync`

主专题建议：

- SSH 登录、免密和端口转发一文讲透
- Linux 远程传文件时，`scp` 和 `rsync` 怎么选
- 远程运维最常见的连接失败排查步骤

### 7. 权限、身份与提权

解决的问题：

- 当前是谁
- 为什么没有权限
- 用户、组、权限位和提权之间是什么关系

现有素材：

- `whoami`
- `id`
- `su`
- `sudo`
- `chmod`
- `chown`
- `chgrp`

主专题建议：

- Linux 用户、组和权限关系快速搞懂
- 没有权限时先查 `whoami`、`id` 还是 `sudo`
- `chmod`、`chown`、`chgrp` 的正确使用顺序

### 8. 文本处理与批量处理

解决的问题：

- 文本筛选、格式化、列提取、批量执行怎么组合
- 如何从日志和命令输出里快速拿到想要的数据

现有素材：

- `awk`
- `sed`
- `sort`
- `uniq`
- `wc`
- `cut`
- `xargs`

主专题建议：

- Linux 文本处理命令组合实战
- 处理日志和命令输出时，`awk`、`sed`、`cut` 怎么选
- 批量处理文件和批量执行命令的安全写法

### 9. 服务、软件与防火墙管理

解决的问题：

- 服务为什么没起来
- 包是否安装
- 防火墙规则是否拦住了流量

现有素材：

- `systemctl`
- `rpm`
- `firewalld`
- `iptables`

主专题建议：

- Linux 服务管理与开机自启排查
- 软件包、服务状态与防火墙规则的联动排查
- 服务启动失败时的最短排查路径

## 四、内容层级的新规则

以后内容按下面 4 类来判断要不要单独发：

### 1. 继续保留单独成篇

适用于：

- 搜索量高
- 参数和场景足够多
- 单篇可以写出明显深度

例如：

- `ssh`
- `grep`
- `find`
- `awk`
- `sed`
- `systemctl`
- `curl`
- `rsync`

### 2. 优先合并成专题

适用于：

- 单个命令本身不复杂
- 更适合跟相邻命令一起讲
- 单独成篇容易模板化

例如：

- `cat/head/tail/less/more`
- `pwd/cd/ls`
- `whoami/id/su/sudo`

### 3. 优先写成问题页

适用于：

- 用户搜索时天然是问句
- 最适合通过“一个问题 + 多个命令”来解决

例如：

- 日志太大怎么快速定位错误
- 磁盘没空间了先查哪里
- 端口不通要怎么判断是网络问题还是服务问题

### 4. 优先写成实战页

适用于：

- 有真实事故
- 有排查过程
- 有根因和复盘

这类文章应该持续增加，占比至少要明显高于现在。

## 五、首页展示顺序建议

首页不要再按命令分类平铺，而建议按用户最常遇到的问题排序：

1. 系统变慢怎么排查
2. 磁盘空间满了怎么处理
3. 日志怎么看得又快又准
4. 网络和端口不通怎么查
5. SSH 远程登录和传文件
6. 权限不足怎么判断
7. 服务启动失败怎么排查
8. 文本处理与批量处理

命令速查页放在每个专题页底部作为延伸阅读，不建议再放在首页主视觉区域。

## 六、下一阶段执行优先级

### 第一优先级：先做流量入口

先把最容易带来搜索流量和收藏的专题补起来：

- Linux 系统变慢时的排查顺序
- Linux 磁盘空间排查命令大全
- Linux 日志查看命令怎么组合使用
- Linux 网络连通与端口排查命令清单
- SSH 登录、免密和传文件实战

### 第二优先级：补问题页

围绕高频搜索问题扩写：

- `df` 和 `du` 为什么结果不一样
- `tail -f`、`less`、`grep` 在日志排查里怎么配合
- `sudo` 失败时要检查什么
- `ss` 和 `netstat` 在排障里怎么选

### 第三优先级：持续积累实战页

之后每次现场问题、线上故障、性能异常都尽量沉淀成复盘文，而不是只补命令详解。

## 七、非运维主题内容的处理建议

仓库里还有：

- `DBSCAN 算法详解：一文看懂密度聚类的核心思想`
- `Spark性能分析之group by与join谁更贵`

这类内容建议和 Linux 运维博客分开展示：

- 如果继续放在同一站点，至少单独设置栏目
- 不要和 Linux 命令内容混在同一个首页流里

这样能避免读者对账号定位产生混淆。

## 八、重组后的目标

重组之后，博客不再追求“覆盖更多命令名”，而追求三件事：

1. 读者遇到问题时，能快速找到解决路径
2. 一篇文章读完后，能自然继续读下一篇
3. 读者会觉得你提供的是运维判断经验，而不只是命令说明
