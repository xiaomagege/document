# Linux命令详解博客路线图

当前路线图已按现有内容完成情况更新，同时同步了博客标题规范。此前规划的五个优先级批次已经全部补齐，现阶段重点转为分类统一、标题统一和后续选题扩展。

## 一、当前状态

### 1. 已完成的命令分类

- 文件与文本查看：`cat`、`head`、`tail`、`less`、`more`、`vim`、`grep`、`find`
- 文件与目录：`ls`、查看文件目录大小
- 进程与性能：`ps`、`top`、`iostat`
- 网络与防火墙：`netstat`、`firewalld`、`iptables`
- 系统与软件管理：`rpm`、`systemctl`
- 排障实战：现场反馈产品系统慢的排查步骤、一次 IO 瓶颈导致系统缓慢排查记录
- 文件与目录基础命令：`pwd`、`cd`、`mkdir`、`touch`、`cp`、`mv`、`rm`
- 磁盘与空间排查命令：`df`、`du`、`lsblk`、`fdisk`、`mount`
- 文本处理与筛选命令：`awk`、`sed`、`sort`、`uniq`、`wc`、`cut`、`xargs`
- 网络与远程运维命令：`ss`、`curl`、`wget`、`ping`、`traceroute`、`nc`、`ssh`、`scp`、`rsync`
- 权限与用户管理命令：`chmod`、`chown`、`chgrp`、`whoami`、`id`、`su`、`sudo`

### 2. 优先级完成情况

- 第一优先级：已完成
- 第二优先级：已完成
- 第三优先级：已完成
- 第四优先级：已完成
- 第五优先级：已完成

## 二、标题规范

### 1. 命令类文章标题格式

统一格式：

```text
Linux<分类>：<命令名或文章主题>
```

示例：

- `Linux文件与目录基础命令：pwd命令详解`
- `Linux文件与目录基础命令：cd命令知多少？`
- `Linux磁盘与空间排查命令：df命令详解`
- `Linux文本处理与筛选命令：awk命令详解`
- `Linux网络与远程运维命令：ssh命令详解`
- `Linux权限与用户管理命令：sudo命令详解`

### 2. 排障类文章标题格式

统一格式：

```text
Linux排障实战：<文章主题>
```

示例：

- `Linux排障实战：现场反馈产品系统慢的排查步骤`
- `Linux排障实战：一次IO瓶颈导致系统缓慢排查记录`

### 3. 当前执行说明

- 文章正文一级标题已经按分类统一调整
- 文章文件名已经与一级标题统一
- 当前命令类与排障类文章均采用“标题即文件名”的方式维护
- 若仓库外部仍存在旧文件名引用，后续需要同步更新对应链接

## 三、统一写作模板

后续新增博客，统一沿用以下结构：

### 1. 命令类文章模板

1. 一、`<命令>` 是什么？
2. 二、常见使用语法与重点参数
3. 三、常用命令与使用场景
4. 四、常见问题与建议
5. 五、常用命令速查
6. 六、总结

### 2. 排障类文章模板

1. 一、问题现象
2. 二、排查思路
3. 三、具体排查过程
4. 四、根因分析
5. 五、解决办法
6. 六、经验总结

## 四、下一阶段建议

在当前基础分类完成后，下一阶段建议继续补充这些高频内容。

### 1. 压缩与归档命令

- `tar`
- `gzip`
- `gunzip`
- `zip`
- `unzip`

### 2. 进程与资源排查命令

- `free`
- `vmstat`
- `lsof`
- `kill`
- `killall`

### 3. 日志与任务命令

- `crontab`
- `journalctl`
- `dmesg`

### 4. 用户与会话命令

- `passwd`
- `groups`
- `w`
- `last`

### 5. 后续专题文章建议

- Linux 日志排查链路
- Linux 磁盘空间排查链路
- Linux 网络排查链路
- Linux 权限问题排查链路

## 五、建议的后续编写顺序

建议按下面顺序继续扩展：

1. `tar`
2. `gzip`
3. `gunzip`
4. `zip`
5. `unzip`
6. `lsof`
7. `free`
8. `vmstat`
9. `kill`
10. `crontab`
11. `journalctl`

## 六、维护说明

- 路线图后续应和仓库中的实际文章保持一致
- 每新增一个分类或一批文章时，同步更新本路线图
- 若后续决定统一重命名文件名，应单独规划一次链接兼容性检查
