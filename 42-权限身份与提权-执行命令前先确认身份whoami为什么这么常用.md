# 42｜权限、身份与提权｜执行命令前先确认身份，whoami 为什么这么常用

很多权限问题一开始就查偏了，不是因为命令不会用，而是因为连“现在到底是谁在执行命令”都没先确认。

`whoami` 看起来很小，但它经常是排障的第一步。脚本为什么手工执行正常、定时任务却失败，`sudo` 后到底是不是 root，切用户之后环境为什么不对，这些问题都值得先跑一遍 `whoami`。

如果只想先记 3 条：

- 排查权限问题时，先确认当前执行身份
- `whoami` 适合看“当前是谁”，`id` 适合继续看“当前属于哪些组”
- `sudo`、`su`、定时任务、脚本执行前后，都很值得补一条 `whoami`

## 一、什么情况下先用 `whoami`

`whoami` 最适合用在这些场景：

- 刚切完用户，想确认自己到底切到了谁
- `sudo` 执行完命令，想确认是不是已经以 root 身份运行
- 脚本手工执行和自动执行结果不一致，怀疑运行用户不同
- 文件提示 `Permission denied`，想先确认当前操作者是谁

它只回答一个问题：当前有效用户是谁。

```bash
whoami
```

如果你只需要这一条信息，`whoami` 比 `id` 更直接。

## 二、先记住这几条命令

```bash
whoami
sudo whoami
su - deploy
whoami
echo "run as: $(whoami)"
id
```

它们分别适合：

- `whoami`：看当前有效用户
- `sudo whoami`：确认 sudo 后是不是 root
- `su - deploy` + `whoami`：确认切用户是否成功
- `echo "run as: $(whoami)"`：在脚本里输出执行身份
- `id`：继续看 UID、GID 和所属组

## 三、排查权限问题时，怎么把它用对

### 1. 先确认当前 shell 身份

```bash
whoami
```

很多误判都发生在这里。你以为自己在 `deploy` 用户下，实际还在 `root`；或者你以为脚本是 root 执行，实际是普通用户在跑。

### 2. 切用户之后立刻确认

```bash
su - deploy
whoami
pwd
```

这一步很值钱，因为它能顺手确认两件事：

- 用户切换是否成功
- 当前目录和环境是不是已经变成目标用户的登录环境

### 3. `sudo` 前后各看一眼

```bash
whoami
sudo whoami
```

第一条看你当前是谁，第二条看 sudo 后是谁。很多“为什么这个命令手工能执行、脚本里不行”的问题，都和这里的身份差异有关。

### 4. 脚本和定时任务里主动打印身份

```bash
echo "run as: $(whoami)"
echo "pwd: $(pwd)"
```

如果脚本在命令行能跑，到了 `cron`、CI 或 systemd 任务里不对，先把执行身份和目录打出来，通常比盲猜更快。

## 四、`whoami`、`who`、`id` 怎么分工

### 1. `whoami`

只看当前有效用户是谁。

### 2. `id`

看得更完整：

```bash
id
id -un
id -Gn
```

它适合继续判断：

- 当前用户属于哪些组
- 为什么明明用户名对了，但还是没权限
- 当前是不是 root

### 3. `who`

看当前有哪些登录会话，而不是看“我是谁”。

```bash
who
```

所以三者不要混用。

## 五、最常见的误区

### 1. 只看用户名，不看是否真的换了执行环境

`su deploy` 和 `su - deploy` 的环境并不一样。只知道用户名还不够，最好顺手再看 `pwd`、`env` 或 `id`。

### 2. 以为手工执行和自动执行一定是同一个用户

这在排查脚本、CI、定时任务失败时尤其常见。自动化场景下，先确认用户再说。

### 3. 把 `whoami` 当成完整权限判断

`whoami` 只能告诉你是谁，不能告诉你属于哪些组，也不能直接告诉你为什么没权限。真正往下查时，通常要接着看：

```bash
id
ls -l 文件
ls -ld 目录
```

## 六、结论

`whoami` 本身很小，但它在权限排查里是很稳的起点。

先确认“谁在执行”，再去看组、属主和权限位，很多原本会绕远路的问题会简单很多。
