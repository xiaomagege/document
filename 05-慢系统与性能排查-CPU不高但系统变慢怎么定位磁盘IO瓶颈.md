# 05｜慢系统与性能排查｜CPU不高但系统变慢，怎么定位磁盘 IO 瓶颈？

这篇不再展开“慢系统通用排查顺序”，只记录一次真实故障里最关键的判断节点：为什么没有继续怀疑 CPU，为什么会把方向迅速收敛到磁盘 IO，以及现场是怎么止损的。

## 一、现场最先暴露出来的不是报错，而是“整体发卡”

最初收到的反馈并不复杂：

- 页面加载明显变慢
- 接口超时开始增多
- SSH 还能登录，但命令回显发卡
- 业务没有成片报错，机器也没有直接表现出 CPU 打满

这类故障麻烦的地方就在这里。它不像进程崩掉那样直接，也不像 CPU 100% 那样一眼能看出来，表面上更像“机器整体变钝了”。

## 二、第一眼先看 `top -c`，但这次我只关心 3 个信号

第一步执行：

```bash
top -c
```

这次没有急着盯某个进程的 CPU 占用，而是先看下面 3 件事：

1. CPU 行里的 `wa` 有没有明显抬高
2. 进程状态里有没有持续出现 `D`
3. `id` 是否还保留了不少空闲

现场给出的信号很明确：

- `id` 还有空闲，说明 CPU 没有真正算满
- `wa` 偏高，说明机器有明显时间花在等待 IO
- 进程列表里能看到 `D` 状态，说明线程卡在不可中断等待

到这一步，我已经不再把重点放在“CPU 不够”上，而是开始怀疑磁盘或存储链路。

## 三、为什么没有继续在 CPU 路线上深挖

接着我用了两条命令做排除：

```bash
ps aux --sort=-%cpu | head -10
ps -eo pid,ppid,user,%cpu,%mem,stat,cmd --sort=-%cpu | head
```

我想确认的是：

- 有没有一个足够夸张的热点进程，能单独解释整机变慢
- `D` 状态是不是偶发，而不是持续出现

现场没有看到那种“一个进程把 CPU 长时间吃满”的情况，但等待状态的进程是持续存在的。也就是说，系统变慢并不是因为机器忙着算，而更像是机器被卡在某个等待链路上。

内存侧也没有出现足够强的异常信号，比如 `Swap` 快速抬升或者大内存进程突然冲上来，所以这时候继续往 CPU 或内存方向深挖，收益已经不高了。

## 四、真正把方向钉死的是 `iostat`

接着执行：

```bash
iostat -y -x 1 3
```

现场最关键的输出是：

```text
avg-cpu:  %user  %nice %system %iowait  %steal %idle
           6.12    0.00    2.31    18.45    0.00   73.12

Device            r/s   w/s   rkB/s   wkB/s  rrqm/s wrqm/s await  r_await  w_await  svctm  %util
sda              120.3  85.6   5200    3400     2.1   1.3  45.6     38.2     56.4     3.2   98.7
```

这个结果出来以后，方向基本就定了：

- `%iowait=18.45%`：CPU 的确在大量等待 IO
- `%util=98.7%`：设备几乎一直处在繁忙状态
- `await=45.6ms`：单次 IO 等待已经明显偏高
- `w_await > r_await`：写路径比读路径更慢

这里最关键的不是“磁盘忙”这件事本身，而是它和前面 `top` 里看到的 `wa`、`D` 状态能完全对上。证据开始闭环了。

## 五、最后一步不是再猜，而是把高写入进程抓出来

继续执行：

```bash
iotop -o -d 1
```

这一步我只想回答一个问题：到底是谁在持续写盘。

现场最终看到的是：

- 某个业务进程持续占用较高写 IO
- 高峰时段写入波动明显
- 读写里写路径更重，和 `iostat` 里 `w_await` 偏高的现象一致

到这里，问题已经不是“是不是 IO”了，而是“哪类业务动作把写盘打满了”。

## 六、根因不是单点故障，而是写入叠加

继续往业务动作上对，最后把问题收敛到两件事叠在一起：

- 高频日志刷盘
- 突发写入任务在高峰时段叠加

单看其中任何一项，都未必会立刻把机器拖慢；但它们落在同一块盘、同一段时间里，就会把写路径顶到很高的位置。前面看到的 `%util`、`await` 和 `w_await`，正好解释了为什么接口会慢、为什么命令回显会发卡、为什么 CPU 明明不高但机器还是不好用。

## 七、现场处理动作

### 1. 先止损

现场先做的是低风险动作，把系统从“持续发卡”拉回可用：

- 降低非关键日志级别
- 减少同步刷盘频率
- 暂停高峰期的重 IO 后台任务
- 对突刺的写入流量做临时限流

### 2. 再做长期优化

系统恢复后，再处理长期问题：

- 日志与数据盘分离，减少争抢
- 高写入路径做批量化、异步化
- 对 `%iowait`、`%util`、`await`、`D` 状态进程数建立基线告警

## 八、处理后的回看

止损动作生效后，现场很快看到几个变化：

- `%iowait` 回落
- `await` 降低，`%util` 不再长期顶高
- 接口响应恢复，超时率下降

这说明前面的判断是对的，问题主因确实在写入侧，而不是 CPU 或内存。

## 九、这次案例里最值钱的 4 个判断点

这篇真正值得记住的，不是又多背了几条命令，而是下面 4 个判断：

1. 系统慢，但 CPU 不高，不要自动把问题归到“机器没压力”
2. `wa` 抬高加上持续的 `D` 状态，已经足够把怀疑点转到 IO
3. `iostat` 不是补充材料，而是这次真正把方向钉死的证据
4. 线上先止损，先把磁盘从高写入里拉出来，再谈长期优化

这就是这次故障里最核心的收获：系统“慢”不一定是在“忙”，它也可能是在“等”，而这两者的处理方向完全不同。

