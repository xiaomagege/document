# 磁盘与空间排查命令：df命令详解

在 Linux 服务器运维中，磁盘空间是否充足是非常基础但也非常关键的问题。应用无法写日志、数据库无法落盘、容器镜像拉取失败、构建产物生成失败，很多问题最后都会指向同一个原因：文件系统空间不够。

`df` 就是用来查看文件系统磁盘空间使用情况的常用命令。它关注的是“挂载点还有多少空间”，适合快速判断整台机器或某个分区是否快满。

---

## 一、`df` 是什么？

`df` 是 disk free 的缩写，用于显示文件系统的磁盘空间使用情况。

它主要用于：

- 查看每个挂载点的总容量、已用容量和可用容量
- 判断根分区 `/`、数据盘 `/data`、日志盘 `/var` 是否快满
- 查看文件系统类型
- 查看 inode 使用情况
- 排查“磁盘明明有空间但无法创建文件”的问题

需要注意的是：

- `df` 看的是文件系统维度，不是某个目录内部具体哪个文件占空间
- 想继续定位大目录或大文件，通常要配合 `du` 使用
- 如果文件被删除但仍被进程占用，`df` 显示的可用空间可能不会马上释放

---

## 二、常见使用语法与重点参数

```bash
df [选项] [文件或目录]
```

常用参数说明：

- `-h`：以人类可读方式显示容量，例如 `G`、`M`
- `-T`：显示文件系统类型
- `-i`：显示 inode 使用情况
- `-a`：显示所有文件系统，包括特殊文件系统
- `-P`：使用 POSIX 输出格式，适合脚本处理
- `--total`：显示总计信息
- `-x 类型`：排除指定文件系统类型
- `-t 类型`：只显示指定文件系统类型

常见示例：

```bash
df -h
df -Th
df -ih
df -h /data
df -h --total
```

---

## 三、常用命令与使用场景

### 1. 查看所有文件系统磁盘使用情况

```bash
df -h
```

这是最常用的写法。输出中通常包含：

- `Filesystem`：文件系统或设备名
- `Size`：总容量
- `Used`：已使用容量
- `Avail`：可用容量
- `Use%`：使用率
- `Mounted on`：挂载点

排查磁盘空间问题时，优先看 `Use%` 和 `Mounted on` 两列。

### 2. 查看文件系统类型

```bash
df -Th
```

会额外显示 `Type` 列，例如 `ext4`、`xfs`、`tmpfs`、`overlay` 等。

这在排查容器、临时文件系统、网络挂载或磁盘格式问题时很有用。

### 3. 查看某个目录所在分区空间

```bash
df -h /var/log
```

这个命令显示的不是 `/var/log` 目录大小，而是 `/var/log` 所在文件系统的空间使用情况。

例如日志写不进去时，可以先看：

```bash
df -h /var/log
```

如果该挂载点使用率接近 100%，就需要清理日志或扩容。

### 4. 查看根分区空间

```bash
df -h /
```

根分区空间不足会影响系统运行、包安装、临时文件创建和服务启动。

生产环境中建议持续关注 `/` 的使用率，避免根分区被日志、缓存或临时文件打满。

### 5. 查看 inode 使用情况

```bash
df -ih
```

如果磁盘容量还有剩余，但创建文件时报错 `No space left on device`，可能是 inode 被耗尽。

inode 常见耗尽场景包括：

- 小文件数量特别多
- 缓存目录没有清理
- 邮件队列、会话文件、临时文件堆积
- 日志按非常细粒度切分

这时需要看 `IUse%` 是否接近 100%。

### 6. 显示所有挂载项

```bash
df -ah
```

`-a` 会把一些特殊文件系统也显示出来，例如 `proc`、`sysfs`、`tmpfs` 等。

日常排查普通磁盘空间时通常不需要 `-a`，但排查容器或系统挂载细节时会有帮助。

### 7. 排除指定文件系统类型

```bash
df -h -x tmpfs
```

可以过滤掉 `tmpfs` 这类临时文件系统，让输出更聚焦。

如果还想排除多个类型，可以重复使用：

```bash
df -h -x tmpfs -x devtmpfs
```

### 8. 查看总计容量

```bash
df -h --total
```

会在最后显示一行 `total`，便于快速了解整体磁盘空间占用。

不过生产环境中更重要的是单个关键挂载点是否充足，而不是总容量是否够用。

### 9. 在脚本中使用稳定格式

```bash
df -P /
```

`-P` 使用更稳定的输出格式，适合脚本解析。

例如只取根分区使用率：

```bash
df -P / | awk 'NR==2 {print $5}'
```

---

## 四、常见问题与建议

### 1. `df` 和 `du` 看到的结果为什么不一样？

`df` 统计文件系统层面的已用空间，`du` 统计目录中文件占用空间。

两者不一致常见原因包括：

- 文件被删除，但仍被进程打开
- 挂载点覆盖了原目录内容
- 权限不足导致 `du` 没统计到部分目录
- 文件系统预留空间或元数据占用

如果 `df` 显示空间没释放，可以使用：

```bash
lsof | grep deleted
```

查看是否有已删除但仍被进程占用的文件。

### 2. 磁盘有空间，为什么还提示没有空间？

可能是 inode 用完了。检查方式：

```bash
df -ih
```

如果 inode 使用率满了，需要清理大量小文件，而不是只关注大文件。

### 3. `tmpfs` 使用率高需要担心吗？

`tmpfs` 是内存型文件系统，常见于 `/run`、`/dev/shm` 等目录。

是否需要处理取决于具体挂载点和业务场景。普通磁盘空间排查时，可以先排除它：

```bash
df -h -x tmpfs -x devtmpfs
```

### 4. 生产环境中如何使用 `df`？

建议重点关注：

- `/` 根分区
- `/var` 或 `/var/log` 日志分区
- `/data` 数据分区
- 数据库数据目录所在分区
- 容器运行目录所在分区

常用检查命令：

```bash
df -Th
df -ih
```

---

## 五、常用命令速查

```bash
df -h
df -Th
df -ih
df -h /
df -h /var/log
df -h /data
df -ah
df -h --total
df -h -x tmpfs -x devtmpfs
df -P /
df -P / | awk 'NR==2 {print $5}'
```

---

## 六、总结

`df` 是查看 Linux 文件系统空间使用情况的基础命令。日常排查磁盘问题时，建议优先掌握：

```bash
df -h
df -Th
df -ih
df -h /path
```

简单来说，`df` 用来判断“哪个挂载点快满了”，`du` 用来继续定位“哪个目录或文件占空间”。线上环境中如果遇到空间不足，先用 `df -h` 找到问题分区，再用 `du` 深入分析，会更加高效。