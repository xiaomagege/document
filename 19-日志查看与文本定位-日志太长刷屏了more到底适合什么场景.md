# 19｜日志查看与文本定位｜日志太长刷屏了，more 到底适合什么场景

`more` 现在已经不是最强的分页工具了，但它仍然有一个现实价值：

**在老环境、精简环境、或者你只想快速分页看一眼的时候，它够用，而且几乎到处都有。**

如果只想先记 3 条：

- 只想分页看长输出，`more` 够用
- 想更灵活地翻页、回退、搜索，优先换 `less`
- `more` 适合“快速看一下”，不适合复杂排查

## 一、什么时候还会用到 `more`

这些场景里，`more` 仍然有位置：

- 登录到很基础的 Linux/Unix 环境
- 只想把长输出按页看，不想整屏刷满
- 想简单浏览系统日志、配置或命令输出
- 环境里 `less` 不一定可用，或者你只想用最基础的分页器

一句话记住：

**`more` 是“够用型分页器”。**

## 二、最常用的几条用法

```bash
more /var/log/messages
dmesg | more
more +/ERROR app.log
more -20 app.log
```

它们分别适合：

- `more file`：分页看文件
- `命令 | more`：分页看长输出
- `more +/ERROR file`：从关键字附近开始看
- `more -20 file`：每页显示 20 行

## 三、进入 `more` 后最常用的按键

- `Space`：下一页
- `Enter`：下一行
- `/关键字`：向下搜索
- `n`：下一个命中
- `q`：退出

有些实现支持：

- `b`：上一页

但这点在不同系统上不完全一致，所以如果你特别依赖回退，还是更推荐 `less`。

## 四、排查里最常见的 3 个场景

### 1. 分页看系统日志

```bash
more /var/log/syslog
```

适合：

- 日志不算太大
- 只是先看一眼
- 不需要复杂跳转

### 2. 避免命令输出直接刷屏

```bash
ps aux | more
```

或者：

```bash
dmesg | more
```

这类场景下，`more` 就是在做最朴素的事情：让输出别一下子冲满终端。

### 3. 从错误关键字开始看

```bash
more +/ERROR /var/log/nginx/error.log
```

这在排查时挺顺手，因为你不用从开头一点点翻到第一个报错。

## 五、`more` 和 `less` 怎么选

你可以直接这样记：

- 只想快速分页看一眼：`more`
- 需要更自由地翻、搜、回退：`less`

现在大多数真正的排查场景里，`less` 会更常用；`more` 更像一个简单、保底、几乎到处都在的基础工具。

## 六、最常见的误区

### 1. 把 `more` 当成和 `less` 一样灵活

它不是。`more` 更轻、更简单，但功能边界也更窄。

### 2. 大日志深度排查还坚持用 `more`

如果你需要反复搜索、前后切换、精细跳转，早点换成 `less` 会省很多时间。

### 3. 忘了它是“分页器”，不是搜索器

`more` 可以搜，但搜索体验并不算它的强项。关键字很多、需要反复跳转时，`grep` 或 `less` 更合适。

## 七、结论

`more` 适合“快速分页看长输出”，尤其适合基础环境和临时浏览。

它不该承担复杂排查的主力角色，但作为一个随处可用的基础分页器，依然很实用。
