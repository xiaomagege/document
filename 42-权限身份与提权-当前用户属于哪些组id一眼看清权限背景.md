# 42｜权限、身份与提权｜当前用户属于哪些组？id 一眼看清权限背景

很多权限问题不是“没有这个用户”，而是“这个用户不在对的组里”。

这也是为什么 `id` 在 Linux 排障里很重要。只看 `whoami` 只能知道当前是谁，而 `id` 能继续告诉你 UID、主组、附加组，以及为什么这个用户看起来正常却还是没权限。

如果只想先记 3 条：

- 文件权限判断前，先看一次 `id`
- `id -u` 常用来判断当前是不是 root
- `id -Gn` 很适合快速确认用户属于哪些组

## 一、什么情况下先用 `id`

`id` 最适合这些场景：

- 用户明明存在，但访问目录或写文件仍然失败
- 想确认当前是不是 root
- 想确认某个用户属于哪些组
- 想判断权限问题该改组、改属主，还是改权限位

最常用的第一条就是：

```bash
id
```

它会一次性把 UID、GID 和附加组都展示出来。

## 二、先记住这几条命令

```bash
id
id deploy
id -u
id -un
id -gn
id -Gn
```

它们分别适合：

- `id`：看当前用户完整身份背景
- `id deploy`：看指定用户的 UID、GID 和组
- `id -u`：只看 UID，脚本里判断 root 很常用
- `id -un`：只看用户名
- `id -gn`：只看主组名称
- `id -Gn`：看所有组名称

## 三、权限排查时，重点看哪几项

### 1. 先确认当前是谁

```bash
id
```

这一步最重要的不是背输出格式，而是快速回答：

- 当前用户是谁
- 当前主组是什么
- 当前是否在预期的附加组里

### 2. 判断当前是不是 root

```bash
id -u
```

如果输出是 `0`，说明当前是 root。

脚本里很常见的写法是：

```bash
if [ "$(id -u)" -ne 0 ]; then
  echo "please run as root"
  exit 1
fi
```

### 3. 判断用户是否属于目标组

```bash
id -Gn deploy
```

比如你怀疑 `deploy` 用户没有写 `/data/app` 的权限，第一反应可以先看它是不是属于应用目录对应的组。

### 4. 配合 `ls -l` 一起判断

```bash
id
ls -l app.log
ls -ld /data/app
```

这一组命令很适合一起看，因为它能把四个关键点连起来：

- 当前用户是谁
- 当前用户属于哪些组
- 文件属主和属组是谁
- 文件和目录权限位是什么

## 四、`id` 最适合帮你做什么判断

### 1. 是不是该改组，而不是改权限

很多人一看到 `Permission denied` 就想 `chmod 777`。但如果 `id` 一看发现用户本来就该进某个组，那通常更合理的动作是修正组关系，而不是直接放大权限。

### 2. 是不是该改属主

如果当前用户和目录属主完全不匹配，且业务上本就应该由这个用户持有目录，那更可能是 `chown` 的问题。

### 3. 是不是会话还没刷新

用户刚加入新组后，当前 shell 里不一定立刻生效。这时 `id -Gn` 如果还看不到新组，往往需要重新登录再确认。

## 五、最常见的误区

### 1. 只看 `whoami`，不看所属组

你知道当前用户叫 `deploy`，并不代表它一定能访问应用目录。真正决定权限的，往往还包括组关系。

### 2. 组已经加了，但当前会话还没生效

这在改完用户组后很常见。不要急着怀疑命令没生效，先重新登录再看一次 `id`。

### 3. 看到数字就跳过

`uid=1001`、`gid=1001` 看起来像细节，但在跨主机同步、NFS、容器挂载等场景里，数字 ID 本身就可能是问题线索。

## 六、结论

`id` 是权限排查里非常值得优先掌握的一条命令。

先用它看清当前用户、主组和附加组，再决定是改 `sudo`、改组、改属主，还是改权限位，判断会稳很多。
