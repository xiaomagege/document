# 40｜服务、软件与防火墙｜软件包装没装、版本对不对，rpm 怎么快速确认

在 RPM 系发行版里，`rpm` 最常见的价值，不是“装包”，而是“先把包的状态查清”。

软件到底装没装、装的是什么版本、某个文件归哪个包管、离线包里到底带了什么，这些问题都很适合先用 `rpm` 来回答。

如果只想先记 3 条：

- 查包是否已安装，先 `rpm -q 包名`
- 查某个文件归谁管，先 `rpm -qf 文件路径`
- 真正装依赖复杂的软件时，优先 `yum` 或 `dnf`

## 一、什么情况下先用 `rpm`

`rpm` 最适合这些场景：

- 想确认某个包装没装
- 想确认版本对不对
- 想看某个命令或配置文件属于哪个包
- 想离线看 `.rpm` 文件里有什么

最常见的起手式就是：

```bash
rpm -q nginx
rpm -qi nginx
rpm -qf /usr/bin/vim
```

## 二、先记住这几条命令

```bash
rpm -q nginx
rpm -qi nginx
rpm -ql nginx
rpm -qf /usr/bin/vim
rpm -qpi package.rpm
rpm -qpl package.rpm
```

它们分别适合：

- 查包是否已安装
- 查已安装包的详细信息
- 查包安装了哪些文件
- 反查文件归属
- 查未安装包的元信息
- 查未安装包里的文件列表

## 三、排查时，它最常帮你解决什么问题

### 1. 这个包到底装没装

```bash
rpm -q nginx
```

这比 `rpm -qa | grep nginx` 更直接，也更适合脚本判断。

### 2. 装的是哪个版本

```bash
rpm -qi nginx
```

排查“为什么线上和测试行为不一样”时，版本差异经常就是线索。

### 3. 某个文件到底属于哪个包

```bash
rpm -qf /usr/bin/vim
```

这在排查：

- 某个命令来自哪个包
- 某个配置文件被谁管理
- 卸载或升级会影响什么

时非常有价值。

## 四、离线包排查时怎么用

### 1. 看包信息

```bash
rpm -qpi nginx.rpm
```

### 2. 看包内容

```bash
rpm -qpl nginx.rpm
```

如果你还没装包，但想先确认它会把文件放到哪，这两条很顺手。

## 五、`rpm`、`yum`、`dnf` 怎么分工

### 1. `rpm`

更适合：

- 查状态
- 查文件归属
- 查离线包内容
- 精细控制本地包

### 2. `yum` / `dnf`

更适合：

- 正式安装
- 升级
- 自动解决依赖

如果软件依赖复杂，直接拿 `rpm -ivh` 硬装往往不是最省心的路。

## 六、最常见的误区

### 1. 把 `rpm` 当成所有装包场景的首选

查询很适合，装复杂包时不一定。

### 2. 不查文件归属就手工删文件

很多系统文件其实是包管理的一部分，先 `rpm -qf` 一下通常更稳。

### 3. 只查“有没有包”，不查版本

很多问题不是“没装”，而是“装的版本不对”。

## 七、结论

`rpm` 最强的地方，不是装包动作本身，而是它能把“包状态”和“文件归属”说清楚。

所以查安装、查版本、查文件归属时优先想到它；真正做依赖复杂的安装升级时，再让 `yum` 或 `dnf` 上场。
