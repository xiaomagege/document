# 29｜网络连通与端口排查｜端口不通时，ss 为什么比 netstat 更值得先看

端口不通时，最怕的是还没分清问题在哪一层，就开始猜防火墙、猜服务、猜网络。

`ss` 的价值就在于，它能很快回答最关键的第一批问题：

- 服务到底有没有监听
- 监听的是 `127.0.0.1` 还是 `0.0.0.0`
- 端口是不是已经被别的进程占了
- 当前连接状态是不是异常

如果只想先记 3 条：

- 先看监听：`ss -lnt`
- 想知道是哪个进程占端口：`sudo ss -lntp`
- 端口不通时，先用 `ss` 确认服务端状态，再去怀疑防火墙和链路

## 一、什么时候先用 `ss`

这些场景里，`ss` 基本都应该是第一步：

- 服务启动了，但访问不了
- 想确认某个端口有没有监听
- 怀疑端口被其他进程占了
- 想看连接数是不是异常高
- 想判断问题是服务没起、地址绑错，还是外部访问链路有问题

`ss` 最适合回答的是：

**这台机器上的 socket 现在是什么状态？**

## 二、先记住这几条命令

```bash
ss -lnt
sudo ss -lntp
ss -ant
ss -ant state established
ss -s
ss -4 -lnt
ss -6 -lnt
```

它们分别适合：

- `ss -lnt`：看 TCP 监听端口
- `ss -lntp`：看监听端口对应进程
- `ss -ant`：看所有 TCP 连接
- `state established`：只看已建立连接
- `ss -s`：看连接统计摘要
- `-4`、`-6`：分开看 IPv4 / IPv6

## 三、端口不通时，推荐先这样排查

### 1. 先确认服务有没有监听

```bash
ss -lnt
```

如果目标端口根本没在监听，问题就不是“网络打不通”，而是服务端自己还没准备好。

### 2. 再确认是谁在监听

```bash
sudo ss -lntp | grep ':8080'
```

这一步可以回答：

- 是不是目标服务在占这个端口
- 有没有被别的进程抢占
- 是不是根本监听在错误端口上

### 3. 再看监听地址对不对

这一点特别关键：

- `0.0.0.0:8080`：通常表示对外可达
- `127.0.0.1:8080`：通常只本机可达

很多“服务明明启动了但外部访问不了”的问题，根因就是它只绑定在回环地址。

### 4. 如果监听没问题，再看连接状态

```bash
ss -ant
```

或者只看已建立连接：

```bash
ss -ant state established
```

这一步适合判断：

- 有没有连接进来
- 连接是不是都卡在异常状态
- 当前连接数是否明显偏高

## 四、最常见的 4 个判断场景

### 1. 服务启动了，但端口没监听

优先查服务日志和启动参数，而不是先查防火墙。

### 2. 端口在监听，但只监听本机

这通常是服务绑定地址问题，不一定是网络问题。

### 3. 端口在监听，也有连接，但访问仍然异常

这时再继续往上看：

- 应用层返回
- 反向代理配置
- 防火墙和安全组
- 上游链路

### 4. `TIME-WAIT`、`ESTAB` 数量异常

这更像连接管理或流量特征问题，不一定是“服务没起”。

先用：

```bash
ss -s
```

看总体状态，再决定是否继续细查。

## 五、`ss` 和 `netstat` 怎么分工

现在大多数场景里，更推荐 `ss`。

你可以直接这样理解：

- 老习惯：`netstat`
- 现在更常用、更快的替代：`ss`

常见替换关系：

```bash
netstat -lntp  ->  ss -lntp
netstat -ant   ->  ss -ant
netstat -s     ->  ss -s
```

## 六、最常见的误区

### 1. 端口不通就先查防火墙

如果服务端根本没监听，防火墙不是第一层问题。

### 2. 只看端口号，不看监听地址

端口存在不代表外部一定能访问，绑定地址经常是关键差异。

### 3. 没权限就以为没有进程信息

看不到 `-p` 里的进程信息，很多时候只是权限不够：

```bash
sudo ss -lntp
```

## 七、结论

`ss` 最适合做服务端口问题的第一轮判断。

先确认监听、进程、地址和连接状态，再决定要不要继续查防火墙、链路和应用层，这样排查会快很多。
