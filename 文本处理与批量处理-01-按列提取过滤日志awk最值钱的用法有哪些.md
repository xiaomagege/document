# 文本处理与批量处理01：按列提取、过滤日志，awk 最值钱的用法有哪些

`awk` 很容易让人又爱又怕。爱的是它几乎什么文本都能切，怕的是一看到 `$1`、`$2`、`NF` 就觉得门槛高。

其实在运维场景里，`awk` 最值钱的并不是“能写复杂脚本”，而是它能帮你把日志和命令输出按列切开，然后做提取、过滤、统计和简单计算。

如果只想先记 3 条：

- `awk` 最适合按列提取和按条件筛选
- `$1`、`$NF`、`NR`、`NF` 是最值得先记住的几个变量
- 字段分隔符不一样时，先想 `-F`

## 一、什么情况下先用 `awk`

`awk` 最适合这些场景：

- 想从命令输出里抽某几列
- 想按字段条件过滤日志
- 想统计某一列的次数、总和、平均值

最常见的起手式是：

```bash
awk '{print $1}' file
awk '$3 > 100 {print $0}' file
awk -F ':' '{print $1}' /etc/passwd
```

## 二、先记住这几条命令

```bash
awk '{print $1, $2}' access.log
awk -F ':' '{print $1, $7}' /etc/passwd
awk 'NR > 1 {print $0}' data.txt
awk '{print $NF}' file.txt
awk '{sum += $1} END {print sum}' nums.txt
```

它们分别适合：

- 打印前两列
- 指定分隔符后取列
- 跳过表头
- 取最后一列
- 做求和统计

## 三、日志和命令输出里，最常用的几种打法

### 1. 按列提取

```bash
awk '{print $1, $9}' file.txt
```

这类写法最适合处理：

- `ls -l`
- 日志固定列
- 命令输出表格

### 2. 按条件过滤

```bash
awk '$5 > 80 {print $0}' disk.txt
awk '$1 == "ERROR" {print $0}' app.log
```

如果你不是只想“搜关键字”，而是想“根据某一列的值做判断”，`awk` 往往比 `grep` 更合适。

### 3. 取最后一列

```bash
awk '{print $NF}' file.txt
```

这在列数不完全固定时特别好用，因为你不需要预先知道到底有多少列。

### 4. 跳过表头

```bash
awk 'NR > 1 {print $0}' data.txt
```

很多统计文件第一行都是标题，这时先跳过再处理会干净很多。

## 四、什么时候该先想 `-F`

`awk` 默认按空白字符切列，但很多系统文件不是这么分的。

比如：

```bash
awk -F ':' '{print $1, $7}' /etc/passwd
awk -F ',' '{print $1, $3}' data.csv
```

如果列怎么都取不对，第一反应通常不是“表达式错了”，而是“分隔符想错了”。

## 五、统计类用法为什么很值钱

### 1. 求和

```bash
awk '{sum += $1} END {print sum}' nums.txt
```

### 2. 求平均

```bash
awk '{sum += $1} END {if (NR > 0) print sum / NR}' nums.txt
```

### 3. 统计次数

```bash
awk '{count[$1]++} END {for (k in count) print k, count[k]}' access.log
```

这类统计对日志和命令输出都很实用，尤其是你想先快速看分布，不想上更重的分析工具时。

## 六、最常见的误区

### 1. 字段分隔符想当然

不是所有文件都按空格切列，`/etc/passwd`、CSV、自定义日志格式都可能完全不同。

### 2. 列位置没确认就直接写表达式

先抽一两行出来看看真实列结构，再写过滤条件，通常更稳。

### 3. 拿 `awk` 做纯关键字查找

能做，但如果你只是想搜关键字，`grep` 更直接。`awk` 的价值在“列”和“条件”。

## 七、结论

`awk` 最值得掌握的，不是复杂语法，而是它在“按列提取、按条件过滤、做简单统计”这三件事上的效率。

先记住 `$1`、`$NF`、`NR`、`-F`，很多日志处理场景就已经够用了。

