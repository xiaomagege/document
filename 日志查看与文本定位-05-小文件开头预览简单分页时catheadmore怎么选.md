# 日志查看与文本定位05：小文件、开头预览、简单分页时，cat、head、more 怎么选？

很多人对 `cat`、`head`、`more` 都不陌生，但真正上手排查时，问题往往不是“命令会不会用”，而是：

**眼前这段输出，到底该先用哪一个看。**

如果只想先记 3 条：

- 小文件想完整看一眼，先用 `cat`
- 只想看前几行或预览输出结构，用 `head`
- 长输出不想整屏刷满，只想临时分页时，用 `more`

## 一、先别背参数，先记住怎么选

这 3 个命令各自最适合解决的，其实是不同的小问题：

- `cat`：文件很小，想直接完整看一遍
- `head`：只想先看开头几行，确认格式和结构
- `more`：输出太长，先按页浏览，不想一下刷满终端

如果你已经知道自己需要搜索、翻页回退、来回跳转，那通常应该直接切到 `less`；如果你只关心最新几行，就应该去用 `tail`。

## 二、先记住这几条高频用法

```bash
cat app.conf
cat -n app.conf
cat -A app.conf
head -n 20 app.log
ps -ef | head
more /var/log/messages
dmesg | more
more +/ERROR app.log
```

它们分别适合：

- `cat file`：快速完整看一个小文件
- `cat -n`：带行号查看
- `cat -A`：看不可见字符、换行和制表符
- `head -n 20 file`：只看前 20 行
- `命令 | head`：预览长输出开头
- `more file`：分页看长文件
- `命令 | more`：分页看长输出
- `more +/ERROR file`：从关键字附近开始看

## 三、这 3 个命令最常见的场景

### 1. 小配置、小结果，先完整扫一眼

```bash
cat app.conf
cat -n app.conf
```

适合：

- 配置文件很小
- 只是确认内容有没有写进去
- 需要快速对行号

如果你怀疑格式有问题，比如多了空格、制表符或者换行不对，再补一条：

```bash
cat -A app.conf
```

### 2. 只想看开头，先别把整份文件都打开

```bash
head -n 20 access.log
head -n 5 data.csv
ps -ef | head
```

适合：

- 想看配置开头长什么样
- 想预览 CSV/TSV 表头
- 想先确认某条命令输出结构对不对

`head` 解决的是“先看开头一点点”，不是“深入排查整个文件”。

### 3. 输出太长又不需要复杂搜索，先简单分页

```bash
more /var/log/messages
dmesg | more
```

适合：

- 老环境里先把长输出按页看一眼
- 只是临时浏览，不需要来回翻
- 想避免日志或命令结果直接把终端刷满

如果你已经知道自己要看某类报错，可以直接从关键字附近开始：

```bash
more +/ERROR app.log
```

## 四、它们和 `less`、`tail`、`grep` 怎么分工

你可以直接这样记：

- 完整看小文件：`cat`
- 只看开头：`head`
- 临时分页：`more`
- 大文件翻页和搜索：`less`
- 看最新变化：`tail`
- 按关键字缩小范围：`grep`

很多人把这几个命令混着用，真正值钱的不是“把所有参数都记住”，而是知道什么时候该立刻换工具。

## 五、最常见的误区

### 1. 大日志还在硬用 `cat`

这通常只会把终端刷满，真正有用的信息反而更难找。

### 2. 需要搜索和回退时还坚持用 `more`

`more` 能分页，但它不是深度排查的大文件主力工具，这时候更适合 `less`。

### 3. 想看结尾却还在用 `head`

如果你关心的是最新日志或最后几十行，直接改用 `tail`。

### 4. 管道里习惯性先 `cat` 再 `head`

很多时候：

```bash
cat app.log | head
```

并不比：

```bash
head app.log
```

更有价值。

## 六、结论

`cat`、`head`、`more` 都不是复杂工具，但它们各自解决的是不同的“第一眼判断”问题。

小文件先看全貌用 `cat`，只想看开头用 `head`，长输出先临时分页用 `more`。先把这层分工想清楚，比记更多参数更实用。

