# 44｜权限、身份与提权｜切换用户后环境不对？su 最容易踩的坑有哪些

`su` 最容易让人误判的地方，不是命令不会写，而是“明明切到同一个用户了，为什么环境还是不一样”。

这类问题在运维里很常见：你想模拟服务用户执行命令、想临时切 root、想验证某个普通用户能不能写日志，但结果和预期完全不同。很多时候，区别就出在 `su user` 和 `su - user`。

如果只想先记 3 条：

- 排查环境问题时，优先用 `su - user`
- `su` 更适合临时切用户，长期授权和审计通常更适合 `sudo`
- 切换完成后，顺手看 `whoami`、`pwd` 和 `id`

## 一、什么情况下先用 `su`

`su` 最适合这些场景：

- 想临时切到 root 或其他用户做排查
- 想验证某个应用用户能不能访问目录、写日志、执行脚本
- 想模拟“如果我以这个用户登录，环境会是什么样”

最常见的几条写法是：

```bash
su -
su - deploy
su deploy
su - deploy -c 'whoami && pwd'
```

其中最值得优先记住的是带 `-` 的写法。

## 二、先记住这几条命令

```bash
su -
su - deploy
su deploy
su - deploy -c 'whoami && pwd'
whoami
id
pwd
```

它们分别适合：

- `su -`：切 root 并加载 root 登录环境
- `su - deploy`：切到 `deploy` 并加载该用户环境
- `su deploy`：只切身份，不完整加载登录环境
- `su - deploy -c '...'`：以目标用户执行一条命令后返回
- `whoami`、`id`、`pwd`：确认切换结果到底对不对

## 三、为什么 `su user` 和 `su - user` 差很多

### 1. `su user`

```bash
su deploy
```

这种写法会切换用户，但通常会保留较多当前 shell 环境。

这意味着你可能已经变成了 `deploy`，但：

- `HOME` 还不对
- 当前目录还停留在原用户目录
- `PATH` 仍然沿用之前的环境

### 2. `su - user`

```bash
su - deploy
```

这更接近“真正以这个用户重新登录一次”。在模拟用户环境、排查服务用户问题时，这种写法通常更可靠。

## 四、排查时重点看什么

### 1. 切换后先确认身份

```bash
whoami
id
```

不要只凭提示符判断自己已经切换成功。

### 2. 再确认目录和环境

```bash
pwd
echo "$HOME"
echo "$PATH"
```

很多“命令找不到”“配置没生效”“脚本行为异常”的问题，根本不是权限本身，而是环境根本没切到目标用户那一套。

### 3. 用 `-c` 验证单条命令

```bash
su - appuser -c 'touch /var/log/myapp/test.log'
```

这很适合验证：

- 服务用户能不能写某个目录
- 某个脚本以指定用户执行会不会失败
- 只想跑一条命令，不想真的切进一个交互 shell

## 五、`su` 和 `sudo` 怎么分工

### 1. `su`

更适合：

- 临时切换用户
- 模拟目标用户的登录环境
- 排查“换成这个用户执行会怎样”

### 2. `sudo`

更适合：

- 细粒度授权
- 审计谁执行了什么
- 不共享 root 密码的生产环境

如果只是想以另一个用户执行单条命令，也可以考虑：

```bash
sudo -u deploy command
```

## 六、最常见的误区

### 1. 用 `su user` 模拟登录环境

这往往会让你误以为“同一个用户为什么表现不一样”。真正需要模拟用户环境时，更推荐 `su - user`。

### 2. 切完用户不做确认

至少顺手看三样：

```bash
whoami
pwd
id
```

### 3. 长期把 `su` 当授权方案

在生产环境里，长期授权、命令审计、权限最小化通常都更适合交给 `sudo`。

## 七、结论

`su` 的关键不在于会不会切用户，而在于有没有切到正确的执行环境。

排查时优先用 `su - user`，切完马上看 `whoami`、`pwd`、`id`，这比只看命令有没有执行成功更重要。
