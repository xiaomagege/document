# 56｜文件与目录操作｜新建文件、改时间戳，touch 不只是“创建空文件”

很多人提到 `touch`，第一反应就是“创建空文件”。这没错，但这只是它最浅的一层用法。

在运维和脚本场景里，`touch` 更有价值的地方其实是时间戳控制。创建占位文件、刷新修改时间、模拟旧时间、做标记文件，这些都很常见。

如果只想先记 3 条：

- 文件不存在时，`touch` 会创建它
- 文件已存在时，`touch` 默认改的是时间，不是内容
- 想控制时间戳时，优先记 `-d` 和 `-r`

## 一、什么情况下先用 `touch`

`touch` 最适合这些场景：

- 创建占位文件或标记文件
- 初始化空日志文件
- 更新已有文件的时间戳
- 让一个文件继承另一个文件的时间戳

最常见的写法就是：

```bash
touch app.log
touch /tmp/deploy.done
touch -r source.txt target.txt
```

## 二、先记住这几条命令

```bash
touch app.log
touch file1 file2 file3
touch -c exists.txt
touch -d "2026-05-06 10:30:00" app.log
touch -r source.txt target.txt
```

它们分别适合：

- 创建或刷新单个文件
- 一次创建多个文件
- 只更新已有文件，不创建新文件
- 按可读时间字符串改时间
- 复制另一个文件的时间戳

## 三、它在脚本和排障里最常见的几种用法

### 1. 创建标记文件

```bash
touch /tmp/deploy.done
```

很多脚本会用这种方式表示某一步已经完成。

### 2. 初始化日志或占位文件

```bash
touch app.log
touch logs/.keep
```

这在需要保留空目录结构或提前准备日志文件时很常见。

### 3. 刷新时间戳

```bash
touch app.log
```

注意这不会清空文件内容，只会更新文件时间。

### 4. 对齐另一个文件的时间

```bash
touch -r source.txt target.txt
```

排查同步、备份、构建缓存等问题时，这种写法很实用。

## 四、什么时候该用 `-d`，什么时候该用 `-r`

### 1. `-d`

```bash
touch -d "1 day ago" app.log
touch -d "2026-05-06 10:30:00" app.log
```

适合你自己明确想指定一个时间点。

### 2. `-r`

```bash
touch -r source.txt target.txt
```

适合你想让两个文件时间保持一致，而不是自己手工写时间。

## 五、最常见的误区

### 1. 以为 `touch` 会清空文件

它不会。它只是创建文件，或者更新时间戳。

### 2. 不想创建新文件，却忘了 `-c`

```bash
touch -c exists.txt
```

如果你的需求只是“更新已有文件时间”，这条更稳。

### 3. 把“删文件再重建”当成刷新时间的方式

很多时候其实只需要 `touch`，没必要真的删除重建。

## 六、结论

`touch` 最值得掌握的，不只是“创建空文件”，而是它对时间戳的控制能力。

创建标记文件、刷新时间、继承时间，这三类场景掌握了，它就已经很实用了。
