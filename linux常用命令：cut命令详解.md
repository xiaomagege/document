# linux常用命令：cut命令详解

`cut` 是 Linux 中用于从文本行中截取指定字段或字符范围的命令。它非常适合处理格式稳定、分隔符明确的文本，例如 `/etc/passwd`、CSV、日志中固定字段、命令输出结果等。

相比 `awk`，`cut` 功能更简单，但也更轻量。只要需求是“按分隔符取某几列”或“按字符位置截取”，`cut` 往往足够好用。

---

## 一、`cut` 是什么？

`cut` 用于从每一行文本中截取指定部分。

它主要用于：

- 按字段提取内容
- 按字符位置截取内容
- 按字节位置截取内容
- 从冒号、逗号、Tab 等分隔文本中取列
- 配合管道处理命令输出

`cut` 是逐行处理的，不会理解复杂语法，也不适合处理字段数量不稳定或引号嵌套的 CSV。

---

## 二、常见使用语法与重点参数

```bash
cut [选项] [文件]
命令 | cut [选项]
```

常用参数说明：

- `-d 分隔符`：指定字段分隔符
- `-f 字段`：按字段提取
- `-c 范围`：按字符位置提取
- `-b 范围`：按字节位置提取
- `--complement`：取反，输出未被选中的部分
- `--output-delimiter=分隔符`：指定输出分隔符

字段或范围写法：

- `1`：第 1 个
- `1,3`：第 1 个和第 3 个
- `1-3`：第 1 到第 3 个
- `2-`：从第 2 个到最后
- `-3`：从开头到第 3 个

---

## 三、常用命令与使用场景

### 1. 按冒号提取字段

```bash
cut -d ':' -f 1 /etc/passwd
```

提取 `/etc/passwd` 中的用户名。

提取用户名和 shell：

```bash
cut -d ':' -f 1,7 /etc/passwd
```

### 2. 按逗号提取字段

```bash
cut -d ',' -f 1,3 data.csv
```

提取逗号分隔文本中的第 1 列和第 3 列。

注意：如果 CSV 字段中包含引号和逗号，`cut` 无法正确理解这种复杂结构，应使用专门的 CSV 工具或脚本处理。

### 3. 按 Tab 提取字段

```bash
cut -f 1,2 data.tsv
```

`cut` 默认字段分隔符是 Tab，所以处理 TSV 文件时可以不写 `-d`。

### 4. 提取字符范围

```bash
cut -c 1-10 file.txt
```

提取每行第 1 到第 10 个字符。

提取第 5 个字符之后的内容：

```bash
cut -c 5- file.txt
```

### 5. 提取固定前缀

```bash
cut -c 1-19 app.log
```

如果日志每行前 19 个字符是时间戳，可以用这个命令提取时间部分。

### 6. 取反输出

```bash
cut -d ':' -f 1 --complement /etc/passwd
```

输出除第 1 个字段以外的内容。

适合排除某列后保留其他内容。

### 7. 指定输出分隔符

```bash
cut -d ':' -f 1,7 --output-delimiter=' -> ' /etc/passwd
```

可以把输出字段之间的分隔符改成更易读的内容。

### 8. 从命令输出中取字段

```bash
df -h | awk 'NR > 1 {print $5}'
```

这个场景不推荐直接用 `cut`，因为 `df` 输出中的空格数量可能不固定。

如果字段分隔稳定，`cut` 更合适；如果分隔不稳定，`awk` 通常更稳妥。

### 9. 结合管道提取日志字段

```bash
grep 'ERROR' app.log | cut -d ' ' -f 1,2
```

如果日志字段之间是单个空格，可以提取日期和时间。

如果空格数量不固定，建议改用：

```bash
grep 'ERROR' app.log | awk '{print $1, $2}'
```

---

## 四、常见问题与建议

### 1. `cut` 默认分隔符是什么？

字段模式 `-f` 默认使用 Tab 作为分隔符。

如果文本用冒号、逗号或空格分隔，需要使用 `-d` 指定：

```bash
cut -d ':' -f 1 file.txt
```

### 2. 为什么 `cut -d ' ' -f 2` 结果不对？

因为多个连续空格会被 `cut` 当成多个分隔符，导致字段位置偏移。

处理连续空白分隔的文本，更推荐：

```bash
awk '{print $2}' file.txt
```

### 3. `cut` 和 `awk` 怎么选？

简单规则：

- 分隔符稳定、只取字段：使用 `cut`
- 分隔符是连续空白、需要条件判断或计算：使用 `awk`
- 需要复杂 CSV 解析：不要用 `cut`，使用专门工具

### 4. 中文文本可以用 `cut -c` 吗？

在 UTF-8 环境中，`cut -c` 通常按字符处理，但不同系统和 locale 设置可能影响表现。

如果按字节截取中文，可能切坏字符，所以对中文内容不要随意使用 `cut -b`。

---

## 五、常用命令速查

```bash
cut -d ':' -f 1 /etc/passwd
cut -d ':' -f 1,7 /etc/passwd
cut -d ',' -f 1,3 data.csv
cut -f 1,2 data.tsv
cut -c 1-10 file.txt
cut -c 5- file.txt
cut -c 1-19 app.log
cut -d ':' -f 1 --complement /etc/passwd
cut -d ':' -f 1,7 --output-delimiter=' -> ' /etc/passwd
grep 'ERROR' app.log | cut -d ' ' -f 1,2
grep 'ERROR' app.log | awk '{print $1, $2}'
```

---

## 六、总结

`cut` 是一个轻量、直接的字段截取工具。建议优先掌握：

```bash
cut -d ':' -f 1 file
cut -d ',' -f 1,3 file
cut -f 1,2 file
cut -c 1-10 file
```

它适合处理分隔符明确、字段位置稳定的文本。如果遇到连续空格、复杂条件、统计计算或复杂 CSV，使用 `awk` 或专门工具会更可靠。